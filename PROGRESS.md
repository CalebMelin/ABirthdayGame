# PROGRESS

PLAN-00 ✅ 2026-07-14 — scaffold, boot placeholder, GitHub + Vercel pipeline live; Caleb confirmed the placeholder loads on his phone
PLAN-01 ✅ 2026-07-14 — engine foundation: 8-scene flow, save system (33 tests of 46 total), self-hosted Press Start 2P + pixel UI kit, BootScene loader, Title v1, 22-level LevelSelect grid. Verified via headless-browser click-through of the entire flow (Title→…→Credits incl. all 22 level loops): 0 console errors, progress survives reload, crisp at desktop 1280×720 and phone-landscape 844×390 w/ touch taps.

PLAN-02 ✅ 2026-07-14 — bike physics & terrain, all acceptance criteria browser-verified (gas-only completion, backflip achievable+recoverable, deterministic-terrain unit tests, 60fps + no body leaks across restarts, this file updated). Every task went through implementer + spec review + code-quality review; details below. Task 1 (terrain generator) done 2026-07-14: `src/systems/terrain.ts` (`generateHeightmap` pure + `createTerrain` Matter/Graphics builder), `TERRAIN` tuning block in `constants.ts`, 21 new deterministic-heightmap tests. Task 2 (bike rig) done 2026-07-14: `src/systems/bike.ts` (`createBike` composite: chassis+head-sensor compound, 2 sprung wheels, gas/brake control laws, airborne + cumulative-rotation tracking, head-vs-terrain crash detection, leak-free `destroy()`), reshaped `BIKE_TUNING`, 37 new pure-helper tests (106 total, all green). Post-review fix: control laws run on the Matter world's once-per-engine-step `beforeupdate` hook (not per render frame), so feel is identical at 120 Hz / 60 Hz / frame drops. Tasks 3+4 (camera + GameScene v1) done 2026-07-14: Matter physics config in `main.ts`, GameScene rebuilt around a hardcoded 15000px test level (flat spawn/finish zones, two 55×520 jump ramps), arrows-for-pedals input, finish flag → LevelComplete, soft-fail overlay ("Oops! Go again 💛" verbatim) + `scene.restart()`, manual camera follow (`CAMERA` block: lookahead / soft vertical / speed zoom), `FAIL` block. Browser-verified via `scripts/playtest-drive.mjs` (playwright-core + system Chrome headless): gas-only run finishes in ~30s with 0 crashes, 0 console errors/warnings, Matter body count 93 baseline == after 3 restarts == after a real fail-restart, fail→playable ~395ms (<500ms). Two BIKE_TUNING nudges were REQUIRED for completability (gasSpinUpPerStep 0.012→0.05, airSpinStepPerStep 0.006→0.0025 — see DECISIONS.md + comments in constants.ts); task 6 must revisit both. Task 5 (debug overlay) done 2026-07-14: dev-only overlay in `GameScene.ts` (D toggles it), showing FPS, speed (px/s), airborne state, cumulative airborne rotation (degrees, resets-at-takeoff noted), crashed flag, and live Matter body count — the visible leak check for the "no body leaks after 3 restarts" criterion. Gated behind `import.meta.env.DEV` and inlined directly in `update()`/`create()` (not a separate method) so Vite's minifier actually dead-code-eliminates it from production — confirmed by grepping the built bundle for overlay-only strings (absent). Two ride-along fixes from the task-3 review also landed: `scripts/playtest-drive.mjs` now fails the exit code on any collected console/page error (previously collected-but-not-gating), and `updateCamera`'s doc got a breadcrumb about per-render-frame lerps being ~2x snappier at 120Hz + the dt-normalizing fix if task 6 needs it. Browser-verified (playwright-core): overlay toggles on/off, values live-update (speed 5→465 px/s while holding gas), body count stays 93 across a fail-restart, D-listener doesn't stack/break after restart, 0 console/page errors; `playtest-drive.mjs` still exits 0. Task 6 (feel-tuning pass) done 2026-07-14: the trick-input mechanic (bike.ts: mid-air/press-buffered pedal presses get full pitch authority, held-from-ground pedals get the stabilization assist + late authority ramp — 4 new pure functions, all unit-tested) resolves the core tension between "gas-only never flips/crashes" and "backflips achievable off the big ramp"; TEST_LEVEL's second ramp became a 336×106 collision-grid-aligned triangle KICKER with a flat run-up zone; anti-endo braking added (weak front brake + grounded brake-stability damping). Tuning journal in DECISIONS.md (incl. rejected gravity 0.8 experiment). Evidence via the new committed `scripts/playtest-tricks.mjs` (flip/gasonly/brake modes, per-physics-step telemetry, exit-code gated): backflip recipe lands ≥360° + clean upright landing 3/3 attempts from real spawn drives (−376/−508/−508° at landing); gas-only full run finishes ~31s with 0 crashes 3/3 runs, worst airborne chassis angle 40° (limit 140°), over the kicker +16° rotation (no flip, no crash); full-brake from top speed: grounded pitch 5–6° flat / 10–34° across descents over 6 runs (no endo, no crash; earlier "10°/38°" point figures were measurement artifacts — see the brake-gate DECISIONS entry); `playtest-drive.mjs` regression gate exits 0 (93 bodies stable across 3 restarts + fail, fail→playable ~397ms, avg FPS 61 asserted ≥55, 0 console errors). Post-review fixes: brake-mode exit gate now actually checks the descent figure (was flat-only — review-caught bug), measures grounded frames only, position-keys the flat stop, and reads telemetry through a named-index schema (`T`) instead of bare tuple positions; drive script gained the FPS assertion; playtest-out/ gitignored; docs corrected where the final quality review found them misleading (held-pedal authority is NOT always zero on the kicker's longest flights — the assist blend is the real safety net; level authors must re-validate gas-only survival on jumps with >0.7s airtime, warned in both constants.ts and the TEST_LEVEL comment). 136 tests green, build clean.

PLAN-03 ✅ 2026-07-15 — Controls & mobile UX: every automated acceptance criterion browser-verified AND 👤 CALEB STEP 3.1 done — Caleb played the test level on his actual phone and confirmed it looks and feels good (pedals comfortable, no lag, rotate-prompt + pause behave); no tuning needed. Five tasks, each run through implementer + spec-compliance review + code-quality review, then a final holistic integration review. Task 1: `src/systems/input.ts` — `createGameInput(scene)` merges keyboard (arrows + WASD; W/D=gas, S/A=brake) with a touch signal into `{gas,brake}`, replacing GameScene's direct cursor reads (`this.ended` gate preserved); pure `mergePedals` OR-merge unit-tested; dev debug-overlay toggle remapped D→backtick (D is now gas). Task 2: `src/systems/pedals.ts` — multitouch pedals (gas bottom-right / brake bottom-left, 144px faces over 480×300 corner hit Zones), `addPointer` top-up so both press at once, GENUINE pointerdown/up→setTouch edges with NO debounce (the backflip mechanic depends on real transitions), shown only on touch devices (`device.ts isTouchDevice()` = `maxTouchPoints>0 && (any-pointer:coarse)`, to defeat the Windows maxTouchPoints=10 over-report), screen-anchored and per-frame zoom-counter-transformed (pure `zoomCompensatedPosition/Scale`, unit-tested). Task 3: `src/systems/orientation.ts` — portrait DOM overlay (verbatim `Flip your phone sideways to ride! 🏍️`, `100dvh`) that, on touch devices only, auto-pauses by sleeping the RAF loop (`game.loop.sleep()/wake()`, composes with the pause menu); best-effort `screen.orientation.lock('landscape')`; `isTouchDevice` extracted to `src/systems/device.ts`. Task 4: `index.html` mobile hygiene (`overscroll-behavior:none`, `user-select`/`-webkit-touch-callout:none`, `user-scalable=no`, `touch-action:none` on canvas, apple-/mobile-web-app meta) + a reproducible 180×180 placeholder `apple-touch-icon.png` (`scripts/gen-app-icon.mjs`, PLAN-10 replaces it). Task 5: `src/scenes/PauseScene.ts` overlay (Resume / Restart / Level Select) launched over a paused GameScene, opened by a zoom-pinned ⏸ HUD button or Esc/P (Esc/P also resume from the menu). Integration fix (holistic review caught it): a touch pedal HELD into `scene.pause()` then released while the menu is up loses its `pointerup` (paused scenes stop dispatching pointer input) and would resume stuck-on — cleared now on scene RESUME (`PedalsHandle.releaseAll()` + a GameScene RESUME handler), with a red→green touch regression in `scripts/playtest-pause.mjs`. Verified: `npm run build` clean, 156 tests green, and FOUR committed exit-0 playtest harnesses — `playtest-drive` (gas-only finish ~31s, 93 bodies stable across 3 restarts + a real fail, fail→playable ~393ms, avg 61 FPS, 0 console/page errors), `playtest-touch` (both pedals held → brake-wins, desktop shows 0 pedals + keyboard still drives, 0px zoom drift), `playtest-orientation` (portrait overlay visible with exact text + loop asleep + bike frozen; landscape resumes; desktop never blocked), `playtest-pause` (⏸/Esc/P open the menu, Resume continues the same run, Restart respawns, Level Select navigates, ⏸ zoom-pinned, plus the stuck-pedal regression). Acceptance criteria (a) simultaneously-pressable pedals, (b) portrait pause + rotate-back resume, (c) desktop arrows work + no pedals on non-touch desktop: all MET in-harness. Judgment calls logged in DECISIONS.md.

PLAN-04 ✅ 2026-07-15 — Character creation: a palette-swap engine + marker-composite base textures (task 1 — `src/systems/palette.ts`'s `recolorTexture`/`MARKERS`/`ColorRemap`, canvas pixel-replacement cached per combination actually chosen, plus BootScene's `tex-gabby-base` (24x48) / `tex-bike-base` (96x28) marker art); character option data (task 2 — `src/data/characters.ts`: 6 hair / 5 eye / 8 bike / 5 outfit swatches, `blonde` the mandated default, `yellow` present-but-not-default for the level-11 easter egg, `riderVariantKey`/`bikeVariantKey` scoped independently so choosing a new bike color never regenerates the rider texture and vice versa, plus total fallback-safe resolvers and a TDD'd `randomCharacterConfig`); a live-preview `CharacterCreationScene` (task 3 — bounce + randomized-interval blink idle tells on the preview rig, four labeled thumb-friendly swatch rows with a decoupled 56px visible face / 88px hit area exactly like the touch pedals, the tightest 8-swatch BIKE row budget-checked AND regression-tested at 1264px inside the 1280px design width, Randomize/"Let's ride!" buttons, save-on-every-change funneled through one `applyConfigChange` path); in-game one-source-of-truth application (task 4 — `bike.ts`'s new `BikeOptions.textures` seam plumbed through GameScene's `create()`, which reads `getSave().loadCharacter()` and builds textures via the SAME `buildCharacterTextures` the live preview calls, so preview and real rig can never silently drift apart; wheels always stay the raw, un-recolored placeholder); and task 5's first-run flow, verified end-to-end by the new committed `scripts/playtest-character.mjs` (47 checks, exits 0): confirmed `TitleScene.ts`'s routing (`loadCharacter() === null` → CharacterCreation, else → LevelSelect; "Edit Character" always → CharacterCreation) was ALREADY correct since PLAN-01/persisting since task 3 — only its now-stale pre-PLAN-04 code comment needed rewording, no logic changed; defaults-on-first-open (blonde hair, `tex-gabby|blonde|blue|classic` preview key); 6 distinct combos (incl. an all-defaults, a yellow-bike, an all-black/stealth, and a pink-hair+teal-bike combo, each of the 6 hair swatches used exactly once) rendering correctly in BOTH the live preview AND, after entering a level, the actual in-game chassis (DEPTHS.bike)/rider (DEPTHS.rider) sprites read directly off `GameScene.children.list` (`BikeHandle` never exposes its sprites), wheels confirmed to stay raw `tex-wheel` throughout; persistence across a real page reload set via genuine swatch clicks (not just seeding), both the scene's re-read config/preview AND raw localStorage checked; the key regression guard — editing the character while `gabby22.progress` holds a partially-completed save (8/22 levels done, `highestUnlocked`=9) leaves progress byte-for-byte unchanged, read from real localStorage before AND after the edit, with the character write itself also confirmed to have genuinely happened; full touch usability on a 1280x720 phone-landscape context via real CDP-backed taps on every swatch row — including the LAST (yellow) swatch of the tight 8-wide BIKE row, proving the >=88px hit target is reachable at the tight end, not just centered — plus Randomize and "Let's ride!" by touch; and first-run routing proven in all 4 combinations (Play/Edit Character × with/without a saved character). 0 console/page errors throughout; screenshots of every combo + the touch session saved to `playtest-out/` (gitignored). `npm run build` clean, 233 tests green (unchanged from the task-4 checkpoint — task 5 adds no new pure logic), and all four pre-existing committed harnesses (`playtest-drive`, `playtest-touch`, `playtest-pause`, `playtest-orientation`) plus `playtest-tricks.mjs gasonly` re-run against the finished PLAN-04 code and confirmed still green (the pre-existing, separately-tracked `playtest-tricks.mjs flip`-mode flakiness noted below was not re-touched — character creation doesn't touch bike/trick physics).

PLAN-05 ✅ 2026-07-15 — Data-driven level system: replaced GameScene's single hardcoded `TEST_LEVEL` with 22 real per-level `LevelConfig`s + a theme system, so every level plays end-to-end (special events stubbed for PLAN-06/07). Executed via `superpowers:subagent-driven-development` — 5 tasks, each a fresh implementer subagent + spec-compliance review + code-quality review, then a final holistic review; no CALEB STEP in this plan. ST-1: `src/levels/types.ts` — `LevelConfig`, `LevelEvent` discriminated union (traffic/police/calebPickup/wheelieRider/billboard), 15-theme `ThemeId`, `DecorationSpec`, `getLevelTerrainSpec`, total `validateLevels`, plus a `LEVEL` constants block (length bounds chosen to keep every level < 100 physics bodies). ST-2: `src/systems/themes.ts` — `THEMES: Record<ThemeId, ThemeDef>` (15 visibly-distinct themes across the suburbs→…→final-dusk arc) + `createBackdrop` parallax handle (real per-layer scrollFactor parallax, camera-fixed sky oversized `DESIGN/zoomMin` so it never gaps under the play camera's zoom — derived + tested against actual Phaser 3.90 source, not guessed). ST-3: 22 `src/levels/levelNN.ts` + `index.ts` (`LEVELS`, total `getLevelConfig`), authored gas-only-safe (gentle WIDE raised-cosine humps only — NO flip kickers, which return in PLAN-07; hilliness ≤ 0.30 on every jump-bearing level; jumps placed mid-level ≥ 1500px from spawn/finish), the level-18 easter-egg billboard text byte-exact, warm PG-13 names + intro one-liners telling the race-to-the-party arc, all rendered copy ASCII-only for pixel-font safety. ST-4: `GameScene` is now config-driven — terrain from `getLevelConfig(level)` via `getLevelTerrainSpec` + the theme's ground colors, `createBackdrop` parallax, `createDecorations` (new `src/systems/decorations.ts`: sign/billboard/balloon/streamer placeholders, 0 Matter bodies), `dispatchLevelEvents` (new `src/levels/events.ts`: exhaustive no-op stubs, never throws), and a name + one-liner intro banner (cream panel, legible on every theme, suppressed on fail-restart) — all ADDITIVE on top of the PLAN-02-tuned bike/camera/pause/fail rig, which is byte-unchanged (`git diff` of `bike.ts`/`terrain.ts` across the whole plan is EMPTY). ST-5: `scripts/playtest-levels.mjs` ghost driver (drives all 22 gas-only, gates on event-free completion + no crash-restart + < 100 bodies + zero errors) + `src/levels/validate.ts` (`validateJumpSafety`/`validateFlatZones` pure guards + tests) + reconciled `playtest-tricks.mjs`. Verified: `npm run build` clean, 326 tests green (incl. `validateLevels`/`validateJumpSafety`/`validateFlatZones` against the real 22 + a byte-exact level-18 billboard guard). Ghost driver on the final HEAD: **all 22 levels finish gas-only, 0 restarts, 0 console/page errors, max body count 99 (< 100 §8 budget), 17.5s–32.5s each**; and all 7 committed harnesses (`playtest-levels`/`drive`/`touch`/`pause`/`orientation`/`character` + `tricks gasonly`) exit 0. Acceptance criteria all met — 22 selectable + gas-only-beatable, config validation passes + event levels dispatch stubs error-free, themes visibly change across the arc, intro one-liners show at level start. Judgment calls (incl. the controller finishing ST-5's commit after its implementer subagent hit a session limit) in DECISIONS.md.

PLAN-06 ✅ 2026-07-16 — Special levels 7/12/15, executed via `superpowers:subagent-driven-development` (4 tasks, each a fresh implementer + spec-compliance review + code-quality review, then a final holistic integration review; no CALEB STEP). Task A (foundation): the reusable event seam — `dispatchLevelEvents` now returns `LevelEventHandle[]` (update-per-frame / destroy-on-shutdown / optional `onFinish()` finish-delay) driven by GameScene, with an `EventContext` (bike/terrain/finishX/worldLength/calebPickedUp/passenger/`isEnded()`/`softFail(message)`/`setInputOverride`); `failLevel(message)` parametrized with word-wrap + long-message sizing (default `Oops! Go again 💛` byte-unchanged); persistent Caleb pillion passenger (`src/systems/passenger.ts`, one cosmetic sprite pinned behind Gabby with independent bob, ZERO Matter bodies, no handling change) keyed off pure `deriveCalebPickedUp` (save.ts): solo < 12, flips mid-level-12 at pickup, aboard 13–22 from `completed[11]` — read-only, never stored. `bike.ts` byte-unchanged. Task B: level 7 invisible-cars traffic (`src/systems/traffic.ts`) — pooled non-Matter cars in a two-lane telegraph-then-drift-into-lane model (≥3s telegraph, 6 encounters with breathers, encounters 4–5 punch-through-able so BOTH plan dodges exist), collision = byte-exact `They really don't see us!! Go again 💛` soft-fail + instant restart; car motion/spawn/collision on the fixed 60 Hz `beforeupdate` step (refresh-independent — review-caught fix, dt-displacement unit test), the ONE level gas-only deliberately does not clear. Task C: level 12 pickup (`src/systems/pickup.ts`) — Caleb's house + byte-exact `MELIN` mailbox + brown-haired standing Caleb (brown per DECISIONS, distinct from blonde Dom), approach → auto-brake via input override → stop → ♥ + byte-exact `Caleb hopped on!!` toast → `passenger.activate()` → control returns (~1.8s, never fails the player); terminal 5-phase state machine, all wall-clock timed. Task D: level 15 police chase (`src/systems/police.ts`) — one non-Matter cop with flashing red/blue lights, rubber-band speed = min(rollingAvg + catchup, hardCap) on the fixed step, hardCap tuned below the MEASURED climb-minimum cruise (0.45×top ≈ 4.86 < 5.9 px/step) so a gas-holder is NEVER caught (kinematically at cruise, harness-gated at spawn transient); caught = 200px for >1.5s (timer resets on gap reopen) → byte-exact `They got us!! ...let's pretend that didn't happen 🚔` (form confirmed by Caleb 2026-07-16 over NORTH_STAR §5's one-`!` prose — see DECISIONS.md) + restart; finish → self-driving `WOOHOO!` + cop spin-out finale via `onFinish()` ~1.2s hold. Verified on the final HEAD: `npm run build` clean, **393 tests green**, THREE new committed harnesses exit 0 — `playtest-level07.mjs` (dodge-beatable at BOTH cautious and max-speed-reactive profiles, 4/4 each, gas-only correctly soft-fails verbatim, no unavoidable encounter, 75 bodies), `playtest-level12.mjs` (cutscene triggers/stops/resumes, gas-only 3/3, persistence: level 13 → Caleb pillion / level 11 → solo, 78 bodies), `playtest-level15.mjs` (gas-only 3/3 NEVER caught, closest gap 441px; stopped run IS caught → verbatim toast + restart; finale renders; Caleb pillion; 87 bodies) — plus the full 22-level `playtest-levels.mjs` sweep exit 0 (every event-free level finishes gas-only with 0 restarts, 11/12/15/18 finish gas-only, 7 by-design does not [WARN-only], max bodies 99 < 100 — ALL new systems add ZERO Matter bodies, 0 console/page errors) and all 6 pre-existing harnesses (`drive`/`pause`/`touch`/`orientation`/`character` + `tricks gasonly`) re-run green. Difficulty gates met: L7 ≥3-in-a-row both profiles, L12 3/3, L15 3/3. Judgment calls (Caleb brown hair, cop hard-cap vs measured cruise, L15 message form confirmation) in DECISIONS.md.

PLAN-07 ✅ 2026-07-16 — Tricks, tulips & easter eggs (4 tasks; no CALEB STEP). Task 1: trick/tulip system (`src/systems/tricks.ts`) — fixed-step landing detection off `BikeHandle.airborneRotation`, a forgiving `flipThresholdDeg`=330 award rule, instant persistence via `save.addTulips` (`gabby22.tulips`), byte-exact `Backflip!! 🌷`/`Frontflip!! 🌷` toasts, and a zoom-pinned top-right bouquet HUD (single→bunch→bouquet) — ZERO Matter bodies. Task 2: level-11 wheelie-rider easter egg (`src/systems/wheelieRider.ts`) — all-black rider on a recolored-yellow bike wheelies past once and rides off, non-interactive, 0 bodies. Task 3: level-18 sleepover billboard (`src/systems/billboard.ts` + shared `drawBillboard`) — the byte-exact egg rendered on the SAME world-anchored props layer/frame as its decoys, word-wrapped so size never gives it away. Task 4 (jump/ramp pass — THIS session): flip-capable KICKERS on levels 2/4/9/17. Introduced a distinct `kind:'kicker'` on `terrain.ts`'s `JumpSpec` with its own validated bounds (a new `LEVEL` kicker block + `validate.ts` grid-alignment/slope guards; hump floors left intact — see DECISIONS), exported the 168px collision-grid constants, and authored 2/2/2/3 uniform grid-aligned 336×106 kickers replacing the old gentle humps, plus level 2's byte-exact tutorial sign `Big jump ahead — try holding GAS in the air to flip! 🌷` (em dash + 🌷 screenshot-verified to render, not tofu). `scripts/playtest-tricks.mjs` flip mode REACTIVATED against level 2's kicker (release recipe retuned to −4.5 rad to land the flip clean at ~−390° instead of over-rotating into a crash). Verified on the final HEAD: `npm run build` clean, **484 tests green** (+17), and the full gate sweep — `playtest-levels.mjs` **all 22 finish gas-only, 0 crash-restarts, max bodies 99 < 100, 0 errors** (L2/4/9/17 at 21.4/23.5/26.9/31.9s, bodies 63/69/78/90, kickers add ZERO bodies); `playtest-tricks.mjs flip` **3/3 land ≥360° clean off the kicker (−388° to −392°), each firing the byte-exact Backflip toast + persisting a tulip, `gabby22.tulips 0→3` survives reload** (the POSITIVE award path Task 1 could only prove negatively, now closed end-to-end); `playtest-tulips.mjs` gas-only level-2 over both kickers awards 0 (max 16.8° — no accidental flips), bodies still 63; `playtest-level11.mjs`/`playtest-level18.mjs` both exit 0 (bodies 81/90, unaffected). The SAME kicker a deliberate mid-air gas tap backflips off is cleared upright by a gas-only hold — NORTH_STAR §4 "flips achievable but optional" made physically real, gas-only survival re-proven on all 22. Judgment calls (kicker-vs-hump validation, per-level geometry, em-dash glyph, flip-harness re-aim) in DECISIONS.md.

PLAN-08 ✅ 2026-07-21 — Level-complete screens, notes system & story polish, executed via `superpowers:subagent-driven-development` (4 tasks, each a fresh implementer + spec-compliance review + code-quality review, then a final holistic integration review + a full committed-harness sweep; no CALEB STEP). Tasks were run notes-engine-FIRST (2→1→3→4) so the complete screen could consume a finished, tested engine. Task 2: `src/data/notes.ts` (four byte-exact fixed level-bound notes L6/L9/L13/L14, authored PURE-ASCII per NORTH_STAR §6 / Rule 4, each style `'hint'`; a 20-entry fun-fact pool, every fact fact-checked TRUE and normalized to ASCII + escaped-emoji) + `src/systems/notes.ts` (`selectNote(level,save,rng)` — fixed notes override + NEVER consume the pool, other levels draw a fact WITHOUT repetition tracked in `gabby22.notesSeen`, exhaustion judged by POOL coverage then recycled via a new `save.resetNotesSeen()`) + unit tests (independent byte-exact/ASCII oracle per fixed note, mapping, no-repeat, exhaustion reset, pool invariants). Task 1: the real `LevelCompleteScene` — `Level N complete!! 🎉` header, self-cleaning confetti, tulips EARNED THIS LEVEL + bouquet TOTAL, a pixel-postcard note card (`Did you know?` facts / `Psst… 💡` hints) with a skippable typewriter revealing the PRE-wrapped note (no reflow, emoji-safe code-point units), and Next/Replay/Level-select buttons; `GameScene` now snapshots `getTulips()` at a FRESH level start (not on a fail-restart) and, on finish, `markLevelCompleted` HERE (moved out of LevelComplete so level 22 — which now SKIPS the congrats screen for PartyScene — is still marked) then routes 1-21 → `LevelComplete{level,tulipsAtStart}` / 22 → PartyScene; `earned = total − snapshot` correctly counts awards that persist during a finale hold. Task 3: fail/restart polish — a generic soft-fail message POOL (random for the default fail; the special traffic/police messages stay byte-exact/verbatim) + a `Try again` restart BUTTON (the instant path, preserving PLAN-02's <500ms budget) + a gentle 2.5s no-input auto-restart, all via ONE idempotent restart latch. Task 4: story-cohesion pass (arc already strong — ONE surgical intro de-dup on level 11) + an end-to-end notes rotation/reload harness. Verified on HEAD `feb155f`: `npm run build` clean, **511 tests green** (+27), the holistic integration review returned SHIP (earned-tulips-through-finale-hold correct, no mark-complete gaps/double-marks, no locked-content regression, no scene-transition leaks), and the **FULL committed-harness suite exits 0** — `playtest-{drive,touch,pause,orientation,character,tulips,fail(new),levelcomplete(new),notes-rotation(new),level07,level12,level15,level11,level18}` + `tricks{flip,gasonly,brake}` + the 22-level `playtest-levels.mjs` sweep (all finish, level 22 → PartyScene, max bodies 99 < 100, level 7's by-design gas-only non-finish WARN-only as always). All acceptance criteria met (the four verbatim notes at L6/L9/L13/L14, non-repeating facts elsewhere + reload-preserved seen-set, confetti + tulip tally + touch/desktop buttons). Judgment calls in DECISIONS.md.

PLAN-09 ✅ 2026-07-22 — Party finale & Credits (the emotional payoff), executed via `superpowers:subagent-driven-development` (6 subtasks, each a fresh implementer + spec-compliance review + code-quality review, then a final holistic integration review + a full committed-harness sweep; no CALEB STEP). The whole finale flows end to end with NO dead ends: level-22 finish → scripted arrival → PartyScene → (Credits →) CreditsScene → (Play again?) Title, plus a post-game Title "Party 🎈" revisit entry. ST-1: `src/data/finale.ts` (the ONE home for PLAN-09's VERBATIM personal content — the four names Andrea/Allison/Dallas/Dom, banner `HAPPY 22nd GABBY!!`, toast `You brought N tulips to the party!! 🌷`, tally `🌷 × N collected`, and the three credits lines — each defined once and imported everywhere, byte-exact guarded in `tests/finale.test.ts` by independent `String.fromCodePoint` oracles) + `src/systems/partyCast.ts` (Gabby + brown-haired Caleb via the shared `calebFigure.ts` + four named guests with bobbing name tags + 8 unnamed crowd, all body-free 2-frame-bounce pooled GameObjects; **Dallas is a STRUCTURAL texture-twin of the player's Gabby** — her roster entry `{kind:'mirrorsPlayer'}` has no authorable colors, so `castTextureSource` routes both to the one `buildCharacterTextures` key; unit-tested + harness-gated before/after a character change). ST-2: `src/systems/confetti.ts` (ONE shared burst + continuous-fall system, exactly frame-rate-independent closed-form integration, pooled, `import type Phaser`; `LevelCompleteScene` migrated onto it, values byte-identical) + `src/systems/partyBalloons.ts` (32 pooled tappable/clickable balloons — click AND tap, per-pointer dedupe, idempotent pops with a radial confetti puff, endless recycle; the ≥20-visible floor made STRUCTURAL — both flight endpoints derived from the art so the alive-but-offscreen band is zero-length; survives sustained popping at ≥26 visible). ST-3: the real `src/scenes/PartyScene.ts` (warm dusk-`finalDusk` venue, streamers, banner, continuous confetti, the cast, the balloons, the bouquet payoff welded to Gabby's bounce via `phase01`+`castBounceOffsetPx`, a 4s-fade-in "Credits →" button; zero Matter, per-entry reset + SHUTDOWN teardown, DEV `__party`) + `scripts/playtest-party.mjs`. ST-4: the real `src/scenes/CreditsScene.ts` (dark field, the three credits lines revealed line-by-line + tap-to-skip, a divider, the `🌷 × N collected` tally — both non-ASCII glyphs INK-proven real, not tofu — a tiny coral heart, "Play again?" → Title keeping progress, "Fresh start" → an in-scene confirm → `resetAll()`; all rendered copy now lives in `data/finale.ts`, none authored in the scene) + `scripts/playtest-credits.mjs` + a shared `createPixelButton` fix in `src/systems/ui.ts` (a **per-pointer press-latch**: `pointerup` fires `onClick` only if that pointer pressed DOWN on the button — kills release-over activation game-wide, keyed per-pointer so multi-touch/`leaving`-guard semantics are preserved). ST-5: `src/systems/arrival.ts` (a body-free 4-phase level event on the PLAN-06 seam — `approaching`→`ridingIn`→`crawling`→`arrived`; the ride-in takes the pedals via `ctx.setInputOverride` BEFORE the flag, a bang-bang crawl controller rolls the bike to the doors, both dismount [Caleb off the pillion, Gabby off the bike], doors open with light spilling, then the hand-off; **never-fail hardened STRUCTURALLY** — forced pedals gate on `!airborne` so they can't become trick input, and the takeover requires the bike already moving so gas from a dead stop on a climb can't stall it, with `onFinish()` opening the venue regardless) + `src/systems/calebFigure.ts` (the brown-haired-Caleb figure extracted from its 3rd verbatim replica; pickup/party/arrival all migrated). ST-6: the post-game Title "Party 🎈" button, shown only when `hasBeatenGame(progress)` (a pure total helper keyed off `completed[TOTAL_LEVELS-1]`, no new persisted flag — level 22's existing `markLevelCompleted` sets it) + `scripts/playtest-title-party.mjs`. Verified on HEAD (final coordinator commit): `npm run build` clean, **782 tests green** (+271 over PLAN-08's 511), the holistic integration review returned **SHIP** (no dead ends/leaks across any scene boundary, verbatim content single-sourced, Dallas twin structural, body budget intact, 60fps hot paths allocation-free, the `bike.ts` change contained, the destructive path unreachable without a deliberate confirm), and the **FULL committed-harness suite exits 0** — all 20 `playtest-*.mjs` (incl. the four new `party`/`credits`/`arrival`/`title-party`) + `tricks{gasonly,flip,brake}` + the 22-level `playtest-levels.mjs` sweep (all finish, **level 22 → arrival → PartyScene, max bodies seen 99 < 100**, level 7's by-design gas-only non-finish WARN-only as always). All acceptance criteria met. Judgment calls in DECISIONS.md.
- **NOTE — `bike.ts` is no longer "byte-unchanged since PLAN-02":** ST-5 added ONE additive cosmetic `setRiderVisible` seam (36 insertions / 0 deletions — no control law, tuning, body, constraint, `update()` ordering or `destroy()` touched) so Gabby's dismount is visible; this was a coordinator override of the earlier light-wash staging. The browser-measured FEEL the invariant protected was re-proven on the final HEAD by the full `tricks{gasonly,flip,brake}` + `drive` + 22-level sweep. Future `bike.ts` edits should still assume feel-critical unless equally additive-and-cosmetic.

PLAN-10 🚧 ST-1 + ST-2 + ST-3 + ST-4a + ST-4b done 2026-07-23 + ST-5 + ST-6 + ST-7a done 2026-07-24 — Art pipeline FOUNDATION + first real proof assets (art/audio/juice plan, in progress). Built a zero-dependency Node ESM pixel toolkit (`src/art/lib/framebuffer.mjs` RGBA framebuffer + `png.mjs` byte-deterministic PNG encoder via built-in `zlib`; NOT node-canvas, NOT screenshots — see DECISIONS 2026-07-23) with `.d.mts` type decls so the TDD suite imports them; a shared palette + STYLE GUIDE (`src/art/palette.mjs`) whose four MARKERS are asserted equal to `src/systems/palette.ts`'s; per-asset generators (`sprites.mjs`/`props.mjs`) + the `npm run art` runner (`src/art/build.mjs`, replacing `scripts/noop.js`); and a BootScene **load-or-generate** refactor (`src/systems/artManifest.ts` → real PNG for manifested keys, placeholder rectangle for the rest — no call site changed). First real committed art (`public/assets/sprites`, `public/assets/props`): recolorable `tex-gabby-base` (24×48, exact hair/eyes/suit markers inside the blink band) + `tex-bike-base` (96×28, exact bikeBody marker), their default-colored raw `tex-gabby`/`tex-bike` fallbacks, `tex-wheel` (36×36), `tex-flag` (24×64) — all EXACTLY their `TEXTURE_SPECS` sizes. `npm run build` clean, **796 tests green** (+14 `art-png`), `npm run art` byte-deterministic (2nd run leaves git clean), and browser-verified: `playtest-character.mjs` exit 0 (recolor still works off the REAL loaded base PNGs — the key risk), `playtest-drive.mjs` exit 0 (real bike/wheel/flag render), `playtest-levels.mjs` 22-level sweep exit 0. Remaining PLAN-10 art (cars, police, party cast, props/backdrops), audio (ST-7), and juice are still to come; those textures still render placeholders. **ST-2 (real character art):** replaced the last two character placeholders with committed 24×48 PNGs — `tex-caleb` (brown-haired per NORTH_STAR §5, `steelBlue` casual top + `slate` jeans, fixed colors / no markers) and `tex-wheelie-rider` (the level-11 all-black rider + black helmet + grey visor). New `drawCaleb`/`drawWheelieRider` generators + `build.mjs`/`artManifest.ts` rows + 24×48 dimension guards. Because tex-caleb now bakes the brown hair in, the redundant brown hair-band overlays collapsed: `calebFigure.ts` is a plain Image, `partyCast.ts` dropped its caleb-band branch, and the pillion Caleb (`passenger.ts`, unchanged) reads brown-haired for free. `wheelieRider.ts` now uses the loaded PNG and dropped its dead lazy rider-texture generation (the yellow `bike-base`→yellow recolor stays lazy — real-art recolor, so the egg's bike is real art for free). Pose/animation frames (ride-cycle/crash/wave) are DEFERRED (feel-critical bike.ts seam vs the EASY/CUTE mandate; not a placeholder-rectangle gap) — see DECISIONS 2026-07-23. `npm run build` clean, **805 tests green** (+2 dimension cases), `npm run art` byte-deterministic, and EACH browser harness exit 0 (`playtest-level11`/`level12`/`party`/`arrival`/`character`/`drive` + the 22-level `playtest-levels.mjs` sweep exit 0 — all finish gas-only, max bodies seen 99 < 100, 0 console/page errors). **ST-3 (vehicle art):** real `tex-car` + `tex-police-car` (both 110×40, new `src/art/vehicles.mjs` with procedural `drawCar`/`drawPoliceCar`). The car body is near-white so `traffic.ts`'s existing 5-tint cycle (UNCHANGED) yields 5 distinct pastel cars — the plan's "4-5 variants" via the tint model; the police car's roof light-bar is aligned to `police.ts`'s exact flash-rect positions (red at `cop.x-13`, blue at `cop.x+13`) so NO `police.ts` change was needed. Both plain Images (0 Matter bodies). `npm run build` clean, **817 tests green** (+4 car/police dimension+freshness), `npm run art` byte-deterministic, and browser harnesses exit 0: `playtest-level07` (5-tint cars dodge-beatable), `playtest-level15` (cop + red/blue flash on the light-bar, caught+escape), `playtest-drive`/`character`, and the 22-level `playtest-levels.mjs` sweep (max bodies 99 < 100, 0 errors; one transient `__gabbyGame` page-context dropout mid-sweep re-ran clean — not an art regression, since level07/level15/drive all pass on this build). **ST-4a (world props + roadside scenery):** the LAST TWO placeholder `TEXTURE_KEYS` textures are now real committed PNGs in `src/art/props.mjs` — `tex-tulip` (16×24, a pink `coral`/`bgPink` bloom over a `grass` stem+leaf, used AS-IS in the bouquet HUD / party bouquet / LevelSelect badge / LevelComplete tally; the pink read fixes the old green-box-mistaken-for-a-balloon party bug) and `tex-balloon` (24×32, near-white body so the runtime `setTint` in decorations.ts/partyBalloons.ts yields distinct pastel balloons like the ST-3 car, + a `overcast` roundness crescent + a dark tied knot; string drawn by callers). Every `TEXTURE_KEYS` texture now has real art. The roadside scenery STAYS PROCEDURAL (dynamic text / size-to-fit / 0 Matter bodies) but was improved into intentional pixel art: `decorations.ts` `drawSign` (wood post + foot + heart topper), `drawBillboard` (foot + metal pole highlight + dark backing frame — the level-18 egg stays BYTE-EXACT and visually identical to its decoys, all new frame parts NON-cream so the harness's cream-board census is untouched), `drawStreamer` (pennant bunting vs a plain zigzag); `pickup.ts` house → a cottage (outlined peaked roof + eave + chimney + paned windows + door knob) and the MELIN mailbox → a rounded box + red flag with the byte-exact nameplate on its front (stale ST-2 hair-band comment fixed). `npm run build` clean, **822 tests green** (+tulip/balloon dimension+freshness), `npm run art` byte-deterministic (only the 2 new PNGs added; all prior committed PNGs unmodified), and EACH browser harness exit 0: `playtest-level18` (egg byte-exact + subtle, 90 bodies), `playtest-level12` (cottage + MELIN + pickup works, 78 bodies), `playtest-tulips`, `playtest-party` (32 tinted balloons + real-tulip bouquet payoff), `playtest-levelcomplete`, `playtest-drive`, and the 22-level `playtest-levels.mjs` sweep (0 over-budget, 0 console/page errors; level 7 the usual by-design WARN). **ST-4b (UI + app chrome — the remaining non-texture placeholders):** the pedal glyphs, phone-rotate art, and app/tab icon are now real. `pedals.ts` `drawGlyph` — GAS is a chunky forward `>>` double-chevron and BRAKE a bold stop-sign octagon (still drawn procedurally on Graphics in `PALETTE.plum`, `fillPoints` fed plain `{x,y}` so pedals.ts stays runtime-Phaser-free; NO sizing/hit-area/press/zoom change). `orientation.ts` — the placeholder phone emoji became an inline crisp-edges pixel-art phone SVG (plum handset + sky screen + coral heart, colors from the shared PALETTE) reusing the existing wobble; the verbatim overlay text `Flip your phone sideways to ride!` (+ motorcycle emoji) is BYTE-UNCHANGED. App icon — new `src/art/icon.mjs` `drawAppIcon` (180x180 cute tulip over a lavender->pink gradient + confetti) rendered by the deterministic pipeline via a new `ROOT_ASSETS_TO_BUILD` registry that `build.mjs` writes to `public/apple-touch-icon.png` (the site root, not public/assets/); the old headless-Chrome `scripts/gen-app-icon.mjs` is DELETED. `favicon.svg` redrawn to the matching tulip (vector, index.html unchanged). `ui.ts` LEFT UNTOUCHED (behavior frozen — the PLAN-09 press-latch is load-bearing). App icon added to the committed-PNG dimension + freshness tests via a new `readCommittedRootAsset`. `npm run build` clean, **824 tests green** (+2 app-icon dimension/freshness), `npm run art` byte-deterministic (identical sha256 on 2nd run), and EACH required browser harness exit 0: `playtest-orientation` (phone overlay + BYTE-EXACT text, loop asleep/resume, desktop never blocked), `playtest-touch` (both pedals pressable + glyphs render + multitouch/zoom-stability — one transient `__gabbyGame` flake re-ran clean), `playtest-pause`/`playtest-title-party`/`playtest-credits` (button behavior unchanged), `playtest-drive` (0 console/page errors). Every `TEXTURE_KEYS` texture AND every UI/app-chrome placeholder is now real art. **ST-5 (per-theme backdrop art):** the 15 city-arc backdrops (`src/systems/themes.ts`) went from flat silhouette bands to cohesive pixel-art places. New sibling `src/systems/backdropArt.ts` (same no-runtime-Phaser contract) holds a STEPPED-gradient sky (K flat oversized camera-fixed bands interpolated top->bottom by integer RGB lerp — renderer-safe on Canvas + WebGL, no `fillGradientStyle`, no committed PNG), an optional celestial disc (dawn/low-sun/moon), drifting clouds, and EIGHT reusable deterministic motif drawers (hills / trees / buildings-with-lit-windows / houses / bridge-truss / cranes / water / party-bunting) dispatched per layer via a new `ParallaxLayerDef.motif` field + `MOTIF_DRAWERS` table. Each layer paints into ONE batched Graphics across the full padded `backdropContentRangePx` at `scrollFactor(fx,0)`; variation is a pure `hash01(index)` (never RNG) so shapes stay deterministic. Cloud DRIFT put the previously-no-op `BackdropHandle.update()` to work: it reads `scene.time.now` (not `Date.now`) and slides pooled clouds, wrapping them off-screen — ALLOCATION-FREE per frame. All ST-5 invariants preserved: zoom-safe oversized sky + full-range parallax layers (no gaps at any scroll/zoom), ZERO Matter bodies, leak-free `destroy()`, no runtime Phaser import. `npm run build` clean, **892 tests green** (+68: new `tests/backdropArt.test.ts` pure-helper suite + themes motif/celestial/clouds checks; the 4 original zoom-safety helpers + tests untouched), `npm run art` byte-deterministic (ST-5 adds NO PNGs). Browser EACH exit 0: `playtest-drive` (level-1 render, avg 60.9/min 60.6 FPS — clouds don't tank framerate — 0 errors) and the 22-level `playtest-levels.mjs` sweep (all finish gas-only, 0 restarts, maxBodies 99 < 100, 0 console/page errors, L7 the usual WARN). Per-theme in-game backdrops eyeballed (throwaway script, deleted): all 15 distinct — lit-window skylines, bridge truss, twin house rows, river shimmer, cranes, tree-line, sunset low-sun, and finalDusk's moon + warm-glow skyline. Absorbs the plan's "soft parallax cloud drift" juice item. **ST-6 (party finale art):** the level-22 arrival venue, the PartyScene venue + banner, and named-guest distinctness went from placeholder to real. `arrival.ts`'s brown-box venue (DECISIONS' "weakest thing on screen") is now a cute warm party BUILDING — `brickRed` walls + `brown` wainscot + cream pilasters, a `plum` pitched roof, paned `sunshine`-lit windows, a striped coral/cream **marquee awning** over the door, a swagged multicolour bulb **garland**, and a coral **heart sign** — all runtime Graphics, ZERO Matter bodies. **The never-fail choreography + door alignment were NOT touched:** `doorX`/`DOOR_WIDTH_PX`/`DOOR_HEIGHT_PX`/the panels/the spill/`VENUE_WIDTH_PX` (300) are byte-identical, so the crawl still rolls the bike to the same doorway, the doors open + light spills, and `ARRIVAL_DISMOUNT_OFFSETS` needed NO re-tuning (the doorway centre/width it is tuned against are unchanged) — the new art is drawn AROUND the fixed opening. `PartyScene.ts` `drawVenue` now shows the SAME brick building with warm windows over the back fence (arrival↔party continuity), `drawStringLights` went multicolour, `drawStreamers` got pennant tips, and `drawBanner` gained a pennant-**bunting** swag framing the banner — **the `HAPPY 22nd GABBY!!` string stays byte-exact** from `finale.ts` (only the art around it changed); the party's depth ladder untouched. Guest distinctness: Allison's hair went to a clearly warmer AUBURN and her one-rect ponytail became a real three-segment side ponytail, so Andrea (dark-brown/band/teal) ≠ Allison (auburn/ponytail/pink) at a glance; **Dallas's structural twin (`{kind:'mirrorsPlayer'}`) and Dom's test-pinned blonde are untouched**. `npm run build` clean, **892 tests green** (no test changes — the retune stayed inside every pinned invariant), `npm run art` byte-deterministic (ST-6 adds NO PNGs — all runtime Graphics). Browser EACH exit 0: `playtest-arrival` (venue 24 rects + 1 Graphics, doors open pre-flag + light spills, both dismount in order and walk equal 211/211px into the doorway, exactly 1 Gabby + 1 Caleb incl. the swap frame, a stopped-dead+brake-held player still carried in, mid-dismount restart clean, 99 bodies), `playtest-party` (banner byte-exact, Dallas===Gabby before+after a character change, cast + balloons + bouquet, 60.7 fps, 0 bodies, 51 backdrop rects + 5 Graphics), the 22-level `playtest-levels.mjs` sweep (level 22 → arrival → PartyScene, max bodies 99 < 100, 0 errors), `playtest-drive`, `playtest-credits`. Screenshots eyeballed: the arrival reads as a warm lit party building, the walk-in centres both figures in the doorway, and the party reads festive + cohesive with the four guests clearly distinct and the Gabby/Dallas twin holding through a character change. **ST-7a (audio ENGINE + mute persistence — the foundation half of audio):** audio is SYNTHESIZED at runtime via the Web Audio API — NO committed audio binaries, NO npm deps (project-owned / CC0, matches the generate-don't-vendor ethos; satisfies "no license-unknown assets" by construction). New `src/systems/audio.ts` — a singleton `AudioManager` (`getAudio()`, mirroring `getSave()`) that is IMPORT-SAFE (touches no `AudioContext`/`window` at module-eval, so Vitest's plain-Node run never crashes; the context is created LAZILY in `ensureStarted()`, guarded on `typeof window` + `window.AudioContext ?? webkitAudioContext`, and if audio is unavailable every method is a safe no-op). Mobile autoplay handled by a one-time global first-gesture unlock (`installAudioUnlock()`, called from `main.ts`) that resumes the context on the first pointer/key/touch; `ensureStarted()` also resume-if-suspends defensively (iOS re-suspends). Master mute via a master GainNode: `setMuted(bool)` ramps gain to 0/target (no click) AND persists via the new `getSave().setMuted`/`getMuted` (`gabby22.muted`, **DEFAULT false = UNMUTED** — a Rule-3 call, logged in DECISIONS: greet the gift with music, mute one tap away); the manager initializes muted-state from the persisted flag. EVERY Web Audio call is guarded so a suspended/unavailable/failed context NEVER throws (the harnesses gate exit on ZERO console errors — the key headless risk). Music is a lookahead scheduler (at most one track; only schedules while the context is running, so a pre-gesture suspended context can't stack copies); PURE, Node-tested helpers `noteToFrequency`/`adsrGain`/`speedToEnginePitch` are exported. The PROOF the whole chain works (gesture→resume→music→mute→persist→muted-runs-clean): a soft button-click SFX wired into `ui.ts`'s `createPixelButton` onClick path (every menu button) + a gentle title-music loop started in `TitleScene.create()` / stopped on SHUTDOWN, and the Title mute toggle now reflects + writes the real persisted state (ST-7b refines the loop + adds the other tracks/SFX). Audio tunables live in a local documented `AUDIO` block in `audio.ts`. `npm run build` clean, **919 tests green** (+27: 5 save `gabby22.muted` cases + 22 `tests/audio.test.ts` cases — pure helpers + the Node import-safety no-op contract), `npm run art` byte-deterministic (ST-7a adds NO PNGs — git clean after). Browser harnesses (system Chrome, external dev server on :5173) EACH exit 0: the NEW `scripts/playtest-audio.mjs` (fresh save defaults UNMUTED "sound: on"/no `gabby22.muted`; toggling flips to "sound: off" + persists `gabby22.muted="true"` across a reload, and back to "sound: on"/"false"; game runs Title→level 1 on gas with ZERO console/page errors both UNMUTED and MUTED), `playtest-title-party` (Title toggle + music-on-gesture; button unchanged), `playtest-drive` (in-game audio adds no console errors), `playtest-pause` + `playtest-touch` (button-click SFX fires cleanly; multitouch), `playtest-orientation` (no regression), and the 22-level `playtest-levels.mjs` sweep (audio adds no errors across all scenes; max bodies 99 < 100). **ST-7 warrants a CALEB STEP** (verify audio-on-by-default is pleasant, mute silences + sticks, and mobile feel is right on his actual phone) — surfaced concretely once ST-7b lands the full track/SFX set. Remaining PLAN-10: ST-7b (full music tracks + all SFX wiring) and the juice pass.

## Known issues
- The un-guarded double-start on plain menu buttons (Title's Play/Edit/Party, LevelComplete's three) is a **project-wide** pattern — a rapid double-press / two-finger tap queues two `scene.start()`, a harmless RESTART of the destination (never a leak, never destructive). Deferred to PLAN-11 QA (the single right fix is in `createPixelButton`); the finale-CRITICAL Party→Credits and Credits→Title/`resetAll()` transitions ARE `leaving`-latched, so nothing on the destructive or dead-end path is exposed.
- Level 22's arrival cutscene is placeholder art (a brown-box venue, solid-rectangle figures) — reads correctly and matches PartyScene's palette, but is PLAN-10's to replace; audio is a `TODO(PLAN-10)` at `arrival.ts openVenue`. Level 22 now finishes in ~36.5–36.9s (was ~32.5s pre-arrival), inside NORTH_STAR §4's 20–45s window with ~8s to spare.
- `npm run test` (vitest 4.1.10) crashes with `TypeError: Cannot read properties of undefined (reading 'config')` when run from a shell whose cwd uses a lowercase drive letter (`c:/...`, e.g. some Git Bash setups). Pre-existing tooling quirk, not a project bug — run from PowerShell/cmd (`C:/...`) and it passes.
- ~~The PLAN-02 placeholder `TEST_LEVEL` can soft-fail a *moderate*-speed run on the natural hill crest near x≈1160.~~ RESOLVED by PLAN-05 ST-4: `TEST_LEVEL` (and its `SPAWN_X`/`FINISH_X`) were removed — GameScene now builds terrain from the real per-level `LevelConfig`s. The config-driven level 1 (flat, hilliness 0.05) finishes gas-only end-to-end; `scripts/playtest-drive.mjs` exits 0 (body count 57 stable across 3 restarts + a real fail — decorations add 0 Matter bodies — avg 60.9 FPS, 0 console/page errors).
- ~~`scripts/playtest-tricks.mjs` `flip` mode is DORMANT (needs a flip-capable kicker, which no PLAN-05 level has).~~ RESOLVED by PLAN-07 task 4: flip mode is REACTIVATED against level 2's first kicker (RAMP_X=4032). It lands a real backflip 3/3 (−388° to −392°, ~30° past the 360 gate) and gates the full POSITIVE award path (byte-exact toast + tulip persisted + survives reload). The old ±360° landing-rotation flakiness was addressed by retuning the harness release threshold to −4.5 rad (`FLIP_RELEASE_ROT_RAD`, env-overridable) so the flip lands ~30° past the gate with margin rather than right at it — 4 measured attempts clustered tightly at −374/−388/−389/−392°, no flakiness observed. `gasonly` mode still runs level 1 (SUPERSEDED for cross-level coverage by `playtest-levels.mjs`); `brake` mode still targets level 22.
- Level 17's 3 kickers sit on hilliness-0.30 terrain (the jump-level ceiling). Gas-only survival is empirically gated (the sweep finishes L17 gas-only with 0 restarts, 90 bodies), but the kickers are UNIFORM 336×106 (the tallest PLAN-02-proven gas-safe size) rather than bigger — any future attempt to make L17's kickers larger/steeper must re-run the `playtest-levels.mjs` sweep AND `playtest-tricks.mjs flip` to re-prove both gas-only survival and clean flips, and re-measure bodies.
- Level 22 (the longest at 16000px) sits at **99 / 100 Matter bodies** — inside NORTH_STAR §8's "< 100 per level" budget, but with zero headroom. Decorations and event stubs add 0 Matter bodies in PLAN-05, so 99 is the true hard max today. Any future Matter-bodied prop or obstacle on the longest levels (20–22) must use object pooling or stay body-free — relevant to PLAN-06 (traffic/police cars) and PLAN-10 (art/props). PLAN-06 resolved its share: traffic cars, the police car, the pickup house/props, and the passenger are ALL body-free (plain GameObjects, manual JS collision — 0 Matter bodies added; sweep re-measured max 99 on the final PLAN-06 HEAD). The `LEVEL.lengthMaxPx` (16000) bound + `validateJumpSafety`/`validateFlatZones` guard the current envelope. PLAN-09 resolved its share: the level-22 arrival cutscene (venue, doors, light, dismounting figures) plus the party cast/balloons/confetti are ALL body-free plain GameObjects — the sweep re-measured max 99 on the final PLAN-09 HEAD, so 99/100 still stands.
