// Type declarations for palette.mjs (see that file's STYLE GUIDE). Hand
// written so the vitest marker-drift assertion type-checks its import.

export declare const MARKERS: {
  readonly hair: number;
  readonly eyes: number;
  readonly bikeBody: number;
  readonly suit: number;
};

export declare const PALETTE: Record<string, number>;
