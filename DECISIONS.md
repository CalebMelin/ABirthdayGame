# DECISIONS — judgment-call log

## 2026-07-14 — Work happens directly on `main`
- **Choice:** No feature branches / PR workflow; commits land directly on `main`.
- **Why:** PLAN-00's deploy pipeline is push-to-main → Vercel auto-deploy; the repo was nearly empty at the start of work, so there's no risk of clobbering in-flight work.
- **How chosen:** Simplest option serving the plan (Rule 3, CLAUDE.md).

## 2026-07-14 — GitHub repo is named `ABirthdayGame`, not `gabby-is-22` as PLAN-00 suggested
- **Choice:** Keep the existing GitHub repo name `ABirthdayGame` rather than renaming to `gabby-is-22`.
- **Why:** Caleb created the repo before the agent started; the repo name doesn't affect gameplay, the package name, or the deploy URL slug requirements.
- **How chosen:** Simplest option serving the plan (Rule 3, CLAUDE.md).

## 2026-07-14 — Renamed `NORTH_STAR.MD` → `NORTH_STAR.md`
- **Choice:** Renamed the file to use a lowercase `.md` extension; contents untouched.
- **Why:** CLAUDE.md/README/plans all reference `NORTH_STAR.md` (lowercase). Windows is case-insensitive so this went unnoticed locally, but the mismatch would break resolution on case-sensitive systems (Linux CI, Vercel build).
- **How chosen:** Code-review fix; simplest option serving the plan (Rule 3, CLAUDE.md).

## 2026-07-14 — LevelSelect gets a "← back" button (not in PLAN-01's task text)
- **Choice:** Added a back-to-Title button to LevelSelectScene.
- **Why:** Without it, "Edit Character" on the title screen is unreachable once you're in level select except by refreshing the page. Small, obvious usability win; no personal content involved.
- **How chosen:** Simplest option serving the vision (Rule 3, CLAUDE.md).

## 2026-07-14 — LevelComplete stub marks levels complete already in PLAN-01
- **Choice:** `LevelCompleteScene.create()` calls `save.markLevelCompleted(level)` now, ahead of the real level-complete flow (tulips: PLAN-07, notes/UI: PLAN-08).
- **Why:** PLAN-01's acceptance criteria require "progress survives page reload" and a LevelSelect grid that "reads from save system" — something must write progress for either to be demonstrable. Two lines, idempotent, replaced naturally when the real flow lands.
- **How chosen:** Minimal wiring to satisfy the plan's own acceptance criteria (Rule 3, CLAUDE.md).

## 2026-07-14 — Terrain generator (PLAN-02 task 1) design choices
- **Choice:** `TerrainSpec.jumps` and `.flatZones` are both optional (default to `[]`), rather than `jumps` required as PLAN-05's draft `LevelConfig` snippet shows. `JumpSpec.x` means "the ramp's base/start x", not its center or takeoff edge. Jump ramps render as a symmetric raised-cosine hump (smooth rise to a peak at the midpoint, smooth descent back to baseline) rather than an asymmetric ski-jump wedge. The Matter collision chain uses coarser segments (`TERRAIN.segmentTargetPx = 160`) than the visual heightmap (`sampleSpacingPx = 24`) specifically to stay under NORTH_STAR §8's "<100 physics bodies per level" budget — this ratio is a placeholder guess since level lengths/bike speed aren't tuned yet (PLAN-02 tasks 2/6), and should be re-checked once they are.
- **Why:** PLAN-02's task text left the exact `TerrainSpec` field shapes, jump anchor semantics, and ramp silhouette to the implementer ("Exact naming/shape is yours — keep it simple and documented"); none of this touches personal content or the north star's WHAT/WHY, only HOW.
- **How chosen:** Simplest option that stays easy/gentle/deterministic and blends with zero height/slope discontinuities (Rule 3, CLAUDE.md) — a symmetric smootherstep-based hump is trivially C1-continuous at both ends with no extra bookkeeping, and optional fields with safe defaults match the rest of the codebase's "total function, never throws on missing/malformed input" style (see save.ts).

## 2026-07-14 — Bike rig (PLAN-02 task 2) design choices
- **Choice:** (a) Gas/brake/air-control/assist are implemented as per-step angular-VELOCITY shaping (`scene.matter.setAngularVelocity` fed by pure control-law functions), not literal Matter torques — a fixed spin-up per tick IS the torque limit, and pure functions are unit-testable in Node. (b) The head fail-sensor is an `isSensor` PART of the chassis compound body (near-zero density), not a separate constrained body — rigid, zero dynamics impact, no extra constraint to leak. (c) `airborneRotation` resets at TAKEOFF, not landing, so PLAN-07 trick detection can read a finished flip's total any time after touchdown. (d) Brake wins over gas when both pedals are held (safer for a non-gamer mashing both). (e) The rear wheel keeps being driven while airborne (no reaction torque on the chassis; a spinning wheel bites on landing). (f) A crashed bike "goes limp" (input + assist ignored, physics keeps tumbling) rather than freezing, until the scene destroys/recreates it. (g) Added `bikeSpawnY()` + `BIKE_TUNING.spawnClearancePx` so the scene spawns with a small air gap — guards the terrain caveat that the physics chain can sit slightly above `heightAt(x)` in troughs. (h) `'terrain'` label became the exported `TERRAIN_BODY_LABEL` constant in terrain.ts, imported by bike.ts.
- **Why:** PLAN-02's task text specifies behaviors, not mechanisms; none of this touches personal content. The velocity-shaping approach keeps every feel-critical number a single commented `BIKE_TUNING` knob for the task-6 tuning pass, and reset-at-takeoff is the only reset timing that lets a polling scene reliably observe a completed flip.
- **How chosen:** Simplest options that keep the game EASY (assist mandatory, torque-limited gas, forgiving crash model) and testable without faking physics (Rule 3, CLAUDE.md).

## 2026-07-14 — GameScene v1 / camera (PLAN-02 tasks 3+4) design choices
- **Choice:** (a) Restart-on-fail uses full `scene.restart()` (not in-place destroy/recreate): Phaser's Matter plugin destroys the whole physics world on scene shutdown, so every body/constraint/listener is swept by construction; a SHUTDOWN backstop in the scene only calls the explicit `destroy()`s if the world somehow survived (calling them after the plugin's teardown would dereference the nulled world — verified in Phaser source). Browser-measured: body count 93 baseline and 93 after 3 restarts + a real fail. (b) Camera is driven manually per frame (not `startFollow`) so lookahead, vertical damping, and speed zoom each get an independent smoothing rate; all knobs in a new `CAMERA` block. (c) The world-bottom fail line is the lowest terrain point + `FAIL.worldBottomMarginPx`. (d) The fail overlay shows for `FAIL.overlayDurationMs` (350ms) then restarts — measured fail→playable ~395ms, under the 500ms budget. (e) The hardcoded PLAN-02 test level lives in GameScene as an exported `TerrainSpec` (it's level config, not a global tunable); 15000px keeps the ground chain + bike at 93 bodies (< NORTH_STAR §8's 100) and a full-gas run at ~30s (inside 20–45s).
- **Why:** Plan text leaves camera/restart mechanisms to the implementer; none of this touches personal content. The fail copy "Oops! Go again 💛" is used byte-exact from NORTH_STAR §4.
- **How chosen:** Simplest options that are provably leak-free and keep every number a named knob (Rule 3, CLAUDE.md).

## 2026-07-14 — Two BIKE_TUNING nudges required to make the test level completable (PLAN-02 task 4)
- **Choice:** `gasSpinUpPerStep` 0.012 → 0.05 and `airSpinStepPerStep` 0.006 → 0.0025. Both carry TUNING NOTE comments in constants.ts; both must be revisited in task 6's feel-tuning pass.
- **Why:** Browser-measured, not guessed: (1) at 0.012 the per-step drive impulse balanced Matter's default body `frictionAir` (0.01) at ~180 px/s — a tenth of the intended top speed, unable to climb ramps, level uncompletable; 0.05 reaches the capped ~600 px/s in ~2–3s. (2) at 0.006 the gas-held airborne nose-up pitch rotated the bike ~100° during the ~0.5s hops natural hill crests produce at cruise speed — EVERY full-gas run tail-planted and crashed, directly violating the "holding only gas succeeds" acceptance criterion; level-side fixes (lower/wider ramps, relocating a ramp off a launch-cliff crest — both also done) couldn't fix hops on plain hills. Task text for the integration explicitly allows minimal tuning nudges when the level is otherwise uncompletable.
- **How chosen:** Smallest knob changes that satisfy the plan's own acceptance criterion, verified by scripted full-gas runs in real Chrome (Rule 2/3, CLAUDE.md).

## 2026-07-14 — playwright-core added as a devDependency (dev tooling, not runtime)
- **Choice:** `npm i -D playwright-core`, driving the SYSTEM Chrome via `channel: 'chrome'` (no browser binary download). `scripts/playtest-drive.mjs` (committed, reused by later plans) automates: UI click-through, gas-only drive, body-leak check across restarts, a real fail-overlay exercise, console-error collection, screenshots. `src/main.ts` exposes the Phaser game as `window.__gabbyGame` in dev builds only (`import.meta.env.DEV` — dead-code-eliminated from production).
- **Why:** CLAUDE.md Rule 2 requires actually playing the level; a scripted real-browser run makes that verification repeatable for every later plan. No runtime dependency, no effect on the shipped bundle.
- **How chosen:** Approved in the task instructions; simplest reproducible-verification setup (Rule 3, CLAUDE.md).

## 2026-07-14 — Pixel font is self-hosted Press Start 2P (TTF + OFL license in public/assets/fonts/)
- **Choice:** Downloaded PressStart2P-Regular.ttf from the google/fonts repo and self-host it with @font-face; OFL.txt shipped alongside; BootScene gates scene start on the CSS Font Loading API with a 3s never-failing timeout.
- **Why:** PLAN-01 named it as the example option; self-hosting satisfies the no-CDN-at-runtime rule; the font-gate prevents Phaser's rasterize-once text from rendering with a fallback font.
- **How chosen:** Plan's suggested option taken literally (Rule 3, CLAUDE.md).

## 2026-07-14 — Trick-input mechanic: mid-air pedal presses get full pitch authority; held-from-ground pedals ramp in late (PLAN-02 task 6)
- **Choice:** Airborne pitch authority is gated by input history (new pure functions in bike.ts: `nextPedalAirFresh`, `airPitchAuthority`, `isTrickAirPhase`, `nextHeldSteps`; knobs `heldPitchDelaySteps`/`heldPitchRampSteps`/`trickGroundDebounceSteps`/`trickPressBufferSteps`): a pedal press that begins mid-air (or within 0.2s before takeoff — "press buffering") gets authority 1 instantly; a pedal held since the ground gets 0 for the first 0.7s airborne, then ramps to 1 (so NORTH_STAR §4's "gas also rotates nose-up in mid-air" stays true on genuinely long airtime). At partial/zero authority the pedal result is blended with the auto-stabilization assist, so a gas-holding non-gamer gets the full assist on every hop.
- **Why:** The acceptance criteria are jointly impossible without an input-history distinction: "gas-only over the big ramp does NOT flip and does NOT crash" and "hold gas in the air off the same ramp = backflip" are the same physical input unless the game can tell a deliberate mid-air press from a cruise-hold. Browser-measured along the way: (1) a purely ballistic gas-held hop (first attempt: zero authority = no assist either) let natural takeoff spin build to ~90° and crash — hence the assist blend; (2) crest wheel-chatter on the coarse collision chords ate mid-air presses (freshness cleared by 1-3-step ground touches) — hence the 10-step ground debounce; (3) requiring the press strictly after takeoff cost ~140° of rotation to reaction latency — hence the 12-step press buffer.
- **How chosen:** Only design found that satisfies NORTH_STAR §4's control description, "a non-gamer never accidentally flips", and "flips achievable but optional" simultaneously; every piece is a commented BIKE_TUNING knob + unit-tested pure function (Rule 3, CLAUDE.md).

## 2026-07-14 — Gravity stays 1.0 (0.8 tried and rejected); flip airtime comes from ramp GEOMETRY (PLAN-02 task 6)
- **Choice:** `GRAVITY_Y` remains 1. The TEST_LEVEL trick ramp is a 336x106 raised cosine whose base/peak/end are aligned to the 168px physics collision-chain grid (nodes 10584/10752/10920), which the coarse chain renders as a clean triangle KICKER — the bike launches at the up-chord angle with real upward velocity (~0.55-1.0s air). A flat run-up zone (10000-10424) precedes it so approach speed (and flip airtime) is reproducible; the zone ends a full flatBlendPx before the ramp base because flat zones are stamped after jumps and would squash the ramp face.
- **Why:** Browser-measured, not guessed: at gravity 0.8 the ~25% longer hops chained into compounding suspension-rebound launches — one lip kicked the chassis ~90° nose-up under gas-only input and crashed a run that is rock-solid at 1.0 (gas-only survival is sacred). Meanwhile a BIG raised cosine (140x700) is NOT a kicker: climbing 140px bleeds most of the speed budget (v²/2g ≈ 210px) and the chain's flat-top chord launches the bike flat — it measured LESS airtime (27 steps) than the aligned 106px kicker (32-59 steps). Ramp height 106 is the max the 45° maxJumpSlope cap allows at width 336 without auto-widening breaking node alignment.
- **How chosen:** Measured alternatives, kept the one that leaves verified behavior untouched (Rule 2/3, CLAUDE.md).

## 2026-07-14 — Anti-endo braking: weak front brake + grounded brake stability damping (PLAN-02 task 6)
- **Choice:** The front wheel brakes at `frontBrakeFraction` (0.25) of the rear's damping, and while the brake is held on the ground the chassis' angular velocity is damped by `brakeGroundStabilization` (0.35) per step (new pure `brakingChassisAngularVelocity` — damping only, deliberately NO spring toward upright, so slope attitudes are never fought).
- **Why:** Browser-measured: a full-brake stop from top speed pitched the bike over its front axle to 108-209° nose-down (braking at the friction limit exceeds the ~0.77g geometric overturn threshold; balancing per-wheel brake strength alone could not fix it because both wheels' demands stay friction-saturated). With the damping assist, GROUNDED chassis pitch peaks at 5-6° on flat stops and 10-26° through top-speed rolling descents (envelope over 5 independent playtest runs; earlier point figures of "~10°/~38°" were contaminated — the measurement mixed in airborne hop attitudes and let a timed flat test drift into the hills, fixed in the brake-gate rework below) — no endos, no crashes. A scared non-gamer slamming the brake must never faceplant (EASY mandate).
- **How chosen:** Simplest law that provably fixes the measured failure without touching slope-following physics (Rule 3, CLAUDE.md).

## 2026-07-14 — scripts/playtest-tricks.mjs dispatches trick input page-side (PLAN-02 task 6)
- **Choice:** The committed flip playtest drives the whole recipe from a page-side monitor (window KeyboardEvent dispatch keyed to game state at ~8ms granularity) instead of CDP `keyboard.*` calls from the harness.
- **Why:** Two measured artifacts a real finger cannot produce: (1) a CDP keyup+keydown pair can land between two render frames, so the game's per-frame pedal sampling never sees the release (the "swap" silently becomes a continuous hold — no trick); (2) harness poll roundtrips add 30-40ms of jitter to timing that a human reacting to the visible ramp beats easily. The gesture itself stays human-scale (90ms release gap, state-triggered, nothing frame-perfect).
- **How chosen:** The harness must be at least as capable as a human hand for the evidence to mean anything (Rule 2, CLAUDE.md).

## 2026-07-14 — Brake-mode playtest gate measures GROUNDED pitch only; descent limit 60° (PLAN-02 task 6 review fix)
- **Choice:** `scripts/playtest-tricks.mjs` brake mode now (1) actually gates on the rolling-descent measurement (the exit condition previously checked only the flat figure — review-caught bug), (2) measures max |chassis angle| over grounded telemetry frames only, (3) keys the flat-stop test to bike POSITION (brake at x≥550, measure only x<700 frames) instead of a 3.5s timed press, and (4) uses limits of 45° flat / 60° descents. Also: playtest-drive.mjs now asserts avg FPS ≥ 55 mid-drive (the PLAN-02 "60fps" criterion previously had no scripted evidence), and playtest-out/ is gitignored.
- **Why:** The old numbers were unreproducible (flat 10-41°, descents 11-54° across reruns) for two measurement reasons, not physics reasons: the timed flat test drove ~1600px into the hills past the 700px flat zone, and airborne hop attitude counted against an "endo" limit even though an endo is a grounded pitch-over (front wheel down — the bike's `airborne` flag needs BOTH wheels off, so real endos still register). Grounded-only + position-keyed: flat 5-6°, descents 10-34° over 6 runs. The 60° descent limit sits midway between that envelope and a real endo (90°+), so the gate can't false-fail on slope alignment yet can't miss a faceplant regression.
- **How chosen:** Measure, then set limits from the envelope — evidence before assertions (Rule 2, CLAUDE.md).

## 2026-07-15 — Touch-pedal visibility gate is `maxTouchPoints > 0` AND `(any-pointer: coarse)`, not maxTouchPoints alone (PLAN-03 task 2)
- **Choice:** `isTouchDevice()` (in `src/systems/device.ts` — extracted there in task 3; originally in pedals.ts) shows the touch pedals only when `navigator.maxTouchPoints > 0` AND `window.matchMedia('(any-pointer: coarse)').matches`. The task text named `navigator.maxTouchPoints > 0` as the sole gate; I AND-ed a coarse-pointer media query onto it.
- **Why:** The task states TWO equally explicit requirements — "visible on touch-capable devices only (`navigator.maxTouchPoints > 0`)" AND "always hidden on pure desktop" — that conflict on Windows: browser-verified, headless system Chrome on this Windows host reports `navigator.maxTouchPoints = 10` on a PLAIN non-touch context (the OS advertises up to 10 touch contacts regardless of whether a touchscreen exists), so maxTouchPoints alone would show the pedals on a mouse-only Windows desktop. `(any-pointer: coarse)` is `false` on that same desktop and `true` on every phone/tablet/touch-2-in-1 (probed: PLAIN → coarse false, hasTouch/isMobile → coarse true), so the AND satisfies the "hidden on pure desktop" requirement while never hiding pedals on a real touch device (a phone passes BOTH terms — the safe direction). This also makes the desktop-absence acceptance check provable in `scripts/playtest-touch.mjs`, which it is not under maxTouchPoints alone on Windows.
- **How chosen:** Rule 3 judgment call resolving two conflicting explicit requirements toward the stated GOAL (hidden on pure desktop) with the smallest robust, standard idiom; no personal content involved. Verified in real Chrome across plain/touch contexts.

## 2026-07-15 — Placeholder apple-touch-icon reuses the favicon's "22" motif, not an emoji (PLAN-03 task 4)
- **Choice:** `scripts/gen-app-icon.mjs` renders a 180x180 `public/apple-touch-icon.png`: `PALETTE.bgPink` (#ffd6e8) background with a bold `PALETTE.plum` (#4a2c40) "22", produced by headless SYSTEM Chrome (playwright-core, `channel: 'chrome'`, same technique as `scripts/playtest-*.mjs`) screenshotting a tiny HTML swatch — no hand-pasted binary. The task text offered "22" or an emoji (e.g. 🎂/🌷) as equally-valid placeholder motifs.
- **Why:** `public/favicon.svg` already established a "22" motif (pink rounded square, "22" text) for this game's tab icon; matching it keeps the new Home Screen icon visually consistent with the existing one instead of introducing a second unrelated motif, and sidesteps emoji-font-rendering variance if the generator is ever re-run on a different machine. PLAN-10 replaces this placeholder with real art regardless.
- **How chosen:** Simplest option consistent with an existing in-repo convention (Rule 3, CLAUDE.md); no personal content involved (both options were placeholders, not fixed text/easter eggs).

## 2026-07-15 — WASD mapping added; dev debug-overlay toggle moved off `D` (PLAN-03 task 1)
- **Choice:** The unified input system (`src/systems/input.ts`) maps gas = Right/Up/**W**/**D**, brake = Left/Down/**S**/**A**. Because `D` is now a gas key, the dev-only debug overlay's toggle key was remapped from `D` to the backtick/tilde `` ` `` (`Phaser.Input.Keyboard.KeyCodes.BACKTICK`); its doc comments in GameScene + constants.ts were updated accordingly.
- **Why:** PLAN-03 task 1 explicitly asks for WASD as a "free bonus" alongside the arrows (NORTH_STAR §2 names arrows as the desktop control; WASD is additive, doesn't change that). The `D`-gas vs `D`-debug collision only bites in dev builds (the overlay is `import.meta.env.DEV`-gated + tree-shaken from production), but leaving it would blip gas every time a dev toggled the overlay; backtick is the conventional debug key and collides with nothing in the gameplay set.
- **How chosen:** Honor the plan's control spec, then move the dev convenience off the conflict (Rule 3, CLAUDE.md); no personal content involved.

## 2026-07-15 — On-screen HUD controls are screen-anchored objects counter-transformed against camera zoom every frame (PLAN-03 tasks 2 & 5)
- **Choice:** The touch pedals (task 2) and the ⏸ pause button (task 5) are `setScrollFactor(0)` objects on `DEPTHS.hud` that GameScene re-lays-out every render frame via shared pure helpers `zoomCompensatedPosition(screenPoint, pivot, zoom)` / `zoomCompensatedScale(zoom)` (exported from `pedals.ts`, unit-tested) — counter-positioning about the design-screen-center pivot `(DESIGN_WIDTH/2, DESIGN_HEIGHT/2)` and counter-scaling by `1/zoom`.
- **Why:** GameScene's play camera ZOOMS (`CAMERA.zoomMin`..`zoomMax`, 0.85..1.0) as speed changes, and a `scrollFactor(0)` object is still rendered through that zoom (it pivots about the screen center), so a naive corner-anchored HUD element visibly drifts inward and shrinks under acceleration — browser-measured on the dev debug overlay. Counter-transforming holds each control at a FIXED on-screen point and size (browser-verified 0px drift across the zoom range), and — verified against Phaser's hit-test path — the interactive hit area tracks the art too. A single shared helper keeps the two controls (and future HUD like the PLAN-07 tulip bouquet) consistent. Considered + rejected for now: a dedicated non-zooming HUD camera (heavier; camera-ignore bookkeeping for every world object) — the per-frame counter-transform is self-contained and proved jitter-free.
- **How chosen:** Simplest self-contained option that makes "fixed to camera" actually mean visually fixed, verified in real Chrome (Rule 2/3, CLAUDE.md).

## 2026-07-15 — Orientation guard is touch-gated and pauses via `game.loop.sleep()`; `isTouchDevice` extracted to device.ts (PLAN-03 task 3)
- **Choice:** `installOrientationGuard(game)` (`src/systems/orientation.ts`, installed once from main.ts) only does anything on touch devices (reuses `isTouchDevice()`, extracted into a shared `src/systems/device.ts` now that the pedals and the guard both need it), and freezes the game while the portrait overlay is up by sleeping the whole RAF loop (`game.loop.sleep()` / `wake()`) rather than pausing individual scenes.
- **Why:** (a) Touch gate — a desktop browser resized to a portrait-shaped window is an edge case, and blocking desktop/dev play behind a "rotate your phone" overlay would be hostile; Chrome DevTools device-emulation (the acceptance path) reports as touch, so gating on touch still satisfies the criterion. (b) `loop.sleep()` freezes updates + rendering (Matter world + bike included) with ONE call and, crucially, does NOT touch per-scene paused/active state — so it composes correctly with the task-5 pause menu (open the menu, rotate to portrait and back → the menu is still up) with zero bookkeeping, where a "pause every running scene and remember them" approach would have to special-case the already-paused menu. The initial sleep is deferred to the first `POST_STEP` because the loop isn't running yet at install time (a page that LOADS in portrait must still freeze). CSS animations on the DOM overlay keep running (compositor-driven) while the loop sleeps. Browser-verified: bike frozen in portrait, resumes on rotate-back, desktop never blocked.
- **How chosen:** Simplest mechanism that freezes everything and composes with pause without state bookkeeping (Rule 3, CLAUDE.md); the verbatim overlay text `Flip your phone sideways to ride! 🏍️` is used byte-exact from the plan (personal content, not paraphrased).

## 2026-07-15 — Held touch-pedal state is cleared on scene RESUME so pausing can't leave the gas stuck on (PLAN-03 integration fix)
- **Choice:** GameScene registers a `Phaser.Scenes.Events.RESUME` handler (`onResume`, paired with an `off` in the SHUTDOWN handler so it can't stack across restarts) that clears both input touch flags and resets both pedal visuals (`gameInput.setTouchGas/Brake(false)` + new `PedalsHandle.releaseAll()`). Esc/P were also made to resume from the pause menu (handled inside PauseScene, since a paused GameScene's `update()` doesn't run).
- **Why:** Found by the final holistic review. The pause menu uses `scene.pause()`, and a PAUSED Phaser scene stops dispatching pointer events — so a touch pedal HELD into the pause, then released while the menu is open, never receives its `pointerup`, and would resume STUCK-ON (phantom gas) until the next tap. That directly breaks the pedals' signature "genuine press/release edges, no stuck pedal" guarantee on the actual phone. Resetting on RESUME is the safe direction: a genuinely-still-held finger simply needs a re-press (the same already-accepted behavior as the fail-restart path). (The portrait-overlay auto-pause path is unaffected — it sleeps the RAF loop with the scene still RUNNING, so DOM events queue and drain on wake.) Committed with a red→green touch-context regression assertion in `scripts/playtest-pause.mjs` (stubbed handler: resume `gas:true`, driven 302px; fixed: `gas:false`, coast-only 85px).
- **How chosen:** Reviewer-recommended minimal fix that restores the invariant without touching the genuine-edge path during normal play (the handler fires only on scene resume) — evidence before assertion (Rule 2/3, CLAUDE.md).

## 2026-07-14 — Debug overlay (PLAN-02 task 5) inlined in update()/create() instead of a private method
- **Choice:** The dev-only debug overlay's read/render logic lives directly inside `if (import.meta.env.DEV) { ... }` blocks in `GameScene.update()`/`create()`, not in a separate `private updateDebugOverlay()` method. Also: overlay text is built with plain ASCII ("deg", not "°") for pixel-font glyph safety, degrees use `Phaser.Math.RadToDeg` (no manual conversion-factor magic number), and `debugVisible` deliberately does NOT reset in `create()` (unlike `ended`/`lookaheadPx`) so the on/off toggle survives a fail-restart.
- **Why:** First attempt DID use a separate method called from inside the DEV gate; building confirmed (via `npm run build` + grepping `dist/assets/*.js` for overlay-only strings like `"resets @ takeoff"`) that Vite's minifier strips a dead `if (false) {...}` *call site* but still emits an unused class *method* as a reachable member, since it can't prove nothing else references it — so the labels/strings leaked into the production bundle. Inlining (matching main.ts's own `__gabbyGame` pattern exactly) let the whole branch, strings included, get eliminated; re-verified absent after the fix.
- **How chosen:** Simplest fix that satisfies the plan's explicit tree-shaking requirement, verified by direct inspection of the built artifact rather than assumption (Rule 2/3, CLAUDE.md).

## 2026-07-15 — Palette-swap engine (PLAN-04 task 1) design choices
- **Choice:** (a) `ColorRemap` is `ReadonlyArray<ColorRemapEntry>` with a named, documented `ColorRemapEntry { from: number; to: number }` interface rather than an inline anonymous-object array type — same shape as the plan's `e.g.` snippet, just given a name Task 2 can also reference. (b) `remapPixels` treats "fully transparent" as exactly `alpha === 0`; any nonzero alpha (1-255, i.e. "fully/partly opaque" per the task text) is recolored, and the alpha channel itself is NEVER written, even on a pixel that gets recolored. (c) `recolorTexture`'s cache check (`scene.textures.exists(variantKey)`) runs BEFORE touching `baseKey` at all, so a repeat call with the same `variantKey` hits the cache even if `baseKey` were stale/bogus — browser-verified. (d) The two new marker-composite base textures (`tex-gabby-base` 24x48, `tex-bike-base` 96x28) are generated by a separate BootScene method (`generateMarkerBaseTextures`) alongside the untouched solid-placeholder path (`generatePlaceholderTextures`/`TEXTURE_SPECS`); both the existing tex-gabby/tex-bike/tex-wheel placeholders and the new `*-base` textures coexist until PLAN-04 task 4 retires the raw-render path. Their sizes are read STRUCTURALLY off the existing `TEXTURE_SPECS.gabby`/`TEXTURE_SPECS.bike` entries (never re-hardcoded as fresh literals), so the two can't silently drift apart. (e) Added one new named `PALETTE.skin` color (`0xffcf9c`) for the rider base's face region — fixed and deliberately NOT a marker color, so a swap never touches it. (f) The rider base has exactly ONE `MARKERS.suit` region (not five), per the plan's own outfit-modeling note that all 5 outfits are colorways of one shared base art.
- **Why:** None of this touches personal content; the plan's task text pinned the exact marker values, the 4-argument `recolorTexture(scene, baseKey, variantKey, remap)` signature, the exact base-texture sizes, and the cache-per-combination contract precisely, leaving only these shape details to the implementer — chosen to fit exactly how PLAN-04 task 2 said it will call this engine (build a `ColorRemap` + `variantKey` from a `CharacterConfig`, call `recolorTexture(scene, TEXTURE_KEYS.gabbyBase, riderVariantKey, riderRemap)`).
- **How chosen:** Simplest options consistent with the plan's explicit contracts and existing codebase conventions (bike.ts/terrain.ts's `import type Phaser` safety pattern; BootScene's `TEXTURE_SPECS`-style local, documented placeholder tables) (Rule 3, CLAUDE.md). Verified beyond the 12 new pure-helper unit tests (`tests/palette.test.ts`) with a real-Chrome, real-Canvas end-to-end check (dev server + playwright-core, matching the project's existing "browser-measured" verification culture — see e.g. the PLAN-02 tuning entries above): 0 console/page errors at boot, both base textures at their exact required sizes, and `recolorTexture` exercised against the actual generated `tex-gabby-base` — hair/eyes/suit marker pixels recolor to the mapped color with alpha preserved exactly, the non-marker skin pixel is provably untouched, and a same-`variantKey` second call hits the cache without needing a valid `baseKey`.

## 2026-07-15 — Character options data + one-source texture seam (PLAN-04 task 2) design choices
- **Choice:** (a) Final swatch ids/colors, verbatim from the controller-specified suggested-and-acceptable table (used as-is, no tweaks): hair (6) `blonde` 0xf2d16b / `brown` 0x6b4423 / `black` 0x2a2320 / `ginger` 0xc65d2e / `pink` 0xff9ecb / `blue` 0x6fa8dc; eyes (5) `blue` 0x5b8fd6 / `green` 0x5fa463 / `brown` 0x6b4a2b / `hazel` 0xa07840 / `grey` 0x8a8f96; bike (8) `red` 0xd94f4f / `blue` 0x4f7fd9 / `black` 0x2f2a2f / `white` 0xf4f4f4 / `pink` 0xff8fc4 / `purple` 0x9a6fd0 / `teal` 0x3fb8a8 / `yellow` 0xf5d23a; outfit (5, design ids — colorway, not design x color) `classic` (Classic Stripes) 0xf0efe8 / `twoTone` (Two-Tone Sport) 0xff8b6b / `stealth` (Stealth Black) 0x26232a / `cafe` (Cafe Racer) 0xb07a4a / `party` (Pastel Party) 0xdcb8f0. (b) `DEFAULT_CHARACTER = { hairColor: 'blonde', eyeColor: 'blue', bikeColor: 'pink', outfit: 'classic' }` — hair mandated by NORTH_STAR §4; the rest tasteful picks (yellow deliberately NOT default, despite being required in the list for the level-11 easter egg). (c) Variant-key scoping: `riderVariantKey` = `tex-gabby|<hair>|<eye>|<outfit>` (independent of bikeColor); `bikeVariantKey` = `tex-bike|<bike>` (independent of hair/eye/outfit) — both built from the RESOLVED ids (post-fallback), so an unknown saved id and its fallback option share one `recolorTexture` cache entry instead of each baking a redundant duplicate texture. (d) Two option shapes: `SwatchOption {id,label,color}` shared by hair/eye/bike, `OutfitOption {id,label,suitColor}` for outfits (id doubles as the PLAN-10 art-selection key). (e) A generic `resolveOption<T extends {id:string}>(options, id, fallbackId)` plus four named per-dimension wrappers (`resolveHair`/`resolveEyes`/`resolveBike`/`resolveOutfit`), each defaulting `fallbackId` to the matching `DEFAULT_CHARACTER` field; `resolveOption` has one extra defensive layer beyond the plan's ask — if even `fallbackId` isn't found (never triggered by any real call site today) it returns `options[0]` rather than `undefined`, so it's total under a hypothetically-malformed options array too. (f) `src/systems/characterTextures.ts`'s `buildCharacterTextures(scene, config)` returns an inline `{riderTextureKey, bikeTextureKey}` object type (no extra named interface exported) — it's a two-line pass-through of `src/data/characters.ts`'s remap/key builders onto `recolorTexture`, kept intentionally trivial per the task's "tiny and dependency-light" instruction.
- **Why:** None of this touches personal content beyond what NORTH_STAR/the plan already pin (blonde default, yellow-in-list, the 5 design names — all used exactly as specified, not paraphrased). The suggested hex table was explicitly "suggested-and-acceptable," and it already satisfies every constraint (mutually tasteful, blonde reads as warm gold, stealth reads as near-black, every value distinct from all 4 `MARKERS.*` primaries) — tweaking it further would add subjective risk for no required gain. The variant-key scoping is pinned verbatim by the task spec (rider must NOT vary with bike color and vice versa, so picking a different bike never regenerates/duplicates the rider texture and vice versa); using resolved (not raw) ids in the key is what makes the earlier "corrupt id -> fallback option" resolver behavior also collapse onto one cache entry instead of silently leaking one wasted canvas texture per distinct garbage string a save file might contain.
- **How chosen:** Simplest options consistent with the task's explicit, largely-prescribed architecture (Rule 3, CLAUDE.md) — TDD per `superpowers:test-driven-development` (tests/characters.test.ts written and confirmed RED — "Cannot find module" — before `src/data/characters.ts` existed, then GREEN; `characterTextures.ts` is the same untestable-in-Node Phaser-scene seam as `recolorTexture` itself, left unit-test-free by design per the task). 214 tests green (38 new + 4 added on self-review — see below), `npm run build` clean, both from PowerShell (not Bash, per the documented cwd-drive-letter vitest crash). Self-review caught that a handful of `riderRemap`/`bikeRemap`/variant-key tests computed their "expected" value via the same resolver function the code under test also calls (wiring-only coverage, tautological against a shared bug in the resolver or a typo'd swatch color); added 4 literal/pinned-value tests (two hex-value pins on `riderRemap`/`bikeRemap` for the default config and for `yellow`, two exact-string pins on the `tex-gabby|...`/`tex-bike|...` key format) that assert against hardcoded values instead, closing that gap without touching production code (all 4 passed immediately, confirming the existing implementation was already correct against the literal spec'd values).

## 2026-07-15 — Character creation scene (PLAN-04 task 3, folding in task 4's "save on every change" bullet + task 5's "Let's ride! → Level Select" nav bullet) design choices
- **Choice:** (a) **Preview layout**: bike-chassis anchor at design `(220, 380)`, uniform `previewScale` 3.5x applied to the bike/wheel/rider sprites AND `BIKE_TUNING`'s own offsets (`riderOffsetX/Y`, `±wheelbase/2`, `wheelDropPx`) — the preview is geometrically identical to bike.ts's real rig, just scaled up, never a second hand-tuned arrangement. **Swatch rows**: a 56px visible face decoupled from an 88px (`= UI_MIN_TOUCH_PX`) hit area spaced 90px apart (pedals.ts's visible-face-vs-hit-Zone pattern), budget-checked for the tightest row (BIKE, 8 swatches: `rowStartX(590) + 7*90 + 44 = 1264 < DESIGN_WIDTH 1280`) and then regression-tested (`tests/constants.test.ts`), not just eyeballed, since NORTH_STAR §8's 88px/no-overlap rule is a hard acceptance criterion. The preview ends up getting the left ~34% rather than a full 40% — traded a little preview width for headroom on the 8-swatch row, which the task text explicitly allowed ("left half is approximate"). Both action buttons sit in one shared bottom bar (Randomize left-of-center, Let's ride right-of-center) rather than under either column — reads as a single "when you're done" row. No back-to-Title button was added (out of scope for this task; LevelSelectScene already has one and nothing becomes unreachable without it here, unlike the PLAN-01 LevelSelect case).
- (b) **Blink technique**: one skin-colored `Rectangle` over the rider sprite's eye-marker band, hidden by default, flashed visible for `blinkDurationMs` (120ms) on a randomized 2.5–3.5s interval via a self-rescheduling `this.time.delayedCall` chain (cancelled wholesale by `this.time.removeAllEvents()` on scene SHUTDOWN so re-entering the scene can't stack a second chain alongside the first). To avoid a second, independently-drifting hardcoded copy of the rider's eye-marker position, `GABBY_BASE_LAYOUT` (hairHeight/faceHeight/eyeSize/eyeX/eyeInsetY) moved from BootScene-local to a shared export in `constants.ts` (BootScene.ts now imports it instead of redefining it); `CHARACTER_CREATE.eyeBandOffsetY` is a derived expression off those same fields plus one documented "rider is 48px tall" literal (matching the pre-existing precedent already set by `BIKE_TUNING.riderOffsetY`'s own doc comment), not a second hand-computed magic number.
- (c) **Bounce**: a yoyo tween (`repeat: -1`) directly on the preview Container's `y`, ±6px, 1400ms half-cycle — moves bike+wheels+rider+eyelid together as one unit, matching the task's "bounce ON THE preview container" wording.
- (d) **Save-on-change**: swatch taps and Randomize both funnel through one `applyConfigChange()` (rebuild preview textures via `buildCharacterTextures` + `setTexture`, refresh all 4 rows' highlights, `getSave().saveCharacter(config)`) — one path to keep in sync, not two. "Let's ride!" also calls `saveCharacter` explicitly (redundant given every prior change already persisted, but matches the button's documented contract and costs nothing). Never calls `markLevelCompleted`/`resetAll`/`addTulips` — `saveCharacter` only ever writes the `gabby22.character` key, so editing the character mid-progress cannot touch `gabby22.progress`.
- (e) `randomCharacterConfig(rand = Math.random)` lives in `src/data/characters.ts`, TDD'd (RED confirmed via "randomCharacterConfig is not a function" — 6 failing tests — before implementing, then GREEN): calls `rand()` exactly once per dimension in hairColor/eyeColor/bikeColor/outfit order (this function's OWN contract, pinned by a test, not mandated elsewhere), `Math.floor(rand()*length)` clamped into `[0, length-1]` so a pathological `rand() === 1` (Math.random() itself never returns it, but a test stub could) can't index out of bounds.
- (f) **Judgment call**: swatch-x-from-index (`rowStartX + index*spacing`) was judged too trivial (a one-line affine formula, no branching/edge cases) to warrant its own pure-helper module + test, unlike `randomCharacterConfig` which has real logic (random selection + clamping) — left inline in `buildSwatchRow` per the task's own "use judgment" allowance for non-trivial layout math only.
- (g) **Ride-along fix (required, not optional)**: all 5 existing `scripts/playtest-*.mjs` regression harnesses (drive / touch / pause ×2 mouse+touch / tricks / orientation) navigated through CharacterCreationScene by clicking/tapping the OLD stub's "next →" button at design `(640, 400)`. That point now falls inside the BIKE row's 2nd swatch hit area in the real UI, so unpatched they'd silently pick a bike color and then hang waiting for `LevelSelectScene` until their `waitForScene` timeout. Updated all 5 to `(820, 660)` (`CHARACTER_CREATE.letsRideButtonX`/`actionButtonY`) and re-ran all 5 against the live dev server post-fix: drive/touch/pause/orientation all green (`allPassed`/`finished` true, 0 console/page errors each); `playtest-tricks.mjs`'s flip mode showed flakiness that a `git stash` baseline rerun proved PRE-EXISTING (see PROGRESS.md Known Issues) and unrelated to this fix.
- **Why:** None of this touches personal content. (a)–(d) are the task's own explicit "ask if unsure" layout/mechanism area, resolved toward the simplest option that provably satisfies the stated hard constraints (88px/no-overlap, "bounce the container", save-every-change/never-touch-progress) — verified in a real browser (screenshots, coordinate-tap assertions against the actual texture keys, polled animation sampling), not just asserted. (e) followed `superpowers:test-driven-development` per the task's explicit instruction. (g) is required by CLAUDE.md Rule 2/4 ("never leave the game in a broken state") — leaving 5 committed, previously-green regression harnesses silently hanging on every future run would be exactly that, and would falsely implicate whatever PLAN-05+ work next ran them.
- **How chosen:** Simplest options consistent with the task's prescriptive spec and existing codebase conventions (pedals.ts's hit-vs-visible decoupling, GameScene's SHUTDOWN-handler teardown style, `BIKE_TUNING` as the one geometry source of truth) (Rule 3, CLAUDE.md). Verified: 233 tests green (16 new since the PLAN-04 task 2 checkpoint — 6 for `randomCharacterConfig`, 5 for `normalizeCharacterConfig`, 5 regression-testing the swatch thumb-friendly budget), `npm run build` clean (both from PowerShell). A scratch, uncommitted browser smoke-check (deleted before this commit, per the task's instructions) drove Title → Play → CharacterCreation → [tap the 2nd HAIR swatch, tap the 8th/last BIKE swatch, Randomize, Let's ride!] → LevelSelect: 0 console/page errors throughout; texture-key assertions confirmed the rider/bike textures update independently and correctly (a hair tap never touches the bike texture key and vice versa) and persist to `localStorage` after every change; polled sampling over several seconds confirmed both idle tells are actually running (preview container `y` oscillating ~6px, matching `bounceAmplitudePx`; the blink eyelid observed visible at least once inside its configured interval window) rather than just present-but-inert in the code.
- **Self-review addendum (same day):** Found that a saved `CharacterConfig` with a well-typed-but-unresolvable id (`isCharacterConfig` in save.ts only checks the four fields are strings, not that they're real option ids — reachable via hand-edited/corrupted localStorage, or an id a later swatch-list change retires) would render correctly everywhere already (every resolver falls back safely) but would show NO swatch highlighted in whichever row held the bad id, understating this task's own explicit "the currently-selected swatch is clearly highlighted" requirement for that edge case. Added `normalizeCharacterConfig(config)` to `src/data/characters.ts` (TDD'd — RED confirmed before implementing, then GREEN; 5 tests: passthrough-when-valid, per-field fallback-to-default, idempotence, "every field of the result is a real option id"), and call it once in `create()` around the loaded/default config before anything reads it. Verified end-to-end in a real browser too (not just unit tests): seeded `gabby22.character` with `hairColor: 'not-a-real-id'` via `page.addInitScript`, opened the scene via Title's "Edit Character", and confirmed the working config normalized to `hairColor: 'blonde'` (matching `DEFAULT_CHARACTER`) while the other, valid fields passed through unchanged, with the rider preview texture key reflecting the normalized values and 0 console errors.

## 2026-07-15 — In-game bike/rider texture seam + GameScene one-source-of-truth wiring (PLAN-04 task 4) design choices
- **Choice:** (a) `bike.ts`'s `BikeOptions` gained exactly one new optional field, `textures?: { bike?: string; wheel?: string; rider?: string }`, each part defaulting to the existing raw placeholder key (`TEXTURE_KEYS.bike`/`.wheel`/`.gabby`) via `??` — resolved into three `const`s (`bikeTextureKey`/`wheelTextureKey`/`riderTextureKey`) immediately after `const t = BIKE_TUNING;`, then substituted into the FOUR existing `scene.add.image(...)` calls (rear wheel, front wheel, chassis, rider) in place of the old `TEXTURE_KEYS.*` literals — the wheel key feeds BOTH wheel sprites, so the four calls share the three distinct resolved keys. Nothing else in bike.ts changed — no Matter bodies/constraints/control laws/`onBeforeUpdate`/pure helpers/sprite positions or depths were touched (self-review confirmed the diff is exactly the field + 3 consts + 4 argument swaps). [The option key was `body` as first committed; the code-review pass renamed it to `bike` — see the dated review-follow-ups entry below.] (b) GameScene's `create()` now loads the saved character (`getSave().loadCharacter() ?? defaultCharacter()`), builds its textures through `buildCharacterTextures(this, character)` — the SAME function CharacterCreationScene's live preview calls — and passes `{ bike: bikeTextureKey, rider: riderTextureKey }` into `createBike`'s new `textures` option; `wheel` is deliberately left unset so wheels stay the raw dark placeholder regardless of character choice (NORTH_STAR: only the motorcycle BODY color is a player choice). This is a pure read of the save (no `saveCharacter`/`markLevelCompleted`/`resetAll`/`addTulips` call added), so GameScene still can never mutate progress. (c) No `normalizeCharacterConfig` call was added around the loaded character in GameScene — unlike CharacterCreationScene, which normalizes because it compares raw ids directly to decide swatch highlighting, GameScene only ever feeds the config through `buildCharacterTextures`'s resolvers (already-total, already-fallback-safe per PLAN-04 task 2), so a corrupt/unknown saved id still renders correctly with no extra bookkeeping. (d) PartyScene/CreditsScene got ONLY a forward-note doc comment on their class, per the task's explicit "NOTE for later plans" framing — no rendering logic, no new imports, no functional change — naming `buildCharacterTextures`/`getSave().loadCharacter()`/`defaultCharacter()` explicitly, and, for PartyScene, pinning that **Dallas** must be built from the player's own resolved `CharacterConfig` (NORTH_STAR §5's intentional-twin joke), never a separately-authored/independently-colored character.
- **Why:** None of this touches personal content beyond what NORTH_STAR §5 already pins (Dallas's twin joke, referenced verbatim, not paraphrased). The task explicitly scoped bike.ts to "ONLY parameterize the three `scene.add.image(..., TEXTURE_KEYS.*)` sprite-creation calls" given it's the single most feel-critical, already browser-tuned module in the game (PLAN-02) — the default-preserving `??` fallback is what keeps every existing bike.ts caller (all 5 committed `scripts/playtest-*.mjs` harnesses, none of which pass `textures`) byte-identical in behavior. Routing through `buildCharacterTextures` (rather than re-deriving remaps/keys in GameScene) is the one-source-of-truth the task asked for — CharacterCreationScene's preview and GameScene's real rig can now never silently drift apart on what a given `CharacterConfig` looks like.
- **How chosen:** The task text prescribed the exact signatures/call sites; the only real judgment calls were (c) skipping a redundant normalize call in GameScene (simplest option consistent with `buildCharacterTextures`'s own already-total contract — Rule 3, CLAUDE.md) and the forward-note wording for (d). Verified: `npm run build` clean, 233 tests green (unchanged count — this task adds no new pure logic, only scene wiring across already-tested/already-browser-verified modules, consistent with `characterTextures.ts`'s own documented "exercised by browser-driven playtest scripts, not unit tests" boundary), both from PowerShell with an uppercase-drive cwd. A real-Chrome (playwright-core) scratch check, driven entirely by coordinate taps on the real swatch UI (Title -> Play -> CharacterCreation -> tap HAIR black (770,185) + BIKE yellow (1220,415) + SUIT stealth (770,530), eyes left at default blue -> Let's ride! -> LevelSelect -> level 1): the saved `gabby22.character` matched the taps exactly, and the ACTUAL in-scene Image game objects (read off GameScene's `children.list`, since `BikeHandle` deliberately never exposes its sprites) showed the chassis at `tex-bike|yellow` (DEPTHS.bike) and the rider at `tex-gabby|black|blue|stealth` (DEPTHS.rider) — both wheels untouched at `tex-wheel` — with 0 console/page errors; a screenshot confirms it visually (yellow bike body, black hair, near-black stealth suit, dark unrecolored wheels). The committed `scripts/playtest-drive.mjs` gas-only regression gate (which never taps swatches, so it exercises the DEFAULT character through this same new code path) still exits 0 with numbers matching the pre-task baseline almost exactly (93 bodies stable across 3 restarts + a real fail, 31.2s drive, 398ms fail-to-playable, 61fps avg/60.7 min — PROGRESS.md's PLAN-02/03 checkpoints read 93/~31s/~395-397ms/~61fps) — evidence the bike.ts seam is behaviorally inert when `textures` isn't overridden, and that wiring real character textures into the live rig has zero physics/perf cost. Noted but out of this task's file scope: `TitleScene.ts`'s "Play" button already implements PLAN-04 task 5's first-run routing (`loadCharacter() === null` -> CharacterCreation, else LevelSelect) and "Edit Character" already exists — its line-42 comment ("CharacterCreationScene doesn't persist a character until PLAN-04...") is now stale since task 3 landed persistence, left unedited since `TitleScene.ts` isn't one of this task's named files.

## 2026-07-15 — PLAN-04 task 4 code-review follow-ups (Approved-with-minors)
- **Choice:** (1) Renamed the seam's option key `BikeOptions.textures.body` -> `.bike` (and the in-`bike.ts` local const `bodyTextureKey` -> `bikeTextureKey`), so GameScene's call site is now a clean identity map `textures: { bike: bikeTextureKey, rider: riderTextureKey }`. (2) Trimmed the new `textures` JSDoc's continuation lines from two leading spaces (`*  `) to one (`* `), matching every other doc comment in bike.ts. (3) Reworded the now-stale "task 4 retires the raw-render path" comments in `src/systems/constants.ts` (TEXTURE_KEYS doc) and `src/scenes/BootScene.ts` (generateMarkerBaseTextures doc): the raw solid `tex-gabby`/`tex-bike` placeholders are deliberately KEPT as `createBike`'s DEFAULT texture fallback (not dead code to delete) so a non-character-aware caller stays behavior-identical, the character path overrides them per-instance via `BikeOptions.textures`, and `tex-wheel` is always used raw (wheels never recolored). (4) Fixed the miscount in the task-4 entry above ("three `scene.add.image` calls" -> four sharing three distinct resolved keys; "3 argument swaps" -> 4). Skipped the reviewer's OPTIONAL shared-`loadPlayerCharacterTextures` helper: only 2 real call sites today (GameScene + the CharacterCreation preview), so extracting now would be premature — a later plan can factor it out when Party/Credits add the 3rd/4th (Rule 3: simplest option).
- **Why:** `body` collided with bike.ts's pervasive Matter "body" vocabulary (`MatterJS.BodyType`, `BikeHandle.bodies`, `scene.matter.body.create`, collision `bodyA`/`bodyB`) and broke from the `bike`-prefixed name every upstream piece already uses for this exact concept (`bikeTextureKey`, `bikeRemap`, `bikeVariantKey`, `TEXTURE_KEYS.bike`); `bike` (not `chassis`, which would clash with those upstream `bike*` names) is the consistent choice, and doing it now — with exactly one call site — costs nothing. The stale comments actively misinformed a future reader (they implied the raw placeholders were transitional and due for deletion, when they are load-bearing as the default fallback). None of this touches personal content or behavior.
- **How chosen:** All four are reviewer-requested minors (no Critical/Important); the rename is the one substantive item, taken while it's a one-line-call-site change. Verified: `npm run build` clean + 233 tests green from PowerShell (uppercase-drive cwd). No browser re-check needed — a rename with a single call site is fully covered by the type-checked build (a missed reference would fail `tsc`), and no runtime behavior changed.

## 2026-07-15 — First-run flow + playtest-character.mjs (PLAN-04 task 5) design choices
- **Choice:** (a) **TitleScene.ts**: verified the routing logic (`getSave().loadCharacter() === null ? characterCreation : levelSelect`, "Edit Character" always → characterCreation) was ALREADY correct — it was scaffolded correctly in PLAN-01 and has persisted real characters since task 3 landed `saveCharacter`. No logic changed; only the now-stale onClick comment (which said persistence didn't exist yet, so the scene "routes to CharacterCreation on every run — expected") was reworded to describe the real, now-active behavior. (b) **Combo-setting mechanism, deliberately mixed across the harness's 6 sections**: criterion 2 (>=6 combos) sets `gabby22.character` by SEEDING localStorage + `page.reload()` (fast/deterministic, and it exercises the exact `getSave().loadCharacter()` path GameScene itself uses); criterion 3 (persistence) and criterion 4 (progress guard) instead set a combo via REAL swatch clicks, specifically so at least one criterion proves the UI's own save-on-every-change path (`applyConfigChange` → `saveCharacter`) rather than only ever proving the read path. Criterion 5 repeats real-interaction coverage again but via genuine touch taps in a `hasTouch`/`isMobile` context. (c) **Locating swatch tap coordinates**: checked `CharacterCreationScene`'s source first, per the task's own instruction to prefer live introspection — `SwatchRowHandle` (the type `buildSwatchRow` returns and the scene stores in `swatchRows`) exposes only `refreshHighlight()`; the per-swatch id+on-screen-position array built inside `buildSwatchRow` is closed over locally and never stored on `this` or returned, so it is NOT reachable via `window.__gabbyGame` at runtime. Fell back to deriving tap coordinates from the `CHARACTER_CREATE` constants block (`rowStartX`/`swatchCenterSpacingPx`/the four row Ys) plus a hardcoded mirror of `HAIR_OPTIONS`/`EYE_OPTIONS`/`BIKE_OPTIONS`/`OUTFIT_OPTIONS`' id order from `src/data/characters.ts`, each with an "IN SYNC WITH" comment — exactly the fallback the task text anticipated, and the same pattern already established by every other `scripts/playtest-*.mjs` (e.g. `playtest-touch.mjs`'s `PEDALS`-derived `GAS_XY`/`BRAKE_XY`, `playtest-pause.mjs`'s `PAUSE`-derived `PAUSE_BTN`). (d) **Combo selection**: 6 distinct combos, each of the 6 hair swatches used exactly once (maximizes per-swatch coverage across the set while guaranteeing all 6 tuples are trivially distinct), explicitly including the task's named examples (all-defaults, a yellow-bike combo, an all-black/stealth combo — hair black + bike black + `stealth` suit, with `grey` eyes since eye options have no pure black — and a pink-hair+teal-bike combo). (e) **Identifying the in-game chassis/rider Image objects**: `BikeHandle` deliberately never exposes its sprites (bike.ts doc comment), so, matching the manual technique already used to verify PLAN-04 task 4 (see that dated entry above), the harness reads `GameScene.children.list` filtered to `type==='Image'` at `depth===DEPTHS.bike`(40)/`DEPTHS.rider`(50) (hardcoded-with-a-sync-comment, same convention as every other structural constant here) — three Images share depth 40 (two wheels + the chassis); the chassis is whichever one is NOT the raw `tex-wheel` key, since wheels are structurally never recolored. (f) **Expected texture-key computation**: `expectedRiderKey`/`expectedBikeKey` are small hardcoded reimplementations of `src/data/characters.ts`'s `riderVariantKey`/`bikeVariantKey` format (`tex-gabby|<hair>|<eye>|<outfit>` / `tex-bike|<bike>`), not calls into production code — an independent oracle, so a future format regression in the real function would show up as a harness mismatch instead of silently passing. (g) **Dev-server lifecycle**: the script itself does NOT start or stop `npm run dev` — same contract as all 5 pre-existing `scripts/playtest-*.mjs` (each just documents "Requires `npm run dev` running" and connects to `DEV_URL`). The task text's "kill the dev server + any leftover node PIDs on exit" was treated as operator/session hygiene (also stated standalone in the House Rules as "kill leftover node PIDs on cleanup"), not a behavior to build into the committed script — a script that kills a long-lived dev server as a side effect would surprise anyone running it alongside its siblings against one shared `npm run dev`. Handled instead by backgrounding `npm run dev` for this session and explicitly stopping it (`TaskStop` + a port-5173 process check/kill) once every harness run finished, verified via `Get-NetTCPConnection`/`Get-Process node` showing nothing left listening/running.
- **Why:** None of this touches personal content. (a) is a Rule-2 "verify before you claim" outcome, not an assumption — reading the file confirmed the logic was correct, consistent with the task's explicit instruction to report rather than silently "fix" a bug that didn't exist. (b)-(f) all serve the task's own explicit self-review bar: "does the harness genuinely prove each criterion, or is any check tautological/weak?" — using two different combo-setting mechanisms across the six sections means the harness can't accidentally only prove one code path twice while believing it proved two different things; the progress-guard check reads `gabby22.character`/`gabby22.progress` directly via `localStorage.getItem` before AND after the edit (never through a cached/in-memory view), so it is reading REAL storage both times, per the task's explicit ask.
- **How chosen:** Simplest options consistent with the task's own anticipated fallbacks and the existing five harnesses' established conventions (Rule 3, CLAUDE.md). Verified: `npm run build` clean, 233 tests green (unchanged — this task adds no new pure logic, only a doc-comment fix and a new browser harness), both from PowerShell with an uppercase-drive cwd. `scripts/playtest-character.mjs` run twice against a live `npm run dev`, both times exiting 0 with all 47 checks passing (screenshots + full JSON evidence in `playtest-out/`, gitignored) — texture-key evidence spot-checked visually too (e.g. the yellow-bike combo's in-game screenshot shows a bright yellow chassis with brown hair; the stealth-all-black preview screenshot shows all four intended swatches — black hair, grey eyes, black bike, stealth suit — highlighted and the live preview rendering black hair on a near-black suit over a dark bike). All four pre-existing committed harnesses (`playtest-drive`, `playtest-touch`, `playtest-pause`, `playtest-orientation`) re-run against the finished PLAN-04 code and confirmed still green, plus `playtest-tricks.mjs gasonly` (the non-flaky mode) — `playtest-tricks.mjs`'s `flip`-mode flakiness is pre-existing/separately tracked (PROGRESS.md Known Issues) and was not re-touched, since character creation doesn't touch bike/trick physics.

## 2026-07-15 — LevelConfig type system + validators (PLAN-05 task 1) design choices
- **Choice:** (a) Each `LevelEvent` union member is a separate exported named interface (`TrafficEvent`/`PoliceEvent`/`CalebPickupEvent`/`WheelieRiderEvent`/`BillboardEvent`) rather than five anonymous object-literal shapes inlined in the union — matches the codebase's existing preference for named, documented option shapes (e.g. `characters.ts`'s `SwatchOption`/`OutfitOption`) and gives PLAN-06/07's event implementations a concrete type to import per event kind. (b) `validateLevels`'s problem strings are plain English sentences (e.g. `"Missing level id(s): 5"`, `"Level 7 is missing its required 'traffic' event"`) — the plan only specified "human-readable," so exact wording was left to the implementer; pinned exactly by `tests/levelTypes.test.ts` so the contract can't silently drift. (c) `validateLevels`/`getLevelTerrainSpec` follow `save.ts`/`terrain.ts`'s existing "total function" defensiveness (optional chaining, `Array.isArray`/`Number.isFinite` guards, degrade-to-problem-string instead of throw) even though the function's TS signature (`readonly LevelConfig[]`) already statically guarantees well-formed input for any caller that respects the type system — the extra runtime guards defend the same class of caller `save.ts` already guards against (malformed/legacy data bypassing the type system), per the task's explicit "never throws on a malformed/short array" requirement. (d) `THEME_IDS`/`ThemeId` use an `as const` array + derived-union (no prior example existed to copy verbatim — `SCENE_KEYS`/`TEXTURE_KEYS` are lookup *objects*, not id arrays); an array (not an object map) is what lets `THEME_IDS` be iterated/validated against directly, which the task called for explicitly (the later `THEMES: Record<ThemeId, …>` table). (e) The new `LEVEL` constants block sits in `constants.ts` immediately after `TOTAL_LEVELS` (both describe "how many/how long the levels are") rather than at the file's end.
- **Why:** None of this touches personal content, tech stack, or scope — all five are pure implementation-shape details the task text explicitly left to the implementer's judgment ("Suggested… adjust names if you find clearer ones, but document your choice"), in the same spirit as PLAN-02's terrain.ts shape choices (see that dated entry above).
- **How chosen:** Simplest options consistent with existing codebase conventions (Rule 3, CLAUDE.md). Verified: TDD'd per `superpowers:test-driven-development` (`tests/levelTypes.test.ts` written first, confirmed RED — `Cannot find module '../src/levels/types'` — before `src/levels/types.ts` existed, then GREEN, 24/24). Full suite 257 tests green (233 pre-existing + 24 new), `npm run build` clean (`tsc` type-checks src/ + tests/ with zero errors, `vite build` exits 0) — both from PowerShell with an uppercase-drive cwd. `git diff --stat` confirmed the `constants.ts` edit is a pure 21-line insertion (the `LEVEL` block only), matching the task's scope boundary exactly — no `src/systems/themes.ts`, level configs, or `GameScene.ts`/`main.ts` changes (those are later PLAN-05 tasks).

## 2026-07-15 — PLAN-05 task 1 code-review follow-ups (Approved-with-minors)
- **Choice:** Four fixes in `src/levels/types.ts` / `tests/levelTypes.test.ts`, no production behavior change beyond the new id check: (1) **`getLevelTerrainSpec` now `return { ...config.terrain }`** (shallow spread) instead of a 5-field hand-copied literal — `LevelTerrainConfig` is structurally assignable to `TerrainSpec`, so the spread forwards every field automatically and can't silently drop a field a later plan adds to both types (the exact drift the `FlatZone = Range` alias exists to prevent; the old hand-listed literal reintroduced it). Still returns a FRESH object, preserving the seam's purpose. (2) **`validateLevels` now guards `Number.isInteger`**: a fractional/NaN/Infinity id is collected and reported as its own `Non-integer level id(s): …` problem (emitted before the consequent missing-id it causes — cause before effect) instead of being silently bucketed and surfacing only as a confusing `Missing level id(s): 7` for `id: 7.5`. Matches the `Number.isInteger` gate `save.ts`'s `markLevelCompleted` and `scenes/types.ts`'s `normalizeLevel` already use for level numbers. (3) **Test filler hardened**: the five "accept when other events are also present" cases now use a new `fillerEventExcept(type)` helper (filler type guaranteed ≠ the case's own required type) instead of a hardcoded `{ type: 'wheelieRider' }` — previously the level-11 case became `[wheelieRider, wheelieRider]` and passed off the duplicate without ever exercising the injected `required` event, contradicting the test's name. (4) **Optional nit taken**: extracted the inline `LevelConfig.terrain` object type into a named, exported `LevelTerrainConfig` interface, so `getLevelTerrainSpec`'s mapping has a documentable source type and later PLAN-05 tasks can reference it. Added one test covering a fractional id (`id: 7.5` → `['Non-integer level id(s): 7.5', 'Missing level id(s): 1']`).
- **Why:** All four are reviewer-requested (2 Important, 1 Minor, 1 Optional); none touch personal content, tech stack, or scope. (1) and (2) both close real robustness gaps — (1) a silent-field-drop drift risk, (2) a defect (`id: 7.5`) that surfaced misleadingly. (3) makes a test actually test what its name claims. (4) is pure documentability with negligible churn.
- **How chosen:** Applied the reviewer's prescribed fixes directly; for (2)'s message ordering I put non-integer before the missing-id it induces (cause-before-effect reads clearest) and pinned that order in the new test. Verified: `npm run build` clean (`tsc` + `vite build` both exit 0 — confirms the spread and the `LevelTerrainConfig` extraction typecheck) and 258 tests green (257 prior + 1 new fractional-id test; the filler-helper change swapped an existing assertion's filler without changing the test count), both from PowerShell with an uppercase-drive cwd. Committed as a NEW follow-up commit (not an amend), per the reviewer's instruction.

## 2026-07-15 — Per-theme parallax backdrop system (PLAN-05 task 2 / ST-2) design choices
- **Choice:** (a) **ThemeDef shape**: `ground: {fill:number; edge:number}` (both REQUIRED, not optional like terrain.ts's `TerrainColors` — still structurally assignable to it) so `createTerrain(scene, spec, THEMES[level.theme].ground)` will work verbatim once GameScene wires it in (ST-4), and a theme accidentally missing a ground color is a compile error, not a silent runtime gap (confirmed: all 15 entries compiled clean under the required-field type). `sky: {top,bottom}` renders as TWO FLAT STACKED camera-fixed bands, not a true gradient: `Phaser.GameObjects.Graphics#fillGradientStyle` is `@webglOnly` (confirmed by reading `node_modules/phaser/src/gameobjects/graphics/Graphics.js`) and this project boots `type: Phaser.AUTO` (main.ts), which can fall back to the Canvas renderer on a device without WebGL — a gradient sky would silently vanish there. Two solid rects is exactly the "Graphics rectangles / silhouette bands" placeholder technique the task text itself names, and it's renderer-safe on both backends. `layers: ParallaxLayerDef[]` is 2 (not 3) per theme — a far skyline/hills band and a near props band, matching "far→near" — each with its own `scrollFactor`/`color`/`y`/`height`. `props: {accent: number}` is a single color (task's own "keep light" instruction), rendered as small blocks scattered along the NEAREST layer's baseline rather than left as inert unused data, so the field has a real, visible effect.
- (b) **Parallax mechanism**: every layer uses `setScrollFactor(fx, 0)` — genuine PER-LAYER horizontal parallax (fx: 0.2 far / 0.45 near, shared across all 15 themes — the RATE is plumbing, not a per-theme creative choice; only color/shape vary per theme), but vertical is ALWAYS camera-pinned (fy=0), never a fractional vertical scrollFactor. Derived and verified directly against Phaser 3.90 source (`GetCalcMatrix.js` + `Camera.js`'s `preRender`) rather than assumed: for this project's single, unmodified default camera, a scrollFactor-(fx,fy) object at local (localX,localY) renders at `screenX = pivotX + zoom*(localX - scrollX*fx - pivotX)`, `screenY = pivotY + zoom*(localY - scrollY*fy - pivotY)` (pivot = design-screen center) — exactly the relationship pedals.ts's `zoomCompensatedPosition` doc comment already describes for the fx=fy=0 case, generalized here to any scrollFactor. With fy=0, screenY has NO dependence on scrollY at all, so vertical coverage is exactly as provable-safe as the existing scrollFactor(0) HUD/fail-overlay pattern (same `DESIGN/zoomMin` oversize technique, pulled out as `cameraFixedOversizePx`) — it can never reveal a vertical gap regardless of camera wobble, at the cost of real vertical drift (acceptable for a placeholder: GameScene's vertical camera follow is already soft/damped and small in practice).
- (c) **Horizontal content padding**: layer content is authored directly across a padded world-x range (`backdropContentRangePx`, world-space, never wrapped/tiled — same one-shot-bake approach as terrain.ts's ground polygon), padded by `DESIGN_WIDTH/zoomMin` on BOTH sides. The tightest required padding to guarantee full-screen coverage at every camera scroll position across a level of any length, at zoomMin, for ANY layer scrollFactor in [0,1) is actually asymmetric and smaller than this — `DESIGN_WIDTH/zoomMin` is deliberately more generous, for one simple, shared formula every layer can use regardless of its own rate. Proven (not just asserted) by `tests/themes.test.ts`'s "geometric coverage guarantee" test, which independently re-derives the same Phaser transform formula (not a call into production code) and checks screen coverage at both level-length extremes (`LEVEL.lengthMinPx`/`lengthMaxPx`), both scroll extremes, and scrollFactor up to 0.99.
- (d) **Silhouette rendering**: each layer is drawn as ONE `Graphics` filled polygon (a deterministic sine-wobble top edge via `buildSilhouettePoints`, closed along the layer's baseline) — mirrors terrain.ts's `drawGround` structure exactly (moveTo/lineTo loop/closePath/fillPath) — rather than many separate rectangle "building" GameObjects or a `TileSprite`. Cheap (a few hundred vertices in one draw call vs. dozens-to-hundreds of separate GameObjects) and avoids needing a real texture (a `TileSprite` needs one, and generating a placeholder texture felt like more machinery than this task's placeholder-art bar called for). The wobble's wavelength is derived from each layer's OWN `height` (`height * 5`) rather than a second array indexed alongside `theme.layers`, so nothing can drift out of sync if a theme's layer count/order ever changes.
- (e) **New PALETTE colors** (8, added to `constants.ts` per the task's explicit allowance): `overcast`/`slate` (greys — downtown/highway/construction/billboardRow/bridge/boulevard), `dustyTan` (construction/old-town accent), `riverTeal` (riverside), `steelBlue` (bridge), `brickRed` (old-town), `sunsetGlow` (sunset), `duskIndigo` (final-dusk night). The existing 12-color pastel palette had no greys, water/steel/brick tones, or a deep night color, and all 15 themes needed to read as visibly distinct (regression-guarded by the new "visibly distinct" sky+ground signature test).
- (f) **Test scope**: `createBackdrop` itself is deliberately left UNIT-TEST-FREE, matching the established precedent for every other Phaser-touching seam in this codebase (`createTerrain`, `createBike`, `createPedals`, `buildCharacterTextures`) — it only calls methods on the `scene` handed to it, verified by `npm run build`'s type-check, and is left for real browser verification in ST-4/5 once GameScene actually wires it in (per this task's own explicit verification-boundary instruction not to browser-test an unwired module). All four pure helpers (`cameraFixedOversizePx`, `backdropContentRangePx`, `buildSilhouettePoints`, `evenlySpacedX`) are exported and TDD'd (RED confirmed — "Cannot find module '../src/systems/themes'" — before the module existed, then GREEN); `THEMES` itself gets a coverage test (every `THEME_IDS` entry present, no extras), a well-formedness test (`it.each` over all 15: valid colors, 2-3 layers, non-decreasing far→near scrollFactor, band placement within `DESIGN_HEIGHT`), and a "visibly distinct" regression guard (no two themes share the same sky+ground color signature) directly encoding the PLAN-05 acceptance criterion "Visual theme visibly changes across the city arc".
- **Why:** None of this touches personal content, tech stack, or scope. The task's own suggested `ThemeDef` shape was explicitly offered as a starting point ("refine names/fields if you find clearer ones, and document your choice"), and the zoom-gotcha/parallax-mechanism/padding choices are the task's own named "CRITICAL gotcha" — resolved by deriving Phaser's actual behavior from source rather than guessing, then proving the fix with a test, per Rule 2 (verify before you claim) applied to a module this task explicitly cannot browser-verify.
- **How chosen:** Simplest options that make the zoom-gotcha provably (not just reasoned-about) safe, staying consistent with existing codebase conventions (terrain.ts's pure-generation/Phaser-construction file split, `import type Phaser` safety, "total function" defensiveness against degenerate numeric input) (Rule 3, CLAUDE.md). Verified: TDD'd (RED confirmed — module-not-found — before `src/systems/themes.ts` existed), 291 tests green (258 prior + 33 new), `npm run build` clean (`tsc` + `vite build` both exit 0), both from PowerShell with an uppercase-drive cwd. The Phaser camera-transform formula the zoom-safety proof rests on was independently verified against actual Phaser 3.90 source (`GetCalcMatrix.js`, `Camera.js`, `TransformMatrix.js`), and Phaser's display-list depth sort was confirmed to be a STABLE sort (`DisplayList.js` calls `StableSort(this.list, this.sortByDepth)`) — the fact `createBackdrop` relies on for same-depth (`DEPTHS.background`) objects to render back-to-front in creation order (sky → far → near → props) — rather than assumed from general Phaser folklore.

## 2026-07-15 — Authored all 22 real level configs + index (PLAN-05 task 3 / ST-3) design choices
- **Choice:** (a) **Hilliness nudges on 3 jump-bearing levels**, using the task's own explicitly-allowed "±0.05 hilliness" nudge: level 16 (old town) 0.32→0.28, level 17 (hilly, 3 big jumps) 0.35→0.30, level 19 (sunset) 0.32→0.27 — each nudge stays within ±0.05 of the suggested table value and brings the level under the separately-stated hard rule "on any level with jumps, keep hilliness ≤ 0.30" (the suggested-table row for these three, taken literally, would have violated that rule). (b) **Level 3's "none or 1 hop" table entry** resolved to no jump — keeps a hop (L2) / breather (L3) / two hops ("extra jumps", L4) pacing distinct, simplest option under the table's own explicit either/or. (c) **Jump geometry**: every non-L17 jump uses the "standard hop" envelope (height 45–60px, width 420–520px) from the task's jump-sizing rules; level 17 alone uses the "big jump" envelope (height 90–95px, width 650–680px, i.e. the WIDE end of 600–700 for extra slope margin) for 3 jumps rather than 2, to read as an actual "tulip farming" field. Every jump's `x` sits within 30–70% of its level's length AND ≥1500px clear of both the spawn flat zone and the finish flag, with ≥1300px of flat gap between consecutive jumps on multi-jump levels — mechanically re-verified against all 22 configs with a temporary, uncommitted self-review Vitest file (48 checks, all green) before this commit, not just hand arithmetic. (d) **Flat-zone convention**: every level gets `{start:0,end:700}` (spawn) + `{start:length-900,end:length}` (finish); level 12 (calebPickup) additionally gets `{start:5750,end:6750}`, centered exactly on the event's `x:6250`, per the task's explicit call-out for that level. (e) **Seeds**: `220000 + id` (the task's own suggested formula), all 22 confirmed distinct. (f) **Decorations scope**: only added where the task explicitly named them — level 1's 3 tutorial signs (used the task's own example copy verbatim: "Hold GAS to go! →" / "Hold BRAKE to slow down" / "Catch air? GAS leans you back!"), level 18's 3 decoy billboards (used the task's own example copy verbatim: "EAT AT JOE'S" / "FRESH MOTOR OIL" / "SUNNYVALE 12 MI", positioned at x 5500/6300/8200 so the real easter-egg billboard at x:7000 sits tucked between two of them), levels 20–22's balloon/streamer scatters (5/6/7 decorations respectively, building density toward the party, all placed before each level's finish flat zone). Every other level intentionally has no `decorations` field at all, per the task's own "don't overdo it" guidance on non-called-out levels. (g) **Names/introText** (flavor, not locked, all 22 authored fresh): level 21 is named "Almost There", reusing NORTH_STAR §5's own theme/backdrop label for that row verbatim; level 22 is named "The Party" (the task's other suggested alternative for that slot) specifically to avoid a name collision with level 21. L1/L12/L22 introText use the task's given example lines verbatim ("8:00 PM. The party's across the city. Let's go!!" / "Caleb's waiting outside…" / "You can hear the music from here!!"); the other 19 are new but were checked against NORTH_STAR §6's fixed level-complete notes (after L6/9/13/14) to confirm no accidental paraphrase of that separately-locked content. (h) **`getLevelConfig`'s non-integer handling** mirrors `src/scenes/types.ts`'s `normalizeLevel` exactly (a non-integer id — e.g. 2.5 — defaults straight to level 1, it is NOT rounded to 2), reimplemented as a small local `normalizeLevelId` helper (not imported from `scenes/types.ts`) so `src/levels/` (data) doesn't gain a dependency on `src/scenes/` (presentation).
- **Why:** None of (a)-(h) touches personal content beyond what NORTH_STAR §5/§7 already locks (the level-18 billboard text, copied character-for-character, double-quoted in source specifically so the internal apostrophe needs no escaping — verified byte-exact both by direct file read and by `tests/levels.test.ts`'s independently-hardcoded literal). (a)-(c) resolve the one real tension in the task's own instructions (a "suggested, nudge ±0.05" table vs. a separately-stated hard "≤0.30 on jump levels" rule) toward satisfying the hard rule, using only the nudge room the task itself granted. (d)-(h) are the task's own named "your judgment, ask only if it touches personal content" areas; none of the invented flavor text (names/intro lines/decoy billboard copy) asserts any new personal fact about Gabby/Caleb beyond what NORTH_STAR already establishes, so no question was needed.
- **How chosen:** Simplest options that satisfy every stated numeric rule with comfortable margin (Rule 3, CLAUDE.md) — verified, not just asserted: `npm run build` clean and 300 tests green (291 prior + 9 in the new `tests/levels.test.ts`), both from PowerShell with an uppercase-drive cwd; `git status` confirmed the diff is exactly the 24 new files (22 level configs + `src/levels/index.ts` + `tests/levels.test.ts`), no edits to `GameScene.ts`/`main.ts`/`themes.ts`/`terrain.ts`/`src/levels/types.ts`/`constants.ts` (all out of this task's scope — GameScene wiring is ST-4, next). A temporary, uncommitted self-review test (deleted before this commit) mechanically re-checked every jump-safety numeric rule (mid-level placement, ≥1500px clearance from spawn/finish, hilliness cap, envelope sizes, no jump-to-jump overlap) plus spawn/finish flat-zone coverage, seed/name uniqueness, and non-empty introText across all 22 real configs — 48/48 green — rather than trusting hand arithmetic alone.

## 2026-07-15 — Ghost-driver harness + jump-safety/flat-zone guards (PLAN-05 task 6 / ST-5) design choices
- **Choice:** (a) **`scripts/playtest-levels.mjs` (ghost driver)**: sweeps ALL 22 levels for gas-only completability (companion to `playtest-drive.mjs`, which stress-tests restart/fail/perf on ONE level). Enters each level DIRECTLY via `game.scene.start('GameScene', {level:n})` — bypassing LevelSelect's unlock gating — verified against installed Phaser 3.90 `SceneManager.start()` source that this does the same `shutdown()`-then-`start(data)` lifecycle `scene.restart()` uses (clean fresh entry, not a no-op/leak). Holds gas only (`ArrowRight`, re-pressed each poll — CDP has no OS key auto-repeat) to a per-level 60s timeout, detecting spawn-warp fail-restarts. **Real bug found + fixed while building it**: `LevelCompleteScene` stays active after a level finishes (both scenes can be active at once in Phaser, like the pause-menu-over-GameScene pattern), and the manager-level `scene.start` bypass has no "calling scene" to auto-stop it the way the real "next ->" button does — so it read stale-true on the next level's first poll, making every level after the first appear to "finish" in 0s. The harness now explicitly `scene.stop('LevelCompleteScene')` before entering the next level, reproducing the real UI's self-cleaning transition. (b) **Exit-code gating**: only EVENT-FREE levels (all except 7/11/12/15/18) are REQUIRED to finish, plus every level's Matter body count `< 100` (NORTH_STAR §8) and zero console/page errors across the whole run. The 5 special-event levels are recorded + WARN-but-not-gated: PLAN-05 stubs their events as no-ops so they finish gas-only today, but a real PLAN-06/07 obstacle (traffic, a police chase) could legitimately need more than gas — gating on them would make this harness go stale the moment that lands. (c) **`src/levels/validate.ts`** (pure, total, TDD'd): `validateJumpSafety` + `validateFlatZones` guard the gameplay-safety invariants `validateLevels` (ST-1) does NOT — jump width/height/placement/clearance/hilliness-ceiling, and the spawn/finish flat-zone convention GameScene's arithmetic now depends on (spawn at `LEVEL.spawnXPx`, finish flag at `length - LEVEL.finishMarginPx`). The new `LEVEL.jump*` bound constants are deliberate SAFETY FLOORS (width≥400, height≤100, placement 25–75%, clearance≥1500, hilliness≤0.30) — looser than ST-3's actually-authored values (widths 480–680, heights 50–95, placement 35–64%) so a minor future config tweak doesn't false-fail the guard while a genuinely-unsafe jump still trips it. Each validator's bad-fixture tests are arithmetic-isolated to trip exactly ONE rule at a time. (d) **`scripts/playtest-tricks.mjs` reconciliation** (it broke when ST-4 removed `TEST_LEVEL`'s x=10584 kicker): `flip` mode is now DORMANT — it exits immediately with a clear message (no browser round-trip, so it genuinely can't hang against a kicker that no longer exists) since NO PLAN-05 level has a flip-capable kicker (those return in PLAN-07; the recipe code is kept intact/unreachable for re-pointing then); `gasonly` mode still runs level 1 but a header note flags it as SUPERSEDED for cross-level coverage by the new `playtest-levels.mjs`; `brake` mode is RE-POINTED at level 22 (hilliest jump-free config) for its rolling-descent portion.
- **Why:** None of this touches personal content. The plan's task 6 asked for exactly this — a "ghost driver" verifying finishability + a Vitest config-validation test (the latter already landed in ST-3's `tests/levels.test.ts` as `validateLevels(LEVELS)===[]`; ST-5 adds the jump-safety/flat-zone guards a code review flagged as higher-value than most regression tests here because they guard the core gas-only-survivable promise). Gating only event-free levels matches the plan's own acceptance wording ("ghost driver finishes at least levels without events") and future-proofs the harness. Leaving `playtest-tricks.mjs` silently broken would violate CLAUDE.md Rule 4 (never leave the game/tooling in a broken state) — the same precedent PLAN-04 task 3 set when it fixed the harnesses its UI change broke.
- **How chosen:** Simplest options consistent with the existing harness conventions (Rule 2/3, CLAUDE.md). Verified: `npm run build` clean, **326 tests green** (300 prior + 26 new in `tests/levelSafety.test.ts`, incl. `validateJumpSafety(LEVELS)===[]` / `validateFlatZones(LEVELS)===[]` against the real configs), both from PowerShell with an uppercase-drive cwd. The ghost driver was run against a live `npm run dev`: **all 22 levels finished gas-only, 0 restarts, 0 console/page errors, max body count 99 (< 100 budget), drive times 17.5s→32.5s** — the empirical proof of PLAN-05's core "every level beatable holding only gas" acceptance criterion. (Process note: the ST-5 implementer subagent was interrupted by a session limit after producing all deliverables but before committing/logging; the controller independently re-verified everything — read every file, ran build + full test suite + the whole 22-level ghost sweep — before committing, rather than trusting an unfinished report.)

## 2026-07-15 — GameScene made data-driven: config terrain, theme backdrop, decorations, event stubs, intro banner (PLAN-05 ST-4) design choices
- **Choice:** (a) **Decoration placeholder DRAWING dimensions** (sign/billboard/balloon/streamer sizes, float heights, zigzag shape) live as documented LOCAL constants in `src/systems/decorations.ts`, NOT in `constants.ts` — following the established `themes.ts`/`terrain.ts`/`ui.ts` precedent for presentation-only shape knobs that have no gameplay effect and get replaced wholesale by real art in PLAN-10. The task's enumerated tunables DID go to `constants.ts`: `LEVEL.spawnXPx` (250) / `LEVEL.finishMarginPx` (500) + a new `LEVEL_INTRO` block (hold 1800ms / fade 600ms / font+layout knobs), matching CLAUDE.md's "gameplay numbers in constants.ts". (b) **Intro banner gets a cream backing panel** (`createPixelPanel`) behind the plum pixel-text, one depth below it — so the level name + one-liner stay legible over ANY theme backdrop, light or dark. Browser-verified this matters: on the finalDusk theme (deep indigo/plum sky) plum text directly on the backdrop would be near-invisible; on the cream panel it reads perfectly (and matches the game's existing cream-face/plum-text dialog look). Panel width auto-fits the wider of title/subtitle (same measure-then-size trick as `ui.ts` buttons). (c) **`dispatchLevelEvents` exhaustiveness uses a non-throwing `const _exhaustive: never = event` guard** in the `default` case rather than a throwing `assertNever(...)`, to honor the task's explicit "must NOT throw" — the guard still makes adding a future `LevelEvent` variant a COMPILE error, but is a runtime no-op (never reached for a valid config). Same pattern used in `decorations.ts`'s kind switch. (d) **`dispatchLevelEvents`'s `scene` param** (reserved for the PLAN-06/07 implementations that will spawn cars / play cutscenes) is kept live via a dev-only `console.debug` breadcrumb (Vite-stripped from prod), which doubles as a "level N dispatched K event(s)" trace useful for verifying the special-event levels. (e) **Event→plan TODO attribution**: traffic/police/calebPickup tagged `TODO(PLAN-06)` (special-levels), wheelieRider/billboard tagged `TODO(PLAN-07)` (tricks-easter-eggs), per NORTH_STAR §5 + the plan filenames. (f) **Two now-stale PROGRESS.md "Known issues" bullets updated** because removing `TEST_LEVEL` obsoleted the code they describe — notably `scripts/playtest-tricks.mjs` drives level 1 expecting the old kicker at x=10584, which no longer exists, so it is now non-functional until a future plan re-points it at a real jump-bearing level config (e.g. level 9/17); this is out of ST-4's scope and `playtest-tricks` was deliberately NOT in ST-4's required regression set (drive/touch/pause/orientation/character, all re-run green).
- **Why:** None of this touches personal content, tech stack, or scope. Decoration art dimensions are throwaway placeholder shape (PLAN-10 replaces the art), so co-locating them with the placeholder drawing code — exactly as themes.ts/terrain.ts/ui.ts already do for their own presentation-only knobs — keeps them where the next art pass will look, while the genuinely-shared level-flow tunables the task named went to constants.ts. The banner panel is the minimum needed for the banner to satisfy its own acceptance criterion ("intro one-liners show at level start") legibly across a 15-theme arc spanning pale-dawn to dark-dusk. The no-throw `never`-assignment is the one form of the assertNever idiom that satisfies BOTH "exhaustive / compile-error-on-new-variant" AND the task's "must NOT throw".
- **How chosen:** Simplest options consistent with existing conventions (Rule 3, CLAUDE.md). Verified (Rule 2): `npm run build` clean (`tsc` + `vite build`), 300 tests green, all from PowerShell with an uppercase-drive cwd; `scripts/playtest-drive.mjs` exits 0 on the config-driven level 1 (gas-only finish, Matter body count 57 stable across 3 restarts + a real fail — decorations add ZERO bodies — avg 60.9 FPS, 0 console/page errors); `playtest-touch`/`playtest-pause`/`playtest-orientation`/`playtest-character` all still exit 0; an uncommitted scratch multi-level check drove levels 1/9/18/22 (suburbs/construction/billboardRow/finalDusk) + swept special-event levels 7/11/12/15/18, screenshot-confirming visibly-distinct theme backdrops/ground, decorations rendering (L1 3 signs, L18 3 decoy billboards, L22 balloons+streamers — object counts matching each config exactly), the intro banner showing level name + one-liner then fading, and 0 console/page errors on every level (events dispatch as no-op stubs). Feel-critical code (bike rig, camera setUpCamera/updateCamera/CAMERA, input/pedals, pause menu, fail/restart) confirmed untouched via `git diff` — the only `createBike` change is the allowed `SPAWN_X`→`LEVEL.spawnXPx` spawn substitution; `bike.ts`/`terrain.ts`/`themes.ts`/`pedals.ts`/`input.ts`/`PauseScene.ts`/`level*.ts` all unmodified.

## 2026-07-15 — Caleb is BROWN-haired; the level-12 standing Caleb gets a brown hair-band overlay, the pillion placeholder stays neutral (PLAN-06 task C)
- **Choice:** Caleb reads as **brown-haired**, never blonde (distinct from blonde Dom). Concretely: (a) added a `PALETTE.brown` (0x6b4423) — the 12-color pastel set had no true brown, so this is added alongside the theme tones for the same "the art needs a color the palette lacks" reason, and is distinct from every `MARKERS.*` swap color (guarded by `tests/palette.test.ts`'s existing distinctness check); (b) the STANDING Caleb rendered in `src/systems/pickup.ts` (a `TEXTURE_KEYS.caleb` Image in a Container) gets a brown `PALETTE.brown` hair-band Rectangle overlaid across the top ~12px of the 48px sprite, so at the pickup moment he clearly reads brown-haired; (c) the persistent PILLION Caleb (`passenger.ts`, which this task must NOT edit) keeps rendering as the neutral **sky-blue** tex-caleb placeholder — which is NOT blonde — with a `NOTE(PLAN-10)` in pickup.ts flagging that his real brown-haired art lands in PLAN-10.
- **Why:** NORTH_STAR §5 establishes Caleb as brown-haired specifically to distinguish him from blonde Dom, and the shared PLAN-06 brief calls this out as a golden rule with the pickup task owning the DECISIONS log. The task explicitly allowed either a brown tint/overlay OR a clear TODO(PLAN-10) "so long as NOTHING renders him blonde." I did both where each is cheapest: a real brown-band overlay on the standing Caleb I own and render here, and a documented deferral for the pillion sprite I'm forbidden to touch (its placeholder is sky-blue, so the "never blonde" invariant already holds). No personal *text* is involved (this is his look, already fixed by the north star), so no question to Caleb was needed — only the byte-exact strings (`MELIN`, `Caleb hopped on!!`) are verbatim personal content and were used exactly.
- **How chosen:** Simplest option that satisfies the north star's brown-hair fact and the brief's golden rule without editing an out-of-scope module (Rule 3, CLAUDE.md). Verified (Rule 2): `npm run build` clean + **373 tests green** (361 prior + 12 new in `tests/pickup.test.ts`), from PowerShell with an uppercase-drive cwd; `scripts/playtest-level12.mjs` (new, committed) exits 0 against a live `npm run dev` — the gas-only run triggers the cutscene at x≈5.9k, AUTO-BRAKES to 0.08 px/step and stops at x≈6046 (inside the {5750,6750} flat zone), the passenger activates mid-level, control returns, the `MELIN` mailbox + `Caleb hopped on!!` toast both render byte-exact, the level finishes gas-only 3/3 in a row (0 fails, ~27s each), entering level 13 shows Caleb pillion from the start while level 11 shows Gabby solo, body count 78 (< 100), 0 console/page errors.

## 2026-07-15 — Level-12 pickup is a self-contained non-Matter cutscene driven by ctx.setInputOverride; pure logic unit-tested, props import-safe (PLAN-06 task C)
- **Choice:** (a) The auto-brake is done ENTIRELY through `ctx.setInputOverride({gas:false,brake:true})` (then `{gas:false,brake:false}` to hold, then `null` to release) — never a direct Matter force on the bike — so the real deceleration runs on the bike's own fixed 60 Hz step and handling is untouched. (b) The cutscene is a 5-state machine (`approaching → braking → stopped → hopping → done`, terminal, never re-triggers), with the transition function + the approach-trigger predicate (`shouldBeginPickup`) + the stop predicate (`isStoppedForPickup`) extracted as PURE functions and unit-tested in `tests/pickup.test.ts`. (c) `pickup.ts` is kept IMPORT-SAFE (no runtime Phaser, no `ui.ts` import — only `import type` + the pure constants), replicating `ui.ts`'s `createPixelText` in a tiny local `pixelText` helper from the shared font constants, exactly so those pure helpers are Node-testable (same discipline as `traffic.ts`, and UNLIKE `decorations.ts` which does import `ui.ts`). (d) Placeholder DRAWING dimensions (house/mailbox/Caleb/heart/toast sizes + offsets) stay as local documented consts in `pickup.ts` (decorations.ts precedent); the GAMEPLAY tunables (trigger window, stop threshold, hop/heart/toast timing) go in a new `PICKUP` block in `constants.ts`, and the per-level placement + `stopWindowPx:380` are authored on `level12.ts`'s `CalebPickupEvent`. (e) The heart particle is drawn as a `Graphics` shape (two lobes + a triangle), NOT a `♥` glyph — the rendered pixel font is ASCII-only (see the PLAN-05 "ASCII-only rendered copy" entry), so a non-ASCII heart would tofu; the toast text `Caleb hopped on!!` is all-ASCII and safe. (f) The `stopped` phase switches the override to `{gas:false,brake:false}` (coast/hold) rather than keeping the brake, so the bike neither reverse-creeps (the held brake's "mild reverse" would drift it backward) nor drives off under the player's still-held gas while the ~1.8s beat plays. (g) `events.test.ts`'s scene stub was widened from an image-only stand-in to a generic Proxy-based chainable GameObject stub (carrying a numeric `width` so `createPickup` can size the MELIN board) so the now-REAL `calebPickup` case constructs under it — the same accommodation task B made when it turned the `traffic` case real.
- **Why:** None of this touches personal content beyond the two byte-exact strings (used verbatim, code-point-checked in `tests/pickup.test.ts`). The `setInputOverride` route (not a force) is the seam's whole point and keeps the "handling must NOT get harder" mandate trivially true (the passenger is cosmetic; no mass change). Import-safety + pure-function extraction is the codebase's established rule for anything a Vitest test imports (`traffic.ts`/`bike.ts`/`terrain.ts`). Co-locating throwaway placeholder-art dims while promoting real gameplay tunables matches the decorations.ts/themes.ts precedent the PLAN-05 ST-4 entry already set.
- **How chosen:** Simplest options consistent with the existing event-seam + import-safety + constants conventions (Rule 3, CLAUDE.md); verified as above (Rule 2). Scope confirmed via `git diff`: only `constants.ts` (PALETTE.brown + PICKUP block), `events.ts` (wire the pickup case + header comment), `level12.ts` (author stopWindowPx), the new `pickup.ts`/`tests/pickup.test.ts`/`scripts/playtest-level12.mjs`, the `events.test.ts` stub widening, and these DECISIONS entries — no edits to `bike.ts`/`GameScene.ts`/`passenger.ts`/`traffic.ts`/`police.ts`.

## 2026-07-16 — Level-15 cop hard-cap is tuned to the level's SUSTAINED gas-only cruise, not the flat top speed (PLAN-06 task 3 / police chase)
- **Question:** What `copMaxSpeedFrac` makes the rubber-band cop fair — i.e. guarantees a gas-holding player is NEVER caught (the EASY mandate) while still reading as a real chase?
- **Choice:** `copMaxSpeedFrac: 0.45` (hard cap = 0.45 × the bike's 10.8 px/step FLAT top speed = 4.86 px/step), with `startBehindPx: 600`, `catchDistancePx: 200`, `catchTimeMs: 1500`, `catchupBonusPxPerFrame: 3`. The design brief's phrasing ("copMaxSpeedFrac < 1 guarantees it kinematically") is only true if the player actually holds the FLAT top speed — but a browser probe of level 15 showed the bike's SUSTAINED gas-only cruise on its rolling (hilliness 0.26) terrain is only ~7.9 px/step (median 8.08, avg 7.87), dipping to ~5.9 (p5) on the steepest climbs, never the theoretical 10.8. An initial frac of 0.85 → cap 9.18 therefore sat ABOVE the ~5.9 climb dips: the cop out-ran the player on every climb and slowly reeled them in, and the harness caught a gas-holding player (closest gap 91px, catch-timer 1500). Lowering the cap below the measured climb MINIMUM (4.86 < 5.9) makes the guarantee hold even mid-climb — the gap can only grow while gas is held. Consequence (intentional): the cop is visible with flashing lights at spawn (600px back, selling the introText), then FALLS BACK as you gun it — you shake the cops and escape, which is exactly the EASY/cute read for a birthday gift. The catch-up bonus (3) only bites once the player is nearly stopped (cop = min(avg+bonus, cap)), so a deliberate stop still gets caught in ~3.5s.
- **Why:** None of this touches personal content, scope, or tech stack. The frac is the one knob the EASY mandate ("the cop must NEVER catch a gas-holding player") turns on, and the north star's own note (row 15: cop speed tuned BELOW player top speed) plus the brief both make "never catch on gas" non-negotiable — so tuning it against the level's real measured cruise (not an assumed flat top speed) is required to keep the promise, not a scope change. The verbatim strings (`They got us!! ...let's pretend that didn't happen 🚔`, `WOOHOO!`) are used byte-exact (ASCII `...`/apostrophes + U+1F694), code-point-checked in `tests/police.test.ts`, so no question to Caleb was needed.
- **How chosen:** Simplest option that makes the guarantee provable, not just reasoned-about (Rule 2/3, CLAUDE.md). Verified: `npm run build` clean + **393 tests green** (373 prior + 20 in the new `tests/police.test.ts`), from PowerShell with an uppercase-drive cwd. `scripts/playtest-level15.mjs` (new, committed) exits 0 against a live `npm run dev`: the gas-only run finishes **3/3 in a row, NEVER caught, closest cop gap 441–443px** (well clear of the 200px catch distance); a deliberately-STOPPED (hold-brake) run IS caught → the byte-exact soft-fail fires and the level restarts (cop reset far back); the escape finale renders the `WOOHOO!` toast, spins the cop out (angle → 180°), and holds the LevelComplete hand-off ~1196ms; Caleb rides pillion (passenger active) on level 15 with progress seeded; body count 87 (< 100 — the cop adds ZERO Matter bodies); 0 console/page errors. The whole-set `playtest-levels.mjs` sweep still exits 0 (level 15 finishes gas-only with the real cop; max bodies 99; no regressions). The cop's x-integration + catch timer run on the fixed 60 Hz `beforeupdate` step (refresh-independent — a dt/frame-rate displacement test is in `tests/police.test.ts`, mirroring traffic's), registered in `createPolice` and removed in `destroy()` so nothing leaks across `scene.restart()`.

## 2026-07-16 — Level 15 caught-message form confirmed by Caleb: `They got us!! ...let's pretend that didn't happen 🚔`
- **Choice:** The shipped L15 caught soft-fail toast keeps the PLAN-06 form — two `!`, ASCII `...` + apostrophes, trailing 🚔 (U+1F694) — over NORTH_STAR §5 row 15's prose form (`They got us! …let's pretend that didn't happen`: one `!`, Unicode ellipsis, no emoji).
- **Why:** The two source documents disagreed on a piece of FIXED personal content (Rule 4: verbatim, never paraphrase), and the final holistic review flagged it. Per Rule 3 ("personal content must be exact — ask if unsure") the question was put to Caleb; he confirmed on 2026-07-16 that he likes the `!!` + 🚔 form. The ASCII `...`/apostrophe rendering follows the established ASCII-only pixel-font convention (see the PLAN-05 "ASCII-only rendered copy" entry).
- **How chosen:** Asked Caleb directly rather than guessing between conflicting sources. Byte-exactness is guarded by code-point assertions in `tests/police.test.ts`; no code change was needed (the implementation already used the confirmed form).

## 2026-07-16 — Trick/tulip system (PLAN-07 task 1) design choices
- **Choice:** (a) The whole trick HUD (bouquet cluster, count text, toast, arcing tulips) lives inside ONE scrollFactor-0 root Container that GameScene zoom-counter-transforms per frame (root at zoomCompensatedPosition((0,0)) with scale 1/zoom), so children use plain design coordinates and the tulip ARC lands exactly on the bouquet at any camera zoom - a generalization of the pause-button/pedal pin to a whole overlay. (b) The award persists at the LANDING STEP (save.addTulips before any animation); the HUD count then displays savedCount minus in-flight arcs and bumps as each arced tulip ARRIVES (the count visually "arrives" with the flower) - the persisted value is never delayed by cosmetics. (c) The flip-count rule is authored in DEGREES (flips = floor((|deg| + 30) / 360), constants TRICKS.flipThresholdDeg 330 / fullFlipDeg 360) with the radians->degrees conversion applied once at the landing step, so the exact-threshold unit tests are float-exact. (d) Trick toast sits at y=260 (below the intro banner's ~200 title row and the pickup/police toasts at 180) so simultaneous beats never overlap text; a new trick toast replaces a still-fading previous one. (e) The arc is a single tween with per-axis eases (x Sine.easeIn, y Sine.easeOut toward the higher corner) - reads as an upward arc with no bezier bookkeeping, and killTweensOf covers it on teardown. (f) Landing detection is NOT gated on the run having ended: on an IMMEDIATE finish (no finale delay) the scene transition destroys the system the same frame, so a still-airborne flip is simply never awarded (matching the plan) — but during a finish-DELAY finale hold (e.g. level 15's ~1200ms POLICE.finaleHoldMs) the Matter world keeps stepping and the listener stays live, so a flip landed cleanly during the hold — like one landed during the brief soft-fail overlay window — STILL AWARDS (persist + toast + arc, swept by the shutdown teardown; kind and deliberate — it WAS landed; genuine crashes are blocked by the crashed latch). PLAN-08 note: per-level tulip accounting must count awards through scene shutdown, not just until `ended` flips true. (g) Bouquet growth stages draw 1 / 3 / 7 fanned tex-tulip placeholder sprites (bunch at 3+, bouquet at 10+ per the plan's "~10+"); PLAN-10 replaces the art.
- **Why:** PLAN-07 task 1 leaves the arc/toast presentation and HUD composition to the implementer ("your call, keep it juicy but simple"); none of this touches personal content beyond the two VERBATIM toasts (`Backflip!! 🌷` / `Frontflip!! 🌷`), which are used byte-exact (U+1F337) and code-point-guarded in tests/tricks.test.ts.
- **How chosen:** Simplest options that keep detection honest (fixed-step, airborne-transition-edge only, crashed-guarded) and persistence instant (Rules 2/3, CLAUDE.md). Verified: build clean, 412 tests green (393 prior + 17 tricks + 2 save-session tests), and the new committed scripts/playtest-tulips.mjs exits 0 against npm run dev - bouquet renders top-right at the right stage/count for seeds 0/3/12, holds its screen point to 0px drift across camera zoom 0.884-0.998, a full gas-only level-2 drive (125 fixed-step landings, max landing rotation 9.2 deg) awards ZERO tulips (the no-false-positive acceptance criterion) with gabby22.tulips byte-unchanged and re-read correctly after page reloads, and the Matter body count (63) equals the pre-tricks baseline measured on the prior HEAD (the system adds ZERO bodies). playtest-levels.mjs (all 22) and playtest-drive.mjs still exit 0 on the same HEAD.

## 2026-07-16 — Level 11 wheelie-rider easter egg (PLAN-07 task 2) design choices
- **Choice:** (a) **Composition**: the whole rig (rear wheel, front wheel, the real `tex-bike-base` recolored yellow, a bespoke rider) sits in ONE Container whose LOCAL ORIGIN (0,0) is the rear wheel's GROUND-CONTACT point (not the chassis center bike.ts's own rig pivots around) — so a single `container.setAngle(WHEELIE_RIDER.pitchDeg)` both keeps the rear wheel near-planted and swings the front wheel up-and-back, selling the wheelie with no manual per-child trig (unlike bike.ts's rider, which hand-rotates via cos/sin because it has no Container). Geometry reuses `BIKE_TUNING`'s own wheelbase/wheelRadius/wheelDropPx/riderOffsetX/Y so the composite reads as "the same bike", just recolored+posed. Sign convention pinned to bike.ts's own (positive angle = clockwise = nose-down facing right, per `BikeHandle.angle`'s doc) — `pitchDeg: -25` (nose-up) — verified correct by direct trig (front wheel lands ~48px above / the origin vs. the rear's ~16px) AND by an actual browser screenshot (front wheel visibly elevated over the rear). (b) **Depth**: the whole rig sits at `DEPTHS.props` — the SAME layer traffic cars / the police car already use, structurally below the player's own bike (40)/rider (50) regardless of x-overlap, so an overtake can never read as a collision (traffic/police precedent, per the brief). (c) **Yellow bike texture**: a DEDICATED `recolorTexture` cache key (`tex-wheelie-rider-bike`), not `characters.ts`'s own `tex-bike|<id>` format — the egg's correctness must never depend on whether the player's OWN chosen bike color happens to already be cached; only the resolved 'yellow' SWATCH COLOR (`resolveBike('yellow').color`) is shared with characters.ts, keeping one source of truth for the actual hex without coupling to its cache-key scheme. (d) **Rider texture**: generated LAZILY inside `wheelieRider.ts` itself (guarded by `scene.textures.exists`, mirroring `recolorTexture`'s own cache pattern) rather than in BootScene — this is the one entity confined to a single level, so pre-generating it at every boot would be wasted work; required a one-line `BootScene.ts` type-only edit (excluding `wheelieRider` from `SolidTextureName`, the same treatment `gabbyBase`/`bikeBase` already get) to keep the `TEXTURE_SPECS` Record type-check happy — a new `TEXTURE_KEYS.wheelieRider` entry was added for it, following the codebase's "every texture key lives in the central registry" convention (traffic.ts/police.ts's own texture keys are all there too). (e) **No DEFAULTS object**: unlike traffic.ts/police.ts (many optional numeric fields), `WheelieRiderEvent` has exactly ONE optional field (`x`), so the "fill an omitted field" contract is one small `resolveWheelieRiderTriggerX(eventX, worldLength)` pure function instead of a static object. (f) **Despawn** requires BOTH clearing the camera's CURRENT right edge by a margin AND clearing the bike itself by a separate, larger lead (900px) — the second, camera-independent check exists so a later lookahead/zoom-out swing (the player speeding back up) can never resurrect an about-to-be-culled rider on screen right before it's destroyed. (g) **Speed**: `speedMultiplier: 1.3` — the middle of the brief's own "~1.25-1.4x" guidance, since (unlike POLICE.copMaxSpeedFrac) this is never a fairness-critical floor the egg's correctness depends on — it can never fail the player regardless of the exact value. (h) **Dust**: simple spawn-tween-self-destroy Graphics puffs (pickup.ts's heart / police.ts's spin-out-puff style), not a pool — traffic.ts's pool exists because MANY cars can be concurrently active; dust never has more than a handful alive at once, so the extra pool bookkeeping would be needless machinery. Color is `PALETTE.dustyTan` (warm tan), NOT police.ts's `PALETTE.overcast` (cool grey) — an early browser screenshot showed overcast puffs nearly invisible against level 11's 'highway' theme ground (`PALETTE.slate`, a near-identical grey), so dustyTan was substituted for visibility against every theme's ground tone (self-review-caught, re-verified with a fresh screenshot after the swap).
- **Why:** None of this touches personal content, scope, or tech stack — NORTH_STAR §5 row 11 only pins the WHAT (all-black rider, black helmet, yellow motorcycle, wheelies past, never explained); every choice above is HOW. (a)/(b) are the task's own explicit "traffic/police precedent decides" instruction, applied literally. (c)/(d) keep the egg fully self-contained (its correctness never depends on what bike color a player happens to have picked, nor does it burden BootScene with a texture only one level ever uses). (h)'s color swap is a Rule-2 "verify before you claim" outcome — the dust WAS being drawn every step, it just wasn't visually legible, which a first screenshot caught before I'd have otherwise noticed.
- **How chosen:** Simplest options that make the wheelie visually unambiguous, keep the egg's zero-Matter-body/non-interactive/never-explained mandates provably true, and stay consistent with the police.ts/traffic.ts structural template the task named (Rule 3, CLAUDE.md). Verified: `npm run build` clean, **438 tests green** (412 prior + 25 new pure-helper tests in `tests/wheelieRider.test.ts` + 1 new `tests/events.test.ts` case), both from PowerShell with an uppercase-drive cwd. The new committed `scripts/playtest-level11.mjs` exits 0 against a live `npm run dev`: the rider spawns exactly once, is observed BEHIND the bike right after spawn, overtakes (crosses bike.x), despawns with its GameObjects CONFIRMED gone from the display list — probed MID-RUN, at the first poll where `despawned` reads freshly true AND GameScene is still active (not just a self-reported flag; see the review-follow-up entry below for why the check must run BEFORE the scene moves on, not after), the Matter body count is IDENTICAL before spawn and during the pass (81 == 81, the ZERO-bodies guarantee), the level finishes gas-only in 25.8s (inside NORTH_STAR's 20-45s window) with 0 restarts, the egg is CONFIRMED ABSENT when briefly driving level 10, and 0 console/page errors. A mid-overtake screenshot (`playtest-out/level11-overtake.png`, plus a 2x-viewport zoomed check) was PERSONALLY reviewed: yellow chassis, an all-black rider silhouette, the front wheel visibly elevated over the rear (the wheelie reads correctly), tan dust puffs trailing, and the rig never draws over Gabby's own bike/rider. The whole-set `scripts/playtest-levels.mjs` sweep still exits 0 on the same HEAD (level 11 finishes gas-only at 26.2s/81 bodies; max bodies across all 22 stays 99/100 — unaffected; level 7's pre-existing by-design non-finish is unrelated, WARN-only per its own established contract) and `scripts/playtest-tulips.mjs` (PLAN-07 task 1) is unaffected (63 bodies, unchanged). No PROGRESS.md line was added for PLAN-07 as a whole — it isn't done yet (tasks 3-4 remain) — matching task 1's own precedent of logging per-task work here in DECISIONS.md and leaving the bundled plan-level PROGRESS.md entry for whichever session completes the final task.

## 2026-07-16 — PLAN-07 task 2 quality-review follow-ups (CHANGES_REQUIRED: 1 Important + 4 doc minors)
- **Choice (Important — the despawn check was structurally vacuous):** `scripts/playtest-level11.mjs`'s "GameObjects actually gone" cross-check ran AFTER the drive loop, which in the success path only exits once `st.complete` is true — by then GameScene has already stopped (Phaser's `scene.start()` semantics stop the calling scene), so the old `hasImageWithTexture` helper's own `if (!g.scene.isActive('GameScene')) return false;` guard fired for BOTH textures regardless of any real leak, meaning `objectsGoneAfterDespawn` could never actually fail. Fixed by folding a live display-list probe (`bikeTexturePresent`/`riderTexturePresent`) directly into the existing atomic `READ_L11` snapshot, and having the drive loop capture it ONCE, at the first poll where `despawned` reads freshly true AND `active` (GameScene still running) — seconds before the level's actual finish, well inside the valid window. The old post-loop `hasImageWithTexture` function was deleted outright (dead/misleading now that the probe lives in READ_L11).
- **Why:** A gate that cannot fail proves nothing — exactly the concern CLAUDE.md's "tests assert real behavior" self-review bar exists to catch, and the reviewer caught it precisely because the fix folds the SAME live-DOM check into a moment the scene is guaranteed still active. The four doc fixes (`src/levels/events.ts`'s dispatch JSDoc + stale `@param scene` note, `src/levels/level11.ts`'s stale "dispatch stub" line + a pre-existing "rider ON a black helmet" -> "rider IN a black helmet" typo against NORTH_STAR §5's exact wording, `src/scenes/GameScene.ts`'s "wheelie/billboard are PLAN-07 no-ops" comment, and `src/systems/wheelieRider.ts`'s trigger-doc claim that "the bike only ever moves forward" — false; it can reverse-creep/roll back downhill, the predicate is just re-evaluated every step regardless) are all reviewer-identified factual drift against the real, now-landed code; none touch personal content, scope, or tech stack.
- **How chosen:** Applied the reviewer's prescribed fix (fold the probe into the existing atomic snapshot, capture it mid-run) and prescribed doc rewordings directly — no alternative design considered, since the bug and the fix were both precisely specified. Verified, not just asserted: (1) re-ran `scripts/playtest-level11.mjs` against a live `npm run dev` post-fix — exits 0, `objectsGoneAfterDespawn: true`, captured via the new mid-run probe (25.7-25.8s gas-only finish, 81==81 bodies, all other checks unchanged). (2) Proved the check is now genuinely non-vacuous by temporarily editing `wheelieRider.ts` to SKIP `destroyVisuals()` on despawn (an intentional, uncommitted one-line break — `despawned = true;` with the `destroyVisuals();` call commented out) and re-running the SAME harness: it correctly FAILED, exit 1, `objectsGoneAfterDespawn: false`, with the message "wheelie-rider GameObjects (bike/rider textures) are still present in the scene after despawn" — direct proof the probe would have caught the exact bug class the reviewer flagged. Reverted immediately (confirmed via `git diff` showing only the intended trigger-doc reword remains, and a `grep` for the sanity-check marker across `src/`/`scripts/`/`tests/` returning nothing) and re-ran the harness once more to confirm green again. (3) `npx vitest run` (438 green) and `npm run build` (clean) both re-run on the fully-reverted, doc-fixed tree, from PowerShell with an uppercase-drive cwd. Dev server (port 5173) killed after use each time.

## 2026-07-16 — Level-18 sleepover billboard renders on the SAME world-anchored props layer as its decoys, not a fractional parallax layer (PLAN-07 task 3)
- **Question:** PLAN-07 task 3's two style directives conflict: "Style it like the decoys (same billboard frame) so it's discoverable, not highlighted — subtle per NORTH_STAR" vs. "Parallax background layer, no interaction needed." Level 18's existing decoy billboards (its `decorations` array, drawn by decorations.ts since PLAN-05) are WORLD-ANCHORED props at `DEPTHS.props` with scrollFactor 1 — they are NOT on themes.ts's fractional-scrollFactor parallax layers. Which layer does the egg get?
- **Choice:** The egg renders through the EXACT SAME drawer, on the SAME `DEPTHS.props` layer, with the SAME scrollFactor-1 world-anchored scroll behavior as every decoy. Concretely: (a) **One shared drawer** — decorations.ts's billboard drawer became the exported `drawBillboard(scene, x, surfaceY, text, accent)`; `createDecorations` calls it for every decoy `{kind:'billboard'}`, and the new `src/systems/billboard.ts` (the real `billboard` case in `dispatchLevelEvents` — the seam's LAST no-op stub is gone; every LevelEvent variant now constructs a real system) calls the same function for the egg, with `config.theme` passed through the dispatch switch so even the frame's accent color resolves identically. Zero Matter bodies, no fixed-step listener, permanent no-op `update()`, idempotent `destroy()` — "background" is satisfied by sitting behind the bike/rider and being purely non-interactive, not by a fractional parallax rate. (b) **Word-wrap so SIZE gives nothing away** — the egg's ~44-char locked text on one line would balloon its board to roughly 2.8-4x a decoy's width, defeating "not highlighted" by size alone; the new pure `wrapBillboardText` (greedy CHARACTER-budget wrap, never mid-word, a single overlong word kept whole — a char budget because Press Start 2P is fixed-width, keeping the helper Node-testable) wraps EVERY billboard's copy at `BILLBOARD.wrapMaxChars` (18). Its round-trip property (wrapping only ever swaps a space for a newline) is what lets the browser harness verify the RENDERED text still equals the verbatim literal once newlines are undone. (c) **Board sizing** — "min OR content, whichever is bigger" on BOTH axes; the height floor `boardBaseHeightPx` (110) is the exact pre-wrap fixed height so every pre-existing single-line decoy renders byte-identically (browser-confirmed: all four short decoys still measure 110 high). Tunables in a new constants.ts `BILLBOARD` block (wrapMaxChars 18 / boardMinWidthPx 220 / boardBaseHeightPx 110 / textPadPx 24 / lineSpacingPx 10) — board sizing now serves a gameplay requirement (the egg's subtlety), unlike the pole/outline placeholder-art dims that stay local per the PLAN-05 ST-4 precedent. The wrapped label stays ONE Text object with embedded '\n's (Phaser's own multi-line centering), which is also what makes the harness's "exactly one text object carries the egg text" check meaningful. (d) **Decoy roster: 7 billboards total (the plan's 5-7 band)** — kept PLAN-05's three decoys (x 5500 "EAT AT JOE'S" / 6300 "FRESH MOTOR OIL" / 8200 "SUNNYVALE 12 MI"), added three (x 3200 "22 FM RADIO - YOUR HITS ALL DAY" — the "22 FM radio" fake ad is the plan's own named suggestion — x 4000 "COLD SODA HERE", x 11000 "PARKER AND PARKER LAW OFFICES - CALL NOW"), egg event at x 7000 (48% of the 14500px level = mid-level per the plan). All decoy copy is generic-by-design ASCII (the pixel-font-safety convention, now also guarded by a levels.test.ts ASCII check); the two long decoys are deliberately authored to wrap multi-line too, and "PARKER AND PARKER..." wraps to the SAME 3 lines as the egg (unit-pinned), so the egg is neither the only multi-line board nor the biggest. (e) **decorations.ts is now Node/Vitest import-safe** — it replicates ui.ts's createPixelText in a local `pixelText` (police.ts/pickup.ts's established discipline) instead of importing ui.ts (runtime Phaser): billboard.ts imports `drawBillboard`, events.ts imports billboard.ts, and tests/events.test.ts imports events.ts under plain Node, so the whole chain must stay Phaser-free; this also lets tests/decorations.test.ts exercise `wrapBillboardText` directly. (f) **Locked-copy discipline** — repo-wide, the egg literal exists in exactly THREE code/test places: level18.ts's event (the source of truth), tests/levels.test.ts's byte-exact guard (its one independent oracle), and scripts/playtest-level18.mjs's independent harness literal — plus the two pre-existing docs (NORTH_STAR §5's row, the plan). tests/decorations.test.ts DERIVES the text from the level config (it pins wrap BEHAVIOR, not the text itself: its wrap-shape pin asserts per-line character counts [17, 18, 7], which under the round-trip property fixes the exact break points without another copy), and constants.ts's BILLBOARD doc describes the text without quoting it.
- **Why:** NORTH_STAR §5 row 18's dominant requirement is "subtle, among decoy billboards" — discoverable only by READING it. A fractional-scrollFactor egg would move against its neighbors every frame (decoys scroll 1:1 with the world), making the ONE special board the ONE that moves differently — highlighted by the very mechanism meant to hide it; and the "same billboard frame" directive can silently break if decoy and egg frames are drawn by two code paths that drift apart. One shared drawer makes "same frame, same scroll behavior" provably true rather than merely intended. The locked text itself is untouched (used byte-exact at its single authoring site); decoy ad copy is NOT personal content (generic fake ads by the plan's own definition), so no question to Caleb was needed.
- **How chosen:** Rule 3 judgment call: the dominant requirement ("subtle, among decoys, same frame") beats the secondary "parallax background layer" phrasing, and the world-anchored props layer is what the decoys already use — joining them is the smallest change that satisfies it. Verified (Rule 2): `npm run build` clean + **467 tests green** (438 prior + 29 new: 22 in tests/decorations.test.ts for wrapBillboardText incl. the round-trip property, 6 level-18 config guards in tests/levels.test.ts, 1 events-seam billboard case) from PowerShell with an uppercase-drive cwd. The new committed `scripts/playtest-level18.mjs` exits 0 against a live `npm run dev`: gas-only finish in 29s (inside NORTH_STAR's 20-45s window) with 0 restarts; EXACTLY ONE display-list text object carries the verbatim egg text (independent literal, code-point compare, wrap newlines undone); 7 billboards on-screen matching the config; the egg's board (480x140) sits within the 1.8x family tolerance of EVERY decoy on both axes (worst real ratio 1.43x against the shortest decoy's 336x110; the size-matched PARKER decoy is an exact 480x140 match); Matter body count pinned EQUAL to the measured 90 terrain+bike baseline (billboards add ZERO bodies — decorations.ts/billboard.ts never touch scene.matter); 0 console/page errors; screenshots of the egg + a short decoy + the size-matched decoy personally reviewed (the egg reads as just another billboard in the row). Regressions on the same HEAD, all exit 0: the full 22-level `playtest-levels.mjs` sweep (all 17 event-free levels finish; event levels 11/12/15/18 finish gas-only — level 18 in 29s / 0 restarts / 90 bodies inside the sweep too; level 7's non-finish is its own documented by-design WARN; max bodies across all 22 stays 99 < 100; 0 errors), `playtest-level11.mjs` (rider spawns once/overtakes/despawns, 81==81 bodies, 25.8s finish), and `playtest-tulips.mjs` (0 false-positive awards over 125 landings, bodies == pre-tricks 63). Dev server (port 5173) killed after the harness runs.

## 2026-07-16 — PLAN-07 task 3 code-review follow-ups (1 Important + 4 Minor)
- **Choice (Important — extracted `src/systems/pixelText.ts`):** decorations.ts's own local `pixelText` helper (added by this task's commit, with a new `lineSpacingPx` param the other copies never needed) was the FOURTH near-identical replica of ui.ts's `createPixelText` implementation across the codebase — pickup.ts (PLAN-06 task C), police.ts (PLAN-06 task 3), and tricks.ts (PLAN-07 task 1) each already carried their own copy, each justified in its module doc the same way ("keeps this module Node/Vitest import-safe, since ui.ts's `createPixelButton` needs a runtime `Phaser.Geom.Rectangle`"), and this task's own commit grew that standing pattern 3→4 instead of reusing/extracting it. Fixed by pulling the ONE actual implementation out into a new import-safe `src/systems/pixelText.ts` (`import type Phaser from 'phaser'` + plain runtime imports from constants.ts only — no other dependency): decorations.ts now imports `pixelText` from there instead of holding its own copy (its module doc + the two local comments referencing "the local pixelText" were updated to match), and ui.ts's exported `createPixelText` delegates to it too (signature/behavior unchanged — `sizePx = 24` default preserved, no `lineSpacing` override, so every existing caller renders byte-identically; ui.ts already pays for a runtime Phaser import for `createPixelButton`'s `Phaser.Geom.Rectangle` hit-area, so importing this module costs it nothing extra). pickup.ts/police.ts/tricks.ts were deliberately NOT touched (minimal fix, per the review's own scoping) — each still holds its own local copy, to migrate to `./pixelText` the next time its file is touched for other reasons.
- **Choice (4 Minor):** (1) `tests/levels.test.ts`'s garbled test title `'every decoy billboard has at least one decoy (not just the egg alone)'` reworded to `'the level authors at least one decoy billboard (the egg is never alone)'` (assertion body unchanged). (2) `tests/events.test.ts`'s stub doc comments updated: the `stubNode` doc now also credits `drawBillboard` (dispatched via `createBillboard`, the billboard event) for needing the stub's numeric `width`/`height` — it sizes its board from BOTH, not just `width` like `createPickup`'s mailbox — and the ctx-stub's construction-time `terrain.heightAt` reader list now names `createBillboard` alongside `createPickup`/`createWheelieRider`. (3) `scripts/playtest-level18.mjs`'s `READ_BILLBOARDS` snapshot dropped its unused `complete`/`bikeX` fields and the harness now GATES on a new check (h): `snapshot.active === true` at the moment the snapshot is captured, pushing a `problems[]` failure if not — proves the snapshot (which checks (b)/(c)/(d)) was taken while GameScene was actually live, not a stale/torn-down scene. (4) `tests/levels.test.ts`'s level-18 ASCII-convention test now ALSO asserts every billboard text (decoys + the egg event's text) matches `/^\S+( \S+)*$/` (single-spaced, no leading/trailing whitespace) — the property both the harness's newline-undo compare and `wrapBillboardText`'s word-wrap family sizing depend on.
- **Why:** All five are reviewer-requested fixes against PLAN-07 task 3's own landed commits; none touch personal content, scope, or tech stack. The pixelText extraction closes a real duplication regression (the task's own commit made an already-repeated pattern worse instead of reusing it) without forcing a broader migration the review didn't ask for — pickup.ts/police.ts/tricks.ts are explicitly left as documented deferred work. The other four are test/tooling precision fixes: a misleading test name, two doc comments that had drifted behind the code they describe, and a browser harness assertion that was capturing state without ever checking whether the capture itself was valid.
- **How chosen:** Applied the reviewer's prescribed fixes directly (Rule 3, CLAUDE.md) — minimal in scope, no alternative design considered since each fix was precisely specified. Verified (Rule 2): `npm run build` clean (`tsc` + `vite build`, exit 0) + **467 tests green** (24 test files, unchanged count from before this fix — fix 5 added assertions inside an existing `it()` rather than a new test case), both from PowerShell with an uppercase-drive cwd. `scripts/playtest-level18.mjs` re-run against a live `npm run dev` and exits 0: `LEVEL 18 OK: gas-only finish in 28.9s, 0 restarts; exactly one text object carries the verbatim egg text; 7 billboards on-screen (matches config); egg board (480x140) within family range of its 6 decoys; body count 90 (< 100); no errors.` — including the new (h) live-scene gate passing (`snapshot.active: true`). `scripts/playtest-tulips.mjs` (chosen specifically because ui.ts's `createPixelText` delegation touches its HUD text path) also exits 0: `TULIPS OK: bouquet renders top-right at the right stage/count for 0/3/12; zoom-pinned (drift 0/0px across zoom 0.884-0.998); gas-only level 2 (121 landings, max 14.1 deg) awarded 0 — no false positives; count survives reloads; bodies == pre-tricks 63; no errors.` Dev server (port 5173) confirmed stopped (no LISTEN state) after both runs.

## 2026-07-16 — Kickers vs humps: distinct `kind` flag + own validated bounds, not widened hump floors (PLAN-07 task 4)
- **Question:** PLAN-02's browser-PROVEN flip ramp is 336px wide × 106px tall — it VIOLATES both `LEVEL.jumpMinWidthPx` (400) and `jumpMaxHeightPx` (100), the floors `tests/levelSafety.test.ts` enforces on the real 22 configs. How do flip-capable kickers coexist with the gas-only-safety guard without gutting it?
- **Choice:** Option (a) from the task framework — a distinct kicker concept with its OWN bounds, leaving the hump floors intact. Concretely: (1) `terrain.ts`'s `JumpSpec` gains `kind?: 'hump' | 'kicker'` (default hump). RENDERING is byte-identical for both (`applyJumps` stamps the same raised cosine) — `kind` is purely an authoring/validation label; the different FEEL comes from the different dimensions + grid alignment a kicker requires. (2) `terrain.ts` exports `TERRAIN_COLLISION_STRIDE`/`TERRAIN_COLLISION_GRID_PX` (168px) — the coarse collision chain's node spacing, previously an inline local in `buildGroundBodies` — so the validator and the chain builder can't disagree about where collision NODES fall. (3) A new `LEVEL` kicker-bounds block (`kickerMinWidthPx` 336 / `kickerMaxWidthPx` 672 / `kickerMinHeightPx` 90 / `kickerMaxHeightPx` 112). (4) `validate.ts`'s `validateJumpSafety` branches on `kind`: a kicker is checked against the kicker width/height envelope, PLUS grid alignment (`x` a multiple of 168, `width` a multiple of 336 so base/peak/end land on collision nodes — the pure `isCollisionGridAlignedKicker`) PLUS a no-auto-widen slope cap (`raisedCosinePeakSlope` = height·π/width ≤ `TERRAIN.maxJumpSlope`, so terrain.ts never widens it off the grid and kills the clean-triangle launch). Placement/clearance/hilliness (the shared rules) still apply to kickers. Both new helpers are pure + unit-tested; every bad-fixture trips exactly one rule.
- **Why:** PLAN-02 proved a kicker and a gentle hump are geometrically distinct enough that ONE bound set can't sanely cover both (a hump is wide-and-short and must stay ≥400/≤100 to launch flat/gas-safe; a kicker is narrow-and-tall-and-grid-aligned and must be ~336×106 to launch nose-up) — exactly the case the task said to prefer (a) for. Widening the hump floors to admit a kicker (option b) would let a genuinely-dangerous narrow+tall hump slip through the guard, defeating its purpose. The guard stays MEANINGFUL: the synthetic bad-kicker fixtures prove it still rejects an off-grid, too-tall/steep, or too-wide kicker, and every one of the 22 real configs passes (`validateJumpSafety(LEVELS) === []`, `validateFlatZones(LEVELS) === []`). No personal content involved.
- **How chosen:** Simplest option that keeps the guard meaningful for BOTH shapes (Rule 3, CLAUDE.md); the `kind` flag + kicker-specific bounds are the minimal change that lets a kicker be validated as a kicker. Verified: `npm run build` clean, 484 tests green (467 prior + 17 new: 7 kicker-bounds/grid/slope fixtures, 3 pure-helper tests, 7 tutorial-sign + per-level kicker-count assertions).

## 2026-07-16 — Per-level kicker geometry: uniform grid-aligned 336×106, measured flip AND gas-only-safe (PLAN-07 task 4)
- **Choice:** Levels 2/4/9/17 each get flip-capable kickers, ALL the SAME proven geometry — `{ width: 336, height: 106, kind: 'kicker' }`, base `x` grid-aligned to 168px — replacing their old gentle humps. Counts honor NORTH_STAR §5's character: L2 (first jump/tutorial) 2 kickers, L4 (park, extra jumps) 2, L9 (construction) 2, L17 (hilly, tulip farming) 3 (the most). Bases: L2 {4032, 6048}; L4 {4032, 6552}; L9 {5040, 7560}; L17 {5040, 7560, 10080} — every one at 35–70% of its level length, ≥3782px clear of spawn and ≥3452px clear of finish (both well over the 1500 floor), with ≥1680px of flat landing gap between consecutive kickers.
- **Why (the numbers flip AND survive gas-only, cited to measurement):** 336×106 is PLAN-02's browser-proven kicker, and the current bike physics (`BIKE_TUNING.airSpinStepPerStep` 0.04) were tuned for exactly it. The choice is validated by real-Chrome runs on the CURRENT builder, not inherited assumption: (a) GAS-ONLY survival — the full 22-level `playtest-levels.mjs` sweep finishes L2/4/9/17 gas-only with **0 crash-restarts** (21.4s/23.5s/26.9s/31.9s, all inside 20–45s), and `playtest-tulips.mjs`'s gas-only level-2 drive over both kickers peaks at **16.8° of rotation** (125 landings) — far under the 330° award threshold, so a gas-only hold NEVER accidentally flips (the SACRED invariant + "no accidental flips" both hold). (b) FLIP capability — `playtest-tricks.mjs flip` deliberately taps gas mid-air off L2's first kicker and lands a clean backflip **3/3** at −388° to −392° (~30° past the 360 gate), awarding a tulip each time. Same kicker, opposite outcome by input history — NORTH_STAR §4 "flips achievable but optional" made physically real. Kept all kickers UNIFORM (not bigger on L17) because 106px is the tallest that stays gas-only-safe per PLAN-02's airtime caveat (`heldPitchDelaySteps`); "most kickers" (3 vs 2) gives L17 its tulip-farming character without a riskier per-kicker size. (c) BODY BUDGET — kickers are pure heightmap edits, ZERO added Matter bodies: L17 (longest, 14500px) measured **90 bodies**, max across all 22 stays **99 < 100**; L2 stays exactly **63** (pre-tricks baseline, unchanged).
- **How chosen:** Reused PLAN-02's proven dimensions rather than re-deriving new ones (Rule 2/3 — evidence over guessing), grid-aligned every base so the coarse chain renders each as a clean launch triangle, and MEASURED both the flip and the gas-only survival on the current terrain builder before committing. No personal content (the sign text is logged separately).

## 2026-07-16 — Level-2 tutorial sign: em dash renders correctly, kept byte-exact (PLAN-07 task 4)
- **Choice:** Level 2's tutorial sign reads exactly `Big jump ahead — try holding GAS in the air to flip! 🌷` (em dash U+2014, tulip U+1F337), placed at x=3200 — BEFORE the first kicker (x=4032) so it teaches before it asks. Byte-exact, NOT paraphrased; guarded by code-point assertions in `tests/levels.test.ts`.
- **Why:** PLAN-07 task 4 pins this copy verbatim. The one open risk was the em dash (Press Start 2P is a limited pixel font). Verified by SCREENSHOT (`playtest-out/level2-sign.png`, personally reviewed): the em dash renders as a clean horizontal rule and the 🌷 as a real tulip — NEITHER tofus. The dash renders via the `FONT_STACK_PIXEL` monospace fallback (`'Press Start 2P', 'Courier New', monospace`) — canvas does per-glyph font fallback, so a glyph Press Start 2P lacks falls through to Courier New, which has the em dash — and it visually matches the chunky pixel style. So the SAFEST faithful option (keep the exact wording, verify the glyph) held; the hyphen-minus surrogate the plan allowed as a fallback was NOT needed.
- **How chosen:** Verify-before-claim (Rule 2): screenshotted the actual rendered sign rather than assuming the glyph. Personal content used exactly as specified (Rule 3/4).

## 2026-07-16 — Flip harness re-aimed at level 2's kicker; release tuned to −4.5 rad to avoid over-rotation crash (PLAN-07 task 4)
- **Choice:** `scripts/playtest-tricks.mjs` flip mode is REACTIVATED (was dormant since PLAN-05 removed TEST_LEVEL): `RAMP_X`/`RAMP_W` re-point at level 2's first kicker (4032/336), `flipAttempt` restarts into `FLIP_TEST_LEVEL_ID` (2), the dormant early-return is gone, and a real flip loop + POSITIVE-AWARD-PATH gate was added. The recipe's release threshold `FLIP_RELEASE_ROT_RAD` is **−4.5 rad (−258°)**, tuned by browser measurement.
- **Why (the recipe tuning, measured):** The old recipe released near a full flip (−6.3 rad); at that threshold the bike, launched by the current kicker's ~58 steps of air, keeps rotating ~+120° AFTER release (built-up angular momentum decays slowly under the assist) and lands at ~−515° = 155° past upright — nose-diving into a HEAD-CLIP CRASH, so NO award fired (diagnosed via `__tricks.landings`/`maxLandingRotationDeg`: the tricks system saw only the post-restart spawn-settle at 0°). Releasing EARLIER at −4.5 rad lands the flip at −388° to −392°: past the 360 gate with ~30° margin (robust against the ±360 landing-rotation flakiness PLAN-02/04 flagged) yet ~30° from upright, so it lands CLEAN and awards. Measured across the tuning sweep: release −4.0 → land −363° (award ✓), −4.5 → −374/−388/−392° (award ✓), −5.0 → −395° but settles further over (riskier). −4.5 chosen for the best margin-vs-clean-landing balance.
- **POSITIVE AWARD PATH now PROVEN end-to-end** (Task 1 could only prove the NEGATIVE before a kicker existed): the flip harness gates that each landed flip (1) reaches ≥360° clean, (2) sets `__tricks.awardedTulips` to the expected flip count, (3) renders the byte-exact `Backflip!! 🌷` toast as a live Text object (recursive display-list search, code-point compare), and (4) bumps `gabby22.tulips` by exactly the flip count, with the cumulative total surviving a page reload. Result: **3/3** attempts award + toast + persist, `gabby22.tulips 0 → 3` survives reload. Exit 0.
- **Residual flakiness:** none observed — 4 measured attempts at −4.5 rad landed −374/−388/−389/−392°, a tight cluster ~30° past the gate. The recipe is env-overridable (`FLIP_RELEASE_ROT`) for future re-tuning if terrain/physics change.
- **How chosen:** Diagnosed the zero-award failure to the specific cause (over-rotation crash, not a detection bug) via the tricks debug snapshot before touching anything (Rule 2, systematic-debugging), then tuned the harness recipe — NOT the game — to land the flip in the sweet spot. No game physics or level geometry changed for the harness's sake.

## 2026-07-21 — Notes engine (PLAN-08 task 2): all four fixed notes use the 'hint' card; fact-pool accuracy fixes; resetNotesSeen added
- **Choice (a) — style mapping:** All four fixed, level-bound notes (L6/L9/L13/L14) return style `'hint'` (the "Psst… 💡" whisper card), NOT `'fact'` ("Did you know?"). PLAN-08 only pinned the two literal hints (L6 "cars can't see motorcycles", L14 "cops pull over motorcycles") as whispers; I extended the SAME card to the two personal asides (L9 "Caleb is most definitely a tease", L13 "feeling down his chest and stomach…"). Each fixed note carries its OWN explicit `style` field in `src/data/notes.ts` (data-driven, not hardcoded by level), so re-framing any single one later is a one-line change.
- **Why (a):** A personal, flirty aside framed as chirpy trivia ("Did you know? Caleb is most definitely a tease") reads wrong — it's a whispered inside joke, not a fun fact. The whisper card fits all four; the random real-motorcycle facts are the only `'fact'` cards. Not paraphrasing or altering the locked text (Rule 4) — only choosing which card frames it, which NORTH_STAR §6 leaves to the implementer.
- **Choice (b) — fact-pool accuracy corrections:** The 20-fact pool is NOT locked content (only the four fixed notes are — NORTH_STAR §6/§7); PLAN-08 supplied a DRAFT and explicitly required verifying each fact TRUE. Corrections made vs. the draft: (1) **counter-steering** reworded to be physically correct — "to go right at speed, you briefly steer left first" (front wheel momentarily steers away from the turn to initiate lean); the draft's phrasing was ambiguous/possibly backwards. (2) **fuel efficiency** — dropped the draft's shaky "typically 2–3x more efficient than cars" hedge for a defensible "plenty get well over 50 mpg, far better than the average car." (3) **wheelie fact** — the draft's "wheelies were originally a drag-racing accident that became a sport" is dubious history; replaced with the verified Guinness record: "the longest motorcycle wheelie is over 200 miles" (Yasuyuki Kudo, ~205.7 mi / 331 km, 1991). (4) **helmet fact** — replaced the vague "must survive impacts at highway speed" with the concrete, true mechanism: the EPS foam liner crushes on impact and that is what absorbs the hit. (5) **non-ASCII punctuation normalization** — every en/em dash (`–`/`—`) became a spaced ASCII hyphen ` - ` and the two `…` ellipses were removed/reworded, so all facts are pure ASCII except fact 20's deliberate tulip emoji (U+1F337, written as a `\u{1F337}` escape). Kept intentionally: fact 19's "Two-up" (callback to level 13) and fact 20's tulip line (ties to the game's tulips). A `tests/notes.test.ts` guard bans en/em dash, ellipsis, and curly quotes across the whole pool so a re-encode can't silently reintroduce them.
- **Why (b):** CLAUDE.md Rule 4 protects the FIXED notes, not the fun-facts pool; the pool must be light AND true (NORTH_STAR §6 "light and true"), and the pixel font renders ASCII crisply, so normalizing punctuation avoids tofu/fallback-glyph risk while emoji are proven fine (tricks.ts toasts).
- **Choice (c) — resetNotesSeen():** Added `resetNotesSeen(): void` to the `SaveSystem` interface + `createSaveSystem` factory in `src/systems/save.ts` (it does `store.removeItem(KEY_NOTES_SEEN)` only, matching save.ts's total-function style). The notes engine calls it to recycle the fact pool once every pool index has been shown (exhaustion judged by POOL coverage, never `notesSeen.length`, so stale/out-of-range indices can't wedge it). NEW-playthrough reset stays `resetAll()`'s job (it already clears notesSeen) when a future "Play again" flow starts — the engine never implements new-playthrough detection.
- **Why (c):** The existing API could only clear the seen-set via `resetAll()`, which would also wipe character/progress/tulips — too broad for a pool recycle. A dedicated single-key clear keeps the exhaustion-reset surgical and leaves playthrough-reset semantics where they belong.
- **How chosen:** Rule 3 judgment calls, all within HOW (card framing, fact wording, save surface) — no fixed personal content altered. Facts verified individually for truth; behavior pinned by `tests/notes.test.ts` + `tests/save.test.ts`.

## 2026-07-21 — Level-complete screen (PLAN-08 task 1): layout, earned-tulips copy, confetti + typewriter approach, and the markLevelCompleted move
- **Question:** PLAN-08 task 1 delegated several HOW choices to the implementer: the earned==0 tulip copy, the exact button layout ("vertically laid out"), the note-card title glyphs, and how to make a word-wrapped typewriter not reflow.
- **Choices:**
  - **earned==0 copy:** "No tulips this level" (kind, never a scolding bare "0") with a dimmed tulip icon; earned>0 shows "x{n} this level!"; the total row shows "Bouquet: {total}". Both rows are a tulip SPRITE (`TEXTURE_KEYS.tulip`) + pixel text, matching the in-game bouquet HUD's sprite use rather than a 🌷 text emoji.
  - **Buttons in TWO rows:** the big primary "Next level →" alone (minWidth 380), then "Replay" + "Level select" side-by-side below (minWidth 300, offset ±210 from center). Row centers kept ≥ one button-height (88px) apart (548 / 664) so the faces never overlap — the first draft at 588/664 overlapped by ~12px (screenshot-caught), now regression-guarded in `tests/constants.test.ts`.
  - **Card titles:** hints → "Psst… 💡" (ellipsis U+2026, bulb U+1F4A1, both `\u`-escaped so a re-save can't mangle them); facts → "Did you know?". Screenshot-verified both render (the … falls back through `FONT_STACK_PIXEL` to a monospace ellipsis; 💡/🎉 render as color emoji, like the existing 🌷/💛 toasts).
  - **Typewriter:** reveals the PRE-WRAPPED note (fixed `\n` from Phaser's `getWrappedText`) by growing a CODE-POINT substring (`Array.from`, so fact 20's 🌷 surrogate pair never splits) — the layout never reflows/jumps as words complete, and an emoji reveals as one unit. Left-aligned, top-left-anchored body so it grows down-and-right without recentering. A tap/click anywhere skips to the full text (gentle default speed for the patient).
  - **Confetti:** a per-frame integrated burst (plain Rectangles with velocity + gravity, alpha fade, self-destroying at end-of-life) rather than tweens — a natural "gravity" feel and trivially self-cleaning (no leftover tweens/objects across the many re-entries), drawn at `DEPTHS.fx` BEHIND the text so it never obscures reading.
  - **markLevelCompleted move + level-22 skip:** completion is now marked in GameScene's finish handler (so level 22, which skips LevelCompleteScene for PartyScene, still gets marked) and LevelCompleteScene ONLY displays. "Earned this level" = current total − GameScene's tulip snapshot taken at the level's FRESH start (NOT re-taken on a fail-restart), so awards that persist any time up to the finish hand-off — including during a finale hold (PLAN-07 tricks.ts caveat) — are all counted. The two regression harnesses' completion detection was updated for level 22 → PartyScene.
- **Why:** All within HOW — no personal/locked content touched (the note text comes verbatim from the notes engine). Kept EASY/CUTE/legible per NORTH_STAR §4/§6.
- **How verified:** new committed harness `scripts/playtest-levelcomplete.mjs` (8 gated checks, exit 0) asserting the byte-exact L6 note + "Psst… 💡" title + "Level 6 complete!! 🎉" header against independent code-point literals, the fact card, earned 3/total 5 (seed 5 / tulipsAtStart 2), the typewriter reveal + tap-skip, all 3 buttons routing by real click AND touch tap, and level 22 finish → PartyScene (`completed[21]`) vs level 1 finish → LevelComplete (`completed[0]`); plus green re-runs of `playtest-tulips.mjs` and the full `playtest-levels.mjs` sweep (updated for the level-22 terminal-scene change).

## 2026-07-21 — PLAN-08 task 3: fail overlay = "Try again" button (instant path) + 2.5s no-input auto-restart
- **Question:** PLAN-08 task 3 asks for a "restart button + auto-restart after 2.5s", but NORTH_STAR §4 says fails are an "instant, friendly restart" and PLAN-02's budget is "restarts are < 500ms". How do these reconcile, and is the generic message pool locked content?
- **Choice:** The fail overlay now shows the message + a **"Try again" button**, and the button IS the instant path — tapping restarts immediately (browser-measured 24–54ms, well under the < 500ms budget, which now applies to the BUTTON path). If the player never taps, a gentle **`FAIL.autoRestartMs` = 2500ms** no-input auto-restart fires as a courtesy fallback (replaces the old 350ms `overlayDurationMs`, which is removed). Both routes funnel through ONE idempotent `restart` closure in `GameScene.failLevel` (a `restarted` latch) so a restart can never fire twice; `scene.restart()`'s shutdown sweeps the overlay/button/pending-timer. A GENERIC fail (head crash / fell-off-world — `failLevel()` with no message) shows a RANDOM message from a new `FAIL_MESSAGES` pool (`Oops! Go again 💛` / `So close!! One more time` / `Even MotoGP riders crash sometimes 💛`, via the pure `pickFailMessage(rng)`); a SPECIAL fail (level-7 traffic / level-15 police) still passes its own message through unchanged and shows it VERBATIM.
- **Why the 2.5s is safe / on-vision:** the button gives the "instant restart" NORTH_STAR promises; 2.5s is only the *no-input* fallback, and it makes the beat KINDER for a non-gamer — enough time to read the friendly message and see a clear tap-to-retry rather than being yanked back in 0.35s. No lives/penalties/timers added (Rule 4 intact).
- **Locked-content ruling:** only `Oops! Go again 💛` is quoted verbatim in NORTH_STAR §4, so ONLY it is locked — it MUST stay in the pool (byte-guarded in `tests/constants.test.ts` with an independent U+1F49B code-point literal, mirroring the traffic/police guards). The other two pool messages are free copy (they touch no personal content). The locked traffic/police soft-fail strings were NOT touched.
- **Button anchoring gotcha (logged so it isn't re-discovered):** `createPixelButton` defaults its container to `DEPTHS.ui` (110) — BELOW the dim rect at `DEPTHS.overlay` (120) — so the button MUST be lifted to `DEPTHS.overlay + 2` and `setScrollFactor(0)`. It is deliberately NOT zoom-compensated (it scales with zoom like the message; the camera is back near zoom 1 by the time the crashed bike settles, and the `UI_MIN_TOUCH_PX` tap target stays generous meanwhile).
- **How verified (browser):** new committed `scripts/playtest-fail.mjs` (exit 0) proves a GENERIC fail shows a pool message + a real "Try again" button, a real tap restarts in ~38ms, NOT tapping auto-restarts in ~2519ms (inside a [1800,4200]ms window), and a SPECIAL fail (level 7 into a car) still shows the byte-exact traffic message + the button; `playtest-drive.mjs` was re-pointed off the removed <500ms auto-assertion onto the BUTTON path (tap restart 24ms, body count 57 stable across 3 restarts + a real fail). All fail-exercising harnesses re-run green — `drive`, `fail`, `level07` (both dodge profiles 4/4 + verbatim gas-only soft-fail), `level15` (gas-only 3/3 never caught + stopped→verbatim caught→restart), `tricks flip` (3/3, tulips 0→3), `tricks gasonly`, and the full 22-level `playtest-levels.mjs` sweep. A harness gotcha found + fixed along the way: teleporting the bike only just below `worldBottomY` lets the stiff suspension struts yank the chassis back above the fell-off threshold in one frame (the fail misses) — the teleport must be FAR (y=5000), and the DEV `__fail` handle is cleared in `create()` so a harness never reads a stale fail across a restart.

## 2026-07-21 — PLAN-08 task 4: story-cohesion pass on the 22 intros (one surgical edit) + committed rotation/reload harness
- **Assessment:** Read all 22 level `introText`s in order alongside the four fixed notes (L6/L9/L13/L14) and the 20-fact pool, as one played-in-order story arc. The arc is already coherent and warm: the L9 fixed note ("Caleb is most definitely a tease") foreshadows Caleb ~3 levels before his L12 pickup; L12 "Caleb's waiting outside..." → the (locked) "Caleb hopped on!!" toast → L13 "and now there's two of us!" reads as a clean pickup→two-up beat; the traffic setup (L6 skyline + fixed "cars can't see motorcycles" hint → L7 "Rush hour downtown... stay sharp") and the cop setup (L14 "making great time" + fixed cop hint → L15 "is that a police car behind you?") both land; and the party build-up escalates by sense — L20 balloons (see) → L21 streamers "can you feel it?" (feel) → L22 "you can hear the music from here!!" (hear). So this was a minimal-edit pass, not a rewrite (over-editing is the failure mode here).
- **The one wording change — L11 `introText`:** `'Merging onto the highway. Hang on tight!'` → `"Merging onto the highway. Let's open it up!"`.
  - **Why:** the ONLY objective rough spot across the 22-line set was a near-duplicate closer — L2 already ends "Hold on tight!", and L11 ended "Hang on tight!". Nine levels apart, but it's the same stock beat twice, and freshness across the one-liner set is exactly what a cohesion pass tightens. The replacement removes the duplicate AND gives L11 (the highway on-ramp, where the yellow-motorcycle easter-egg rider wheelies past) a touch more of its own speed identity, while "Let's" echoes the opener L1's "Let's go!!" for a subtle forward-momentum thread. Kept ASCII-only, single-spaced, no leading/trailing whitespace, and still cute/EASY (narrative flavor about the fun of highway speed, never a mechanic — NORTH_STAR §4). No test asserts intro copy (the ASCII/whitespace guard in `tests/levels.test.ts` covers only level 18's billboards), so no test needed updating; the full suite stays green (511 tests).
  - **Left deliberately unchanged:** every other intro, all four fixed notes, the 20-fact pool, and every easter-egg/personal string (L18 billboard, L2 tutorial sign, trick/pickup toasts, MELIN mailbox, police WOOHOO!/soft-fail messages, credits, character names). NO locked/verbatim content was touched.
- **New committed harness `scripts/playtest-notes-rotation.mjs` (exit 0):** exercises the plan's "no repeat across a real playthrough + reload preserves the seen-set" criterion end-to-end through the REAL LevelCompleteScene + notes engine + save system (which the unit tests and `playtest-levelcomplete.mjs` did not cover as a rotation). It resets the save, directly-starts LevelCompleteScene for a sequence of FACT levels (1,2,3,4,5 then 8,10,16) collecting each shown note, asserting all are style `'fact'` and pairwise distinct; confirms a fixed level (6) shows its byte-exact verbatim note under style `'hint'` in the same run WITHOUT consuming the pool; then RELOADS the page mid-run and asserts `gabby22.notesSeen` persisted byte-identical (`[19,13,6,1,8]` before and after) and that no post-reload fact repeats a pre-reload one. **What is DETERMINISTIC vs. probabilistic here:** because the scene drives the real `Math.random`-backed `selectNote`, the "all distinct / no cross-repeat" assertions are a PROBABILISTIC end-to-end smoke check on ONE run (a correct engine always yields distinct facts so green is stable; a hypothetical no-repeat regression would only sometimes collide in a single run) — the DETERMINISTIC no-repeat proof (injected RNG, full-pool cycle) stays in `tests/notes.test.ts`. This harness's own load-bearing DETERMINISTIC guarantees are `seenBefore.length === 5` (the fixed L6 does not consume the pool + the fact draws actually persisted) and the byte-identical `notesSeen` across the reload. Same structure as the sibling harnesses (playwright-core system Chrome, `window.__gabbyGame` + the DEV `__levelComplete` hook, screenshots to gitignored `playtest-out/`, gated exit code); dev server killed after (port 5173 verified free).
- **How chosen:** Rule 3 judgment call within HOW (intro wording only), guided by the plan's "make only genuine improvements / over-editing is a failure mode." Verified: `npm run build` clean, `npm run test` 511/511 green, the new harness exit 0.

## 2026-07-21 — PLAN-08 execution order + the "17 fact-levels" clarification (coordinator notes)
- **Execution order 2 → 1 → 3 → 4 (not the plan's numeric 1 → 2 → 3 → 4):** PLAN-08 lists LevelCompleteScene as task 1 and the notes engine as task 2, but the complete screen CONSUMES the engine (`selectNote`). Building the scene first would have forced the task-1 implementer to invent/stub the notes interface, then rework it when task 2 landed. Running the pure, fully-tested notes engine FIRST let the scene consume a finished contract with no rework. Tasks 3 (fail overlay) and 4 (story pass) stayed in order. Pure HOW/sequencing choice; no scope or content change.
- **The fact pool serves 17 fact-levels, not 18 (holistic-review catch):** task 2's original `src/data/notes.ts` comment + a `tests/notes.test.ts` test NAME said "18 fact-levels" (22 total − the 4 fixed = 18). But task 1 made level 22 SKIP LevelCompleteScene (→ PartyScene), so only levels 1-21 ever show a note; minus the four fixed-note levels (6/9/13/14) = **17** fact-levels in a linear playthrough. The 20-fact pool with the conservative `>= 18` floor still guarantees no forced repeat with headroom — this was purely an inaccurate prose count that became stale once the level-22 skip (a task-1 decision) existed, a cross-task subtlety the per-task reviews couldn't see. Corrected the comment + test description to "17" (floor assertion left at `>= 18`). No logic changed.
- **How chosen:** Coordinator judgment (Rule 3). The full PLAN-08 change was verified end-to-end on HEAD `feb155f`: `npm run build` clean, 511 tests green, holistic integration review = SHIP, and the entire committed-harness suite (18 runs incl. the 22-level sweep) exits 0.

## 2026-07-22 — PLAN-09 ST-1: party cast layout, guest looks, Allison's tell, and the crowd's half-step interleave
- **Question:** PLAN-09 task 2 fixes WHO is at the party and WHAT they are called, but leaves the implementer every HOW: the guests' placeholder colors, what makes Allison "clearly distinct" from Andrea, how many unnamed partygoers inside 8–15, and how the whole cast is arranged.
- **Choices:**
  - **Dallas is modeled as a data VARIANT, not a color set.** `src/data/finale.ts` types a guest's look as a union: `{kind:'authored', hairColor, eyeColor, suitColor, hairStyle}` or `{kind:'mirrorsPlayer'}` — and Dallas is the latter, with NO color fields to fill in. The twin joke (NORTH_STAR §5 "sprite looks the same as the Gabby character") therefore cannot be broken by someone later hand-authoring her; there is nowhere to put the colors. At render time she reuses the very same `riderTextureKey` `buildCharacterTextures` returns for Gabby (browser-verified: `tex-gabby|pink|green|stealth` for both, and both switch to `tex-gabby|blue|hazel|party` together when the saved character changes).
  - **Allison is distinguished on THREE independent axes,** not one: a lighter/redder brown (`0x9c6b3f` vs Andrea's `0x6b4423`), a completely different suit (`0xff8fc4` pink vs `0x3fb8a8` teal), and a ponytail silhouette (a small hair-colored overlay rect off the side of her head — the same cheap overlay trick `pickup.ts`'s standing Caleb uses for his brown hair band). One difference could vanish under PLAN-10's real art or a color-blind viewer; three cannot. Both still read as "brown-haired girl" per NORTH_STAR §5. The ponytail is deliberately EXCLUSIVE to her — no crowd member ever gets it, so it stays a reliable tell.
  - **Dom is blonde (`0xf2d16b`, the game's own blonde swatch); Caleb keeps his brown hair band.** That convention (DECISIONS.md 2026-07-15) exists precisely so Dom and Caleb are never confused — this is the level where it pays off.
  - **Crowd = 9 unnamed partygoers, spaced 150px, i.e. the SAME spacing as the front row but an ODD count centered on the same x.** That lands every crowd member exactly a HALF step (75px) from its nearest front-row neighbour — squarely in the gaps, like the back row of a group photo — which clears the summed sprite half-widths (36 + 26.4 = 62.4) so nobody is hidden. 9 is inside NORTH_STAR §5's 8–15, and with the 6 front-row members the venue holds 15 people alternating every 75px across the full screen, which reads as a packed party. **This was screenshot-driven:** the first draft used 12 crowd members at 104px spacing, and the real-Chrome screenshot showed crowd heads poking out from directly behind Andrea (11px apart) and Dom (11px apart) — they read as HATS, not as people. A `tests/finale.test.ts` guard now asserts the sprite-level non-overlap so a future spacing tweak can't silently reintroduce it.
  - **Layout is a pure function of the member index, never `Math.random()`:** front row left→right is Andrea, Dallas, Gabby, Caleb, Allison, Dom — Gabby+Caleb straddle the exact centre, Dallas is IMMEDIATELY to Gabby's left (adjacency is what makes the twin gag land), and the other three flank the pair. Crowd looks come from `crowdGuestAppearance(i)`, which cycles hair/suit/eye lists of coprime-ish lengths 5/6/4, so the first 60 members would all be distinct combinations and no two neighbours share a hair color. The party therefore looks identical on every visit and the tests/harnesses can assert exact positions.
  - **Idle motion is a literal 2-frame bounce** (`castBounceOffsetPx` returns 0 or −amplitude, never an interpolated value) rather than a sine — the plan says "2-frame bounces are fine" and a hard flip is what pixel art actually does. Each member gets its own phase (`index * 0.37 mod 1`, distinct for the first 100 members), and each name tag adds its own slower, half-cycle-offset bob on top so a tag drifts against its owner instead of welding to it.
  - **Where the numbers live:** every tunable (positions, depths, scales, crowd count, bounce, name-tag geometry) went into a new `PARTY` block in `src/systems/constants.ts` with room reserved for ST-2's balloon knobs and ST-3's scene knobs; every COLOR and all verbatim copy live in `src/data/finale.ts`; only the throwaway placeholder DRAWING dimensions (the 24×48 sprite size, Caleb's hair band, Allison's ponytail rect) stay as local documented consts in `partyCast.ts`, following the decorations.ts / pickup.ts / police.ts / tricks.ts precedent since PLAN-10 replaces that art wholesale.
- **Why:** All within HOW — no personal content invented or altered. The four names, `HAPPY 22nd GABBY!!`, `You brought N tulips to the party!! 🌷` and the three credits lines are reproduced VERBATIM from NORTH_STAR §5 in one module, with byte-exact independent-code-point oracles in `tests/finale.test.ts` (the discipline `tests/notes.test.ts` uses for the four fixed notes). **The N=1 "1 tulips" grammatical wart is INTENTIONAL and pinned by a test** — the sentence is locked personal content, so it is never pluralized or branched.
- **How verified:** `npm run build` clean; `npm run test` 584/584 green (511 → 584, +73 new). Plus a throwaway real-Chrome render check (playwright-core, system Chrome headless, 1280×720, against the running dev server) that mounted the cast into the live PartyScene and read back: all 15 sprites + 4 name tags rendered as live GameObjects; the tags read exactly `Andrea` / `Allison` / `Dallas` / `Dom`; Dallas's texture key === Gabby's (both before and after a character change); Caleb on `tex-caleb`; **12** distinct `tex-party|…` palette-swap variants on screen — 9 crowd + Andrea/Allison/Dom — in their own namespace with no collision with `tex-gabby|` (14 is the count of distinct texture keys across all 15 sprites, i.e. those 12 plus `tex-gabby|…` and `tex-caleb`); `update()` moved 16/20 containers over 500ms; `destroy()` returned the display list to its pre-mount count exactly; zero console and page errors. ZERO Matter bodies — the whole cast is plain Containers/Images/Rectangles/Text.
- **Post-review follow-up (same day):** code review flagged that the Dallas-twin invariant had no committed coverage (the texture decision lived inside a `createPartyCast` closure). Extracted the pure `castTextureSource(slot): 'caleb' | 'player' | 'authored'` so `castTextureSource(dallas) === castTextureSource(gabby) === 'player'` is now a unit test and `textureKeyFor` collapsed to a switch over it. Also: `PartyCastMemberInfo` now derives from `PartyCastSlot` via `Omit<…, 'appearance'>` and carries `phase01` (ST-3 needs it to sync Gabby's bouquet to her bounce); `FrontRowEntry` became a discriminated union so `{role:'gabby', guest: GUEST_DOM}` is unrepresentable; `update()` uses an indexed loop so its "allocates nothing" claim is literally true; and the crowd's hair/eye tones are now derived from `HAIR_OPTIONS`/`EYE_OPTIONS` via `data/characters.ts`'s resolvers instead of re-hardcoding the same eight hex literals. **One review item was NOT applied as written:** the claim that `nameTagGapPx`'s margin was "~2-3px optimistic" because Press Start 2P renders ~20-22px tall at 16px. Browser-measured at 8/16/24/32/40px (and on the live tag panels: `labelH` 16, `panelH` 32, panel top 314 vs head top 356), Phaser reports a single-line Text height of EXACTLY 1.0× the font size — so the original model was exact, not optimistic. Kept the model but restated it in terms of the MEASURED label height (which is what the code uses) with a conservative 1.5× line-height bound covering a font-load fallback to Courier New, and guarded that bound in the test.

## 2026-07-22 — PLAN-09 ST-2: one shared confetti system (and the LevelComplete migration), plus the pooled poppable balloons
- **Question:** PLAN-09 adds three new confetti consumers (PartyScene's continuous fall, CreditsScene's continuous fall, a burst on every balloon pop) on top of the one `LevelCompleteScene` already had. Where does the shared code live, where do its numbers live, and how do the balloons meet "at least 20, varied, tappable, endless supply" at 60fps?
- **Choices:**
  - **Extract, don't copy a fourth time.** New `src/systems/confetti.ts` owns ONE integrator, ONE fade curve, ONE recycle rule, and two factories over them: `createConfettiBurst` (re-triggerable one-shot pop) and `createConfettiFall` (endless rain). This is the same lesson `src/systems/pixelText.ts` exists to record — four files had each replicated one ~15-line helper and had already diverged. `LevelCompleteScene`'s private `ConfettiPiece`/`spawnConfetti`/`updateConfetti` were **deleted** and it now holds a burst handle; its `LEVEL_COMPLETE.confetti*` values are byte-identical and `scripts/playtest-levelcomplete.mjs` still exits 0.
  - **ONE burst implementation covers both shapes via a single number.** `launchSpreadRad` is the fan half-angle around straight up: LevelComplete passes its tuned 1.15 rad (an upward pop), a balloon pop passes `Math.PI` (a full radial puff). No `mode` flag, no branch.
  - **NO shared `CONFETTI` constants block — the knobs stay with their consumer.** Every value is a per-screen FEEL choice (LevelComplete's burst is tuned to clear a 40px header on a pastel menu; a pop is a small quick puff; the party rain is a slow drift), and no single number is genuinely shared, so a shared block would either duplicate values or force one screen's taste on another. `LEVEL_COMPLETE.confetti*` therefore did not move; `PARTY` gained `popConfetti*` and `confettiFall*` sub-sections; the module takes them as options. One number, one home. The one thing all three DO share is the color SET, which lives in `confetti.ts` as `CONFETTI_COLORS` (lifted verbatim out of the scene) — a color set is presentation content, not a tunable number, the same distinction the `LEVEL_COMPLETE` block's own doc already draws.
  - **Exact closed-form integration instead of semi-implicit Euler.** `stepConfettiKinematics` uses `y += v*dt + g*dt^2/2` rather than the scene's old `v += g*dt; y += v*dt`. For CONSTANT acceleration the closed form is exact, so one 16.7ms step and two 8.35ms steps agree to floating-point noise — a 120Hz phone and a 60Hz laptop rain identically, and the unit test can assert that with real equality rather than a fudge tolerance. The deliberate, documented cost is NOT sub-pixel and the first draft of this entry understated it by ~160x: summing both forms gives an exact gap of `new - old = -g*dt*T/2` that GROWS LINEARLY with flight time — 7.5px per second at LevelComplete's own numbers (g=900, 60Hz), i.e. 0.125px after one frame, 7.5px at 1.0s and **20.25px by the 2.7s `confettiLifetimeMaxMs`** (numerically re-derived, not estimated). The exact form falls slightly LESS far, because the semi-implicit one overshot. Accepted anyway, for two reasons: (a) "behavior-preserving" and "identical at 60Hz and 120Hz" are strictly in conflict here — the OLD code already disagreed with ITSELF by 10.13px at T=2.7s between a 60Hz and a 120Hz display, so there was never one behavior to preserve, and the closed form is exactly the dt->0 limit both rates converge to; (b) 20px at end-of-life lands on a tumbling square that has been fading since 60% of its life. The code comment carries the same table.
  - **Balloons are RANDOM where the cast is DETERMINISTIC.** `partyCast.ts` never touches `Math.random` because a group photo must look identical on every visit; balloons are weather, not staging, and a pool that recycled to fixed positions would visibly re-form into rows within a minute. So `partyBalloons.ts` takes an injectable `rng` defaulting to `Math.random` (the `randomCharacterConfig` precedent), which also makes `balloonSpawn` fully assertable in Node.
  - **Motion is a closed-form function of `scene.time.now`, not an accumulator.** Each balloon stores the time and knot y it entered at; every frame recomputes `spawnY - riseSpeed * elapsed` plus a sine sway. Exactly refresh-independent by construction, with no drift to accumulate, and the whole model unit-testable. The sway is a SMOOTH sine deliberately unlike `castBounceOffsetPx`'s 2-frame flip: a person's idle bounce is pixel-art-honest animation, but a balloon on a string genuinely drifts and a hard flip reads as a glitch.
  - **">= 20 balloons present" is made STRUCTURAL, not statistical** (this replaces the first draft's answer, which was neither — it computed an ~87% expected value that no test could fail, over-counted the visible span, and ignored popping entirely; measured floor was 21, one balloon off the criterion, with nobody playing). Two changes: (1) **both flight endpoints are now DERIVED FROM THE ART, not tuned** — a balloon enters at `DESIGN_HEIGHT + bodyHeight` (the exact y at which its top edge touches the bottom of the screen) and recycles at knot y `<= 0` (the exact y at which its bottom edge finishes leaving the top), so the invisible transit band is **exactly zero px long** and a rising balloon is never alive-but-off-screen. `PARTY.balloonSpawnBelowPx`/`balloonRecycleAbovePx` were DELETED rather than retuned, because any tuned value reopens that band; they live in `partyBalloons.ts` as `BALLOON_ENTRY_KNOT_Y`/`BALLOON_RECYCLE_KNOT_Y` beside the body height they derive from. The one accepted cosmetic cost is that the 40px string tail vanishes at the very top edge instead of being waited for. (2) The only remaining out-of-view balloons are ones the player just popped, so that is bounded explicitly: `balloonRespawnDelayMs` shortened 700 -> 400 (0.57x — there is no "blinking back" risk to buy off, since a replacement re-enters at the BOTTOM edge, nowhere near where it popped), and two new constants record the assumptions the bound is proven against: `balloonWorstCasePopsPerSec` and `balloonWorstCaseFrameMs`. The frame term matters because `update()` only tests `now >= respawnAtMs` once per frame, so the real out-of-view window is the delay PLUS up to one frame; modelling it as the delay alone understated the worst case by a frame per pop. `tests/partyBalloons.test.ts` now asserts a DERIVED WORST-CASE LOWER BOUND — `32 - ceil(12 x 0.434) = 26 >= 20`, with a `> 24` slack guard — and sweeps the whole flight proving positive visible area at every y. `balloonCount` went 30 -> 32 so that bound keeps real slack over the guard once the frame is counted (and "lots of balloons" is what the plan asked for anyway).
  - **The worst-case pop rate is an INSTANTANEOUS BURST rate (12/sec), not an average — and the browser found that out.** The first bound used 8/sec as a sustained average; the harness then measured **8 balloons out of view at once at a 4.2 pops/sec average**, because real taps arrive in clusters and two clusters can land inside one respawn window. An average-rate bound understated the true dip by 2x. The same 12/sec now also sizes the pop-confetti pool (`popConfettiConcurrentBursts` 4 -> 12), so one written-down assumption drives both.
  - **Hit areas are decoupled from the art, and one press pops exactly one balloon.** A drawn balloon is only ~58x77px, so each gets its own invisible `UI_MIN_TOUCH_PX` (88) Zone centred on its body — `pedals.ts`'s visible-face-vs-hit-Zone split and `CHARACTER_CREATE.swatchHitSizePx`'s budget note. At 30 balloons those Zones DO overlap. **Correction to the first draft of this entry, which misdescribed the engine:** one press pops one balloon because of PHASER's own default, not because of anything this module invents — `InputPlugin` sets `topOnly = true` and splices its hit-tested list down to a single entry before dispatching `pointerdown`, and this project never overrides it (verified in `node_modules/phaser/src/input/InputPlugin.js`). The `(pointer id, downTime)` dedupe (`popEventKey` / `isDuplicatePopEvent`) is therefore **defence in depth, kept deliberately** — two string comparisons per press so that the day anything sets `topOnly = false` a single thumb can't pop a whole column — and it correctly still lets two simultaneous THUMBS pop two balloons (different pointer ids). Popping is idempotent independently of it: a popped balloon returns early AND has its Zone's input disabled so it can't swallow a tap meant for a live balloon behind it. Both properties now have committed unit coverage rather than only a throwaway script.
  - **The balloons own their pop confetti** (create/update/destroy it) rather than making the scene wire two systems together — "balloons that pop with confetti" arrives as one unit. AUDIO IS PLAN-10's: there is a one-line forward-note at the single `pop()` call site and nothing sound-related anywhere in this subtask.
  - **Depths:** ambient fall on `DEPTHS.fx`, balloons `+1`, pop puffs `+2` — all above `PARTY.nameTagDepth`, honouring the invariant ST-1 recorded in `constants.ts` (name tags stay strictly below `DEPTHS.fx` precisely so these layers can sit above the cast). Guarded by a test.
- **Why:** All within HOW — no personal content touched, no scene/credits/transition/button built (those are ST-3..ST-6). ZERO Matter bodies anywhere (plain Rectangles/Images/Containers/Zones), and both new modules keep the `import type Phaser` contract so their pure helpers run in plain Node.
- **How verified:** `npm run build` clean; `npm run test` **687/687 green** (584 -> 687, +103 across three new test files). `node scripts/playtest-levelcomplete.mjs` exits 0 after the migration (byte-exact L6 note, typewriter, all three buttons by click AND tap, level-1 and level-22 routing, no errors) — but note what that harness is and is not: it asserts NOTHING about confetti (`grep confetti scripts/playtest-levelcomplete.mjs` is empty), so it is a SMOKE GATE proving the migrated scene still builds, runs and routes without errors, not evidence the burst looks unchanged. The real evidence for THAT is `tests/confetti.test.ts`'s pin of the exact six-color set lifted out of the scene, plus the `LEVEL_COMPLETE.confetti*` block being byte-identical to before. COMMITTED FACTORY COVERAGE (`tests/confetti.test.ts`, `tests/partyBalloons.test.ts`, over the shared duck-typed fake scene in `tests/fakeScene.ts` — the `tests/palette.test.ts` pattern, no DOM and no runtime Phaser): pool allocated once and never grown across 10 bursts / 3600 rain frames / a full slowest-flight balloon sweep, pieces RETURNED to the pool rather than destroyed, the same Rectangle instances reused on the next burst, an exhausted pool drawing thinner instead of allocating, `destroy()` freeing everything with a second `destroy()` a no-op and `update()`/`burst()` inert afterwards, one press popping exactly one balloon, a repeat press on a popped balloon doing nothing, two fingers on the same millisecond popping two, a popped balloon returning tappable at the derived entry y, **every hit Zone tracking its balloon on every frame** (verified by mutation: deleting the per-frame `zone.setPosition` fails 2 tests, where before it failed none), and the flock re-scattering after a simulated five-minute tab freeze. Plus a throwaway real-Chrome run (playwright-core, system Chrome headless, 1280x720, `hasTouch`), repeated for stability: **32 balloons pooled and 32/32 visible at every idle sample** (the zero-invisible-band geometry); a real MOUSE CLICK popped **exactly one** balloon (14 confetti pieces appeared), its hit Zone went input-DISABLED (31/32 enabled) and re-ENABLED when it floated back in at y=795, the derived bottom-edge entry (32/32); a real TOUCH TAP popped exactly one more; a repeat press on an already-popped balloon was a no-op; **SUSTAINED POPPING: 58 pops in 12.3s (4.7/sec) never dropped below 28 visible, with at most 4 out of view at once** — inside the derived worst-case bound of 6, and the number that actually answers PLAN-09's "balloons poppable" + ">= 20 present". (`constants.ts`, `partyBalloons.ts` and this entry all cite that one run.) 60 fall pieces steady; **display list flat at 292 objects across 10s** and pop confetti settled back to 0 live pieces; `destroy()` returned the display list to baseline 0 and a second `destroy()` was safe; **avg 61.0 fps (min 61.0)** with the whole pool live; zero console and page errors.
- **Also (ST-1 review leftovers):** `tests/finale.test.ts`'s seven layout tests were re-parented back into `describe('buildPartyCastSlots layout')` (a `});` had closed it early, so they had been reporting under the `castTextureSource` describe — grouping only, they always passed). And `partyCast.ts`'s `void appearance;` was **verified** to be a genuine no-op before deletion: with `noUnusedLocals` on, TypeScript deliberately exempts a binding that exists only to be omitted by a rest sibling — `npm run build` is clean without it. A comment now records why no `void` is needed there.

## 2026-07-22 - PLAN-09 ST-2 code-review round: hit-Zone tracking was untested, and three latent bugs it flushed out

- **Question:** The code-quality review approved the subtask but flagged one Important gap and a batch of minors. Which of them were real defects rather than polish?
- **Findings, in order of how much they would have hurt:**
  - **The per-frame `zone.setPosition` had NO test.** Deleting it left all 685 tests green while every balloon became tappable only at its spawn point, drifting visibly away from its own hit area — i.e. PLAN-09's "balloons poppable" silently broken, in the one place `tests/fakeScene.ts` was already able to catch it (the suite only ever read `zone.inputEnabled`, never `zone.x`/`zone.y`). Now asserted in the full-flight sweep, at first-frame placement, and after a pop -> respawn cycle. **Verified by mutation: with the line removed, 2 tests fail.**
  - **A 7-value `cyclingRng` in the balloon fixture made several assertions unfalsifiable.** `balloonSpawn` draws exactly 7 values, so a 7-long cycle handed all 32 balloons the identical spawn — the new "flock re-scatters" test was the first to notice, failing on 1 distinct y. Replaced with a real seeded PRNG (mulberry32) in `tests/fakeScene.ts`, and `cyclingRng`'s doc now warns about the divides-the-draw-count trap.
  - **A backgrounded tab resynced the whole flock into a row.** `scene.time.now` is Phaser's RAW rAF timestamp (`TimeStep.step` -> `Clock.update: this.now = time`), so it jumps by the entire wall-clock gap when a hidden tab resumes, even though `delta` is smoothed. Every alive balloon was then past the recycle line on one frame and all 32 re-entered at the identical entry y — precisely the single-horizontal-row artifact the randomised flight exists to prevent, taking ~10s to disperse. Fixed: an overshoot larger than a whole flight now re-enters via `'initial'` (scattered across the flight) instead of `'recycle'`.
  - **A duplicated helper and a lost clamp.** `partyBalloons.ts` carried a byte-identical private copy of `confetti.ts`'s `confettiRangeAt`, and its inline tint pick had dropped `confettiColorAt`'s LOWER clamp — a negative injected draw would have indexed `-1` and produced an `undefined` tint. Both now import from `confetti.ts`. This is exactly what this module's own doc cites `pixelText.ts` about, three files later.
  - **One wasted rng draw per burst-pool slot.** `createConfettiBurst` rolled a creation color that `burst()` always overwrites before showing the piece, which also put its rng budget out of step with `createConfettiFall` (which has always created with `PALETTE.white`). Made consistent; the pop pool now costs exactly one draw per slot, which is what the documented 168-draw offset in `balloonSpawn`'s rng-budget note depends on.
- **Also corrected (documentation is load-bearing here):** three files had cited three different numbers for the same harness run (52 / 63 / 53-63) — all now cite one; "every unpopped balloon is on screen, always" overstated the endpoints, where visible area is exactly 0 for at most a frame; "0.13px" was rounded up from 0.125 in a comment whose whole job is numerical honesty; "halved 700 -> 400" is 0.57x; the string-tail trade was undersold as "a 3px hairline" when it is 3px WIDE and 40px LONG and blanks out once or twice a second across the flock; and `LevelCompleteScene`'s "self-cleaning" confetti is now pooled-and-freed-on-shutdown. `scripts/playtest-levelcomplete.mjs` is now described as what it is — a smoke gate that asserts nothing about confetti — with the color-set pin and the untouched `LEVEL_COMPLETE.confetti*` block named as the actual evidence the burst is unchanged.
- **Recorded, no code change:** every balloon Container AND its Zone sit at the same `PARTY.balloonDepth`, so both draw order and Phaser's input sorting (`InputPlugin.sortGameObjects`, which falls back to display-list index on a depth tie) are decided by insertion order alone. Because zone *i* is created immediately after container *i*, the two orders agree and the balloon that DRAWS on top is exactly the one whose zone WINS the hit test. That is load-bearing and was undocumented; a comment at the zone creation site now says so, and warns against building all containers then all zones.
- **How verified:** `npm run build` clean; `npm run test` **687/687 green**; `node scripts/playtest-levelcomplete.mjs` exit 0; throwaway real-Chrome re-check after the `confettiRangeAt`/`confettiColorAt`/pool-size changes — 32 balloons, click and tap each still pop exactly one, 58 pops in 12.3s never below 28 visible, 292 display objects flat over 10s, `destroy()` to baseline, 61.0 fps, zero errors.

## 2026-07-22 — PLAN-09 ST-3: the real PartyScene — venue composition, streamers, bouquet anchoring, button timing, and the two things the screenshots overruled

- **Question:** PLAN-09 task 2 fixes WHAT is at the party (venue, streamers, banner, balloons, cast, bouquet payoff, a "Credits ->" button after ~4s) but leaves ST-3 every HOW: what the venue actually looks like, how the streamers are drawn in a screen-space scene, how the bouquet attaches to a bouncing Gabby, how long the toast lives, and where/when the button appears.
- **Choices:**
  - **The scene is an ASSEMBLER, not an engine.** `PartyScene.create()` builds four things — `createPartyCast`, `createPartyBalloons`, `createConfettiFall`, and the venue/banner/toast/button chrome it owns — and `update()` drives the three handles plus one bouquet reposition. No new system module was invented, and no pure logic worth unit-testing ended up in the scene: the fade-in is a Phaser tween (the pickup.ts / tricks.ts / police.ts precedent) and the reveal is a `time.delayedCall`, both of which Phaser already tears down on scene shutdown. What DID get committed coverage is the LAYOUT ARITHMETIC — 15 new `tests/finale.test.ts` cases pinning that the bands run top-down, that the ground line is above every pair of feet, that banner -> toast -> string lights -> crowd heads never collide, that no streamer crosses the banner, that the button is fully on screen in the bottom-right below the cast, and that the bouquet stays on Gabby and clear of her front-row neighbours. Those are the failures a screenshot would otherwise have to catch twice.
  - **Venue = warm-lit BACKYARD at dusk, in level 22's own palette.** Two dusk sky bands (`duskIndigo` -> `plum`), the sunset glowing behind a plank fence, a dark `plum` patio, and a warm `sunsetGlow` pool of light the party stands in — i.e. exactly `themes.ts`'s `finalDusk` ramp plus its `sunshine` "warm lit-window" accent, so riding out of level 22 into the venue never changes palette. A strand of 21 sagging bulbs across the yard is the cue that most says "party" at a glance. The floor is deliberately DARK with light washed over it: a dark ground under warm pools is what reads as "warm-lit at dusk", and it keeps the pastel cast and the cream text panels popping instead of fighting the floor.
  - **Flat bands are rectangles; LIGHT is ellipses.** Both glows started as full-width translucent bands and both had to change, because a saturated warm band does not read as light. The horizon read as a painted ledge / countertop at 22px AND again at 42px; the floor pool just uniformly lightened the patio and read as a different floor color. Both are now two nested translucent ellipses (the alphas compose, which gives the falloff its middle step) — the horizon centred on the fence line, the pool centred between the two rows of feet. Screenshot-driven, twice.
  - **The party's streamers are its own, not `decorations.ts`'s.** That module's `drawStreamer` is a private function keyed to world-anchored level geometry: it takes a `DecorationSpec` plus the terrain `surfaceY` under it and hangs the ribbon a fixed height ABOVE THE GROUND at `DEPTHS.props` in the level THEME's accent color. A screen-space scene at zoom 1 has no terrain, no `DecorationSpec` and no theme, and these ribbons hang from the TOP EDGE — exporting/parameterising it for one caller would have meant threading four unrelated arguments through the level decoration path. The zigzag SHAPE is deliberately identical (same segment/amplitude/thickness model) so a party streamer still reads as the same prop level 21's backdrops use, and a comment at the call site says so. The six ribbons are clustered at the left and right edges to FRAME the banner; a test asserts none of them can overlap its panel.
  - **The bouquet is welded to Gabby's bounce, not merely placed near her.** It hangs off her cast slot's `x`/`groundY` and re-applies `castBounceOffsetPx(now, gabby.phase01, ...)` every frame — the same pure helper and the same phase the cast itself bounces by (ST-1 exposed `phase01` on `PartyCastMemberInfo` for exactly this), so it moves on precisely her frames rather than a frame late or on its own timer.
  - **The TOAST IS PERMANENT, not a timed pop-up.** "Toast" is the plan's word, but PLAN-09 also says "the scene stays alive — no forced exit", and the whole point of this screen is that Gabby can sit in it. A line that congratulates her on her tulips and then vanishes on a timer she didn't ask for is the one thing that would make her feel she had missed something. It sits on its own cream panel directly under the banner and stays for the life of the scene. BOTH halves of the payoff are gated on `tulips > 0` (bouquet AND toast): at zero there is no toast at all, never a "0 tulips" one.
  - **The "Credits ->" button is CREATED at the 4s mark, not created hidden and revealed.** An alpha-0 Phaser Container is still fully interactive, so a pre-built button would have silently routed a stray bottom-right tap to the credits during the first four seconds. It is built by a `time.delayedCall`, fades in over 600ms, and its bottom-right anchor is DERIVED (`DESIGN_WIDTH - marginX - minWidth/2`, `DESIGN_HEIGHT - marginY - UI_MIN_TOUCH_PX/2`) rather than eyeballed. Nothing in the scene ever advances it: the harness sits in the party for ~14s past entry and asserts it is still the active scene with the credits not started.
- **The two things the SCREENSHOTS overruled (both were passing every assertion):**
  - **The balloons were burying the party, so they moved BEHIND the cast.** ST-2 put `balloonDepth` at `DEPTHS.fx + 1`, in front of everybody. The first full-scene render showed 32 balloons at 58x77px drawn over the cast with TWO OF THE FOUR NAME TAGS UNREADABLE — and "all four named characters present with exact names" is a literal PLAN-09 acceptance criterion, so the balloons were losing an argument they were never meant to have. `balloonDepth` is now `DEPTHS.props + 5`: above the unnamed crowd, below the front row and its name tags. Behind the people is also the more natural read (balloons drift around a room; they do not fly at the camera). **WHAT THAT COSTS — this bullet first claimed "NOTHING", which was wrong, and the spec review caught it:** tappability is genuinely unchanged (no cast object is interactive, and click and tap each still pop exactly one), but a balloon drifting through the front row's ~432x144px band is now HIDDEN behind a cast member, so a press there pops a balloon the player cannot see. That band is ~7% of the screen and it is exactly where the eye goes — see the follow-up entry below for the pop-puff depth that fixes the feedback. The ambient confetti stays on `DEPTHS.fx`, the frontmost layer, because small tumbling squares read as depth where large balloons read as occlusion. `tests/partyBalloons.test.ts`'s depth test was REWRITTEN to assert the whole new ladder rather than deleted, and `partyBalloons.ts` + the `balloonDepth`/`nameTagDepth` docs now record why.
  - **The party was happening at the back of an empty room, so the cast moved down.** ST-1 parked `frontRowGroundY` at 500 explicitly "pending ST-3's venue", and at that value the first render had the whole cast crowded into the upper-middle with ~220px of dead floor beneath them. `frontRowGroundY` 500 -> 570 and `crowdGroundY` 400 -> 470 (same 100px front/back separation), with the venue bands moved down to match; the front row now stands in the lower-middle of the frame and still clears the Credits button's face by 34px, which a test pins.
- **Also screenshot-driven: the bouquet had to be made to read as flowers.** A single `tex-tulip` placeholder beside Gabby read as a stray green box, and three stems packed 13px apart merged into one flat green slab indistinguishable from the pale-green BALLOONS drifting past (`PALETTE.grass` is in `BALLOON_TINTS`). It is now three stems fanned wider than half a blossom with the middle one standing tallest — a stepped silhouette no balloon has — over a dark stem bundle she grips. The bundle is `PALETTE.outline`: `PALETTE.brown` was tried first and vanished against the brown-lit patio, and a cream "paper wrap" would vanish against Gabby's default off-white racing suit. A `playtest-out/party-bouquet-closeup.png` clip is now part of the harness's evidence, because `bouquetShown === true` is not the same as "you can SEE her holding it".
- **Why:** All within HOW — no personal content invented or altered. `PARTY_BANNER_TEXT` and `bouquetToastText(n)` are IMPORTED from `src/data/finale.ts` and never re-typed in the scene; the only copy authored in `PartyScene.ts` is the `Credits \u{2192}` button label, which is UI chrome (the LevelCompleteScene precedent for its own button labels) and is written as an escape so a re-encode cannot mangle it. ZERO Matter bodies (the scene never touches `this.matter`), no zoom compensation, every tunable in `constants.ts`'s PARTY block, and every field reset in `create()` with the three handles destroyed on SHUTDOWN.
- **How verified:** `npm run build` clean; `npm run test` **702/702 green** (687 -> 702, +15 layout/venue cases, plus one ST-2 depth test rewritten). New committed harness `node scripts/playtest-party.mjs` exits 0 — it gates the four name tags against INDEPENDENT `String.fromCodePoint` oracles both in the cast data AND as Text objects that really rendered, the banner and toast byte-exactly the same way, the crowd count inside 8-15 with no tags, `>= 20` balloons with a real MOUSE CLICK and a real TOUCH TAP each popping EXACTLY ONE (plus a Zone input-enabled census, since Phaser's `topOnly` decides WHICH overlapping balloon takes the press) and a replacement re-entering below the bottom edge, the bouquet payoff BOTH ways, the Credits button absent at 220ms / faded to alpha 1 by ~4.0s / routing to CreditsScene by click AND tap, the party still alive ~14s in with no auto-advance, 0 Matter bodies, avg 60.9 fps (min 60.8) with the full scene live, and zero console/page errors — with screenshots (with bouquet, without, after a character change, after popping, with the button, plus the bouquet close-up) to the gitignored `playtest-out/`. **THE TWIN JOKE END TO END:** Dallas's texture key === Gabby's === the one-source-of-truth `riderTextureKey` and NOBODY else's (`tex-gabby|blonde|blue|classic`); after saving a different character and re-entering, BOTH became `tex-gabby|pink|green|stealth` together — visible in `party-character-changed.png` as two identical pink-haired guests standing two apart. Neighbouring harnesses re-run and green: `node scripts/playtest-levelcomplete.mjs` (which drives a real level-22 finish INTO this scene — 32.5s, no errors) and `node scripts/playtest-notes-rotation.mjs`.

## 2026-07-22 — PLAN-09 ST-3 spec-review round: the balloon-depth move left a no-feedback press zone, and one depth doc was stale

- **Question:** The spec review upheld both cross-subtask constant changes (the balloon-depth reversal and the ground-Y moves) but found two defects in what shipped with them. Which were real?
- **Findings, in order of how much they would have hurt:**
  - **THE BALLOON MOVE CREATED A NO-FEEDBACK PRESS ZONE, and three files said otherwise.** ST-3 moved the balloons behind the cast (`DEPTHS.props + 5`) AND took their pop puffs down with them (`+ 6`) — so both ended up BELOW the front row (50) and the bouquet (51). A press landing on one of the six front-row figures therefore popped a balloon the player could not see AND hid the puff too: zero visible feedback, over roughly 7% of the screen and precisely where the eye goes. The reviewer reproduced it live — a press on Dallas popped balloon #10, fully hidden behind her, with nothing drawn. Worse, PLAN-10's balloon-pop SFX hooks in at that exact call site, so it would have fired with nothing on screen at all. **Fix: `popConfettiDepth` is now `DEPTHS.fx - 1` (59) — above the balloons AND above the whole cast including `nameTagDepth` (55), just below the ambient rain (60).** The puff is the pop's only guaranteed feedback, so it is the one thing that must never be occluded. Puffs are 14 pieces of 6-12px living 0.5-1.0s, so unlike the balloons they cannot re-create the name-tag occlusion that motivated the move — **verified in a screenshot, not assumed** (`playtest-out/party-pop-over-cast{,-closeup}.png`, captured mid-puff: the burst reads clearly over Andrea's chest while all four name tags stay fully legible).
  - **The "costs the interaction NOTHING" claim was overstated in THREE places** — `PARTY.balloonDepth`'s doc, `partyBalloons.ts`'s module doc, and the ST-3 entry above. All three now state what is actually true: TAPPABILITY is unchanged, but the balloon itself can be hidden behind a cast member and the PUFF is what makes the press legible. That is also why `popConfettiDepth` is documented as load-bearing rather than as "one above the balloons".
  - **A third depth doc was stale and self-contradicting.** `confettiFallDepth` still read "above the whole cast ... but BELOW the balloons and their pop puffs" — describing the pre-ST-3 ladder, and contradicting `PartyScene.ts`'s own comment two files away. ST-3 had correctly rewritten `nameTagDepth`'s and `partyBalloons.ts`'s docs and missed this one. Now states the truth: the rain is the FRONTMOST layer, which is exactly what ST-1's "keep `nameTagDepth` strictly below `DEPTHS.fx`" invariant guarantees.
- **The ladder, final:** crowd 20 < balloons 25 < front row 50 < bouquet 51 < name tags 55 < pop puffs 59 < ambient rain 60 < UI 110. `tests/partyBalloons.test.ts` pins all six links; reverting either the balloons or the puffs fails it.
- **New harness coverage so this cannot silently regress** (`scripts/playtest-party.mjs`): a press aimed at a live balloon whose hit centre lands ON a front-row figure's drawn box, asserting exactly one pop AND that the puff layer draws above both the cast and the name tags. The puff layer is IDENTIFIED BY OBSERVATION, not by importing a constant — it is the one display-list depth bucket that gains visible top-level Rectangles across the press (every other Rectangle in the scene is either static, always-visible ambient rain, or inside a Container and therefore off the display list). The check settles for 1200ms first (> `popConfettiLifetimeMaxMs`) so the previous pops' pieces have expired and the measured delta IS this pop's burst — without that wait it undercounted 13 of 14, and it now also asserts the layer was empty beforehand. Live result, stable across four runs: a press on Andrea / Allison / Dallas / gabby (whoever a balloon drifts behind) pops the hidden balloon and throws **14 puff pieces at depth 59, over the cast (50) and the name tags (55)**.
- **Not changed (reviewer explicitly non-blocking):** a confetti piece can briefly sit on a name tag (the rain is above the tags by design); the bouquet is three flat `PALETTE.grass` boxes and the balloons read as flat pastel cards — both are the `tex-tulip` / `tex-balloon` placeholders, PLAN-10's to replace.
- **How verified:** `npm run build` clean; `npm run test` **702/702 green**; `node scripts/playtest-party.mjs` exit 0 across four consecutive runs (avg 60.9-61.0 fps, zero console/page errors); `node scripts/playtest-levelcomplete.mjs` exit 0 (level-22 finish still flows into the scene). Screenshots eyeballed for the puff-vs-name-tag question specifically.

## 2026-07-22 — PLAN-09 ST-3 code-quality round: a double-start on the last button, three composition calls, and a comment-drift habit worth naming

- **Question:** The code-quality review returned APPROVED_WITH_MINORS with two Important findings, three composition calls and a batch of harness/doc polish. Which mattered, and what did the composition changes actually do to the screen?
- **The two Important findings:**
  - **Two presses on "Credits ->" started CreditsScene TWICE.** The reviewer patched `CreditsScene.create` live and counted: a rapid double-click gives 2 creates, and so does a two-finger tap. Phaser's `SceneManager.start` on a running scene shuts it down first, so this is a RESTART rather than a leak — invisible today against ST-4's stub. But ST-4 builds a line-by-line reveal there, and a two-finger tap on a phone would visibly snap it back to line 1 at the gift's very last beat. `PartyScene` now latches `private leaving` on the first press (reset in `create()`), and the button routes through a `goToCredits()` guard. **Flagged for PLAN-11 QA, deliberately NOT fixed here:** `LevelCompleteScene.ts`'s three buttons have the same un-guarded shape (`onClick: () => this.scene.start(...)`), and so will every button ST-4/ST-6 add. It is a project-wide pattern, and the right place to fix it once is `createPixelButton` itself — out of ST-3's scope, and not worth touching a stable scene for mid-plan.
  - **Two comments written IN THE SAME COMMIT that moved a constant still cited the old value.** Both new venue comments quoted a crowd head-top of `~294` when `crowdGroundY` 400 -> 470 had just made it 364.4. The claims stayed true (the lights bottom out at 306, the toast at ~195) and `tests/finale.test.ts` derives the head top from the REAL cast slots, so nothing was actually broken — but this was the **third review round running** in which a load-bearing comment drifted from the constant it describes. Both fixed, and the whole diff was then grepped for other numbers cited in prose. **The habit is the finding, so name it: in this codebase a comment that quotes a derived number is a claim, and it has to be re-derived whenever the change touches its inputs — or written as "see the test that computes it" instead of as a literal.** Where a number is load-bearing the comment now points at the test that derives it rather than repeating it.
- **The three composition calls (the plan says "spend polish here"):**
  - **The screen's dead centre was a stranger, and now it is a gap.** Crowd slot 4 sat at x=640 — exactly between Gabby (565) and Caleb (715), i.e. the geometric centre of the payoff screen was an unnamed partygoer's head, in the one spot NORTH_STAR §5 reserves for the couple. Fixed by SPLITTING the constant in two: `crowdSlotCount` (9, odd) drives POSITION and `crowdCount` (8) is what actually gets built, with the middle slot skipped. Shifting `crowdCenterX` by a half step instead would have aligned EVERY crowd member with a front-row member and re-created the "crowd heads read as hats" problem ST-1 fixed with screenshots; dropping a SLOT cannot disturb the interleave, because that property (`150(i-j) = 225` has no integer solution, and 75px clears the summed sprite half-widths) holds slot by slot. `buildPartyCastSlots` now carries two indices on purpose — `slot` for x, a compacted `member` for id/look/stagger/scale/phase — so the built crowd stays a contiguous `crowd-0..crowd-7` with no hole in its numbering and no gap in `crowdGuestAppearance`'s colour cycles. 8 is still inside NORTH_STAR §5's 8-15. New tests pin the odd grid, `crowdCount === crowdSlotCount - 1`, and that NO crowd member stands between Gabby and Caleb.
  - **The light pool read as a hard-edged oval rug.** Two nested ellipses at 0.22/0.16 left one crisp curve sweeping the patio. A third, wider ring at alpha 0.08 now sits outside them, so the falloff steps 0.22 -> 0.08 -> 0 instead of 0.22 -> 0. **The pool is NOT floor-only and that is now recorded as deliberate rather than retuned:** its top reaches 370, above `venueGroundY` 450, so ~80px washes up the fence and it is still ~967px wide at the ground line. Light falling on a yard does hit the fence behind it, and the spill is what keeps the fence from reading as a flat cut-out band. The test only ever pinned the pool's CENTRE (genuinely on the floor, between the two rows of feet), which stays true; its comment now says so explicitly instead of implying floor-only.
  - **The bouquet was obscuring the one character this gift is about.** At `-30/-50` and scale 1.6 its centre sat INSIDE her 36px half-width with its blossoms at her chin: a pale-green mass over her whole left half, in the same `PALETTE.grass` as the balloons drifting past. Retuned to `-50/-28` at scale 1.2 (spread 14, lift 8, grip 12x12) — smaller, lower and further out, so it overlaps her silhouette enough to read as HELD while its centre sits outside her sprite and its blossoms clear her face. The test was rewritten from "the bouquet's centre is within her sprite" into the three properties that failure actually violated: it must overlap her, its centre must be outside her, and its top must clear her chin — with the chin derived from `GABBY_BASE_LAYOUT` (the hair+face bands BootScene really draws) rather than from a guessed fraction of the sprite.
- **Harness hardening (all taken):** `poppedWasHiddenBehindCast` is now ASSERTED, not just reported — without that the occluded-pop check could keep passing while quietly ceasing to exercise a hidden pop; the puff census POLLS for ~250ms instead of racing the input pipeline with a single read (both CDP round trips landing inside one 16.7ms frame would have failed the run spuriously); `popOneBalloon` returns early on zero pops instead of following a still-alive balloon and reporting a nonsense "replacement re-entered at y=<mid-screen>"; the scenes-to-stop list is DERIVED from `game.scene.getScenes(false)` rather than hardcoded (it omitted BootScene, and ST-5/ST-6 add entry points); the fps window now pops ~4 balloons/sec right through the sample, so the 168-piece pop pool is loaded rather than dormant (17 pops, still 60.9 fps min 60.9); and a new smoke gate asserts THE VENUE DREW (46 backdrop rectangles + 5 Graphics), which ~180 lines of bands/seams/glows/bulbs/streamers/hangers previously had no automated coverage of at all.
- **Also:** `PartyScene` now DELETES `__party` on SHUTDOWN and exposes `bouquetShown` as a live getter. Half the snapshot is a frozen literal, so leaving it behind let `READ_PARTY` return a plausible-looking party for a scene that is not running — which starts to matter at ST-6, when the Title screen becomes a second way in and a harness may read `__party` before entering. The harness's `sumPops`/`aliveCount` default to `[]` so a poll that samples during a shutdown reads as "nothing there" instead of throwing. And `PartyDebug.balloons`' doc had re-introduced the "alive is equivalent to on screen" overstatement the previous round removed from `partyBalloons.ts`; the zero-area-for-one-frame caveat is restored.
- **DELIBERATELY NOT DONE — `balloonCount` stays 32.** The reviewer suggested 24-28 for visual calm. Rejected for now: 32 is load-bearing in ST-2's worst-case derivation (`32 - ceil(12 x 0.434) = 26 >= 20` visible while a player mashes), so changing it re-opens that proof and its tests for a look that is really the `tex-balloon` placeholder's — flat pastel cards — rather than the count's. **Recorded as a PLAN-10 consideration:** once the balloons are real pixel art with highlights and varied silhouettes, re-judge the density from a screenshot, and if a lower count is genuinely better, re-derive the visible-count bound in the same change.
- **How verified:** `npm run build` clean; `npm run test` **704/704 green** (702 -> 704). `node scripts/playtest-party.mjs` exit 0 — now reporting 8 unnamed partygoers (cast 14), the venue's 46 rects + 5 Graphics, and avg 60.9 fps (min 60.9) *while popping 17 balloons through the sample*. `node scripts/playtest-levelcomplete.mjs` exit 0. Screenshots re-examined: the centre gap reads as clean patio between Gabby and Caleb rather than as a missing person; the bouquet is a small stepped green bunch at her hip with her face completely clear; the pool's edge is a soft two-step falloff instead of one hard curve; and the mid-puff shot still shows the burst reading clearly over Andrea with all four name tags legible.

## 2026-07-22 — PLAN-09 ST-4: the real CreditsScene — reveal pacing, the `×` glyph, the fresh-start confirm, where the heart goes, and one Phaser input race the harness had to learn about

- **Question:** PLAN-09 task 3 fixes WHAT the credits say (the three VERBATIM lines, a divider, `🌷 × N collected`, "Play again?", a "Fresh start" behind a confirm, "a tiny heart somewhere") but leaves ST-4 every HOW: how fast "line by line" should be, what the confirmation says and how it is made safe, where the heart goes, what the dark background actually is, and whether the plan's two non-ASCII characters can be rendered at all.
- **Choices:**
  - **VERBATIM COPY IS IMPORTED, NEVER RE-TYPED.** `CREDITS_LINES` comes from `src/data/finale.ts`; the scene maps over it and never spells a line out. `tests/finale.test.ts` already pinned that constant byte-exactly against an independent code-point oracle, and `scripts/playtest-credits.mjs` now re-checks it against a SECOND, separate oracle on what actually reached the screen. The only copy authored in `CreditsScene.ts` was UI chrome (the button labels and the confirmation's wording). **SUPERSEDED by the two review rounds below: the tulip tally moved to `data/finale.ts` in the spec-review round, and the chrome strings followed it in the code-quality round — `CreditsScene.ts` now authors NO rendered copy at all, so every string on the screen is unit-testable.**
  - **Dark background = `PALETTE.duskIndigo`, not a near-black.** It is level 22's and the party's own night sky (`themes.ts`'s `finalDusk`, `PARTY.venueSkyMidY`'s upper band), so walking out of the party into the credits never changes palette, and the pastel confetti reads far better falling through a deep indigo than through black.
  - **EVERY string sets its colour explicitly, through one helper.** `createPixelText` defaults to `TEXT_COLOR` (plum `#4a2c40`), which is tuned for the pastel-pink menus and is effectively invisible here. The scene routes every string through a private `creditsText()` that applies `CREDITS.textColor` (cream, measured 10.3:1 on duskIndigo) so no call site can forget. A unit test asserts the pair by MEASURED WCAG luminance/contrast rather than by name, and the harness asserts the rendered fill of every credit line is not the default plum.
  - **Reveal pacing: a 400ms beat, then a line every 900ms (each fading over 350ms), then a 600ms beat, then the tail fades in over 400ms.** The buttons exist 3.2s after entry; a tap/click ANYWHERE skips straight to the finished screen (LevelCompleteScene's courtesy to impatient players). "Line by line" is one WHOLE LINE at a time — deliberately NOT LevelComplete's per-character typewriter, which is that card's voice, not this screen's.
  - **THE BUTTONS ARE NOT CREATED UNTIL THE TAIL FADE FINISHES, and that is load-bearing rather than cosmetic.** An alpha-0 Phaser Container is still fully interactive (the hazard `PARTY.creditsButtonDelayMs`'s doc describes) — and here it has a live trigger: the reveal is skippable by a tap ANYWHERE, so a pre-built button sitting under the skip tap would take that same tap's `pointerup` and navigate away the instant the player tried to hurry the credits along. Nothing on this screen is interactive until that fade completes.
  - **"Fresh start" is made safe THREE ways, because it deletes her whole save.** (1) It never fires directly — it opens an in-scene confirmation built from `ui.ts` (never `window.confirm`), reading "Start over? / This clears your levels, your tulips / and the Gabby you made, so the whole / ride begins again at level 1." (2) CANCEL is `Keep my progress` — named for what it PRESERVES, on the LEFT and the WIDER of the two faces (460 vs 360), so the easy out is also the obvious one; CONFIRM is `Erase it all`, named for what it does. (3) While the dialog is open the two buttons underneath are input-DISABLED, not merely covered: the dim is a plain Rectangle with no hit area, so without that a press where "Play again?" sits would sail straight through the panel and navigate away mid-confirmation. The harness proves that exact press does nothing, and that CANCEL leaves every `gabby22.*` key byte-identical — read from RAW localStorage, never from the scene's own state.
  - **The tulip tally is shown at EVERY count, including zero.** The party's bouquet toast CONGRATULATES, so it is gated on `tulips > 0`; this is a factual tally, and hiding it would leave the divider with nothing under it but buttons.
  - **The heart sits directly under "Happy 22nd!!!", above the divider,** so it reads as a signature ON the message rather than as decoration floating in a corner. It is the SAME three-primitive shape `pickup.ts` draws when Caleb hops on in level 12 (two lobes + a point, `PALETTE.coral`), kept as a local copy rather than extracted: that one is a tracked, tweened, self-destroying particle inside a level-event handle and this one is a static 22px signature — they share only six ratio constants.
  - **`scene.start` is guarded by a `leaving` latch on BOTH routes** (the ST-3 discipline). On "Erase it all" an unguarded double-press would also run `resetAll()` twice. The harness instruments `TitleScene.create` with a counter and fires a real rapid double-press at each button: exactly one create, both times.
- **THE `×` AND `🌷` QUESTION, SETTLED BY LOOKING AT PIXELS (the level-2 em-dash precedent, 2026-07-16):** the plan writes the tally as `🌷 × N collected`, and this project's house rule is ASCII-only rendered copy with non-ASCII written as `\u{...}` escapes, precisely because a glyph the font stack lacks renders as tofu. Both characters are written as escapes (`\u{1F337}`, `\u{00D7}`) AND **screenshot-verified in real Chrome at the exact 24px they render at**: the `×` draws as a real multiplication sign out of Press Start 2P itself, and the tulip draws as the system colour emoji (**CORRECTED by the ST-4 spec review, see the entry below: the 24px advance this bullet originally offered as the proof is NOT one** — that font is fixed-width and its own .notdef box advances 24 too, so width only rules out the Courier New fallback at 14.4. The proof is the drawn INK plus the eye), exactly as the party's bouquet toast already does. **No fallback was needed, so the plan's form is kept byte-exact.** The harness now proves both automatically as well as visually: each character is drawn into a 2D canvas and its ink signature compared against a PRIVATE-USE code point (U+E123) that no font can define — whatever the browser draws for THAT is this stack's missing-glyph rendering, so a character whose ink differs from it drew a real glyph. Live: tulip ink 288, `×` ink 135, control 363 — all three distinct.
- **THE ONE THING THAT WASN'T A SCENE BUG, and cost a debugging round:** the first harness run failed four checks with "the button did nothing". Instrumenting the button showed the SCENE receiving `pointerdown` at the button's exact coordinates while the button itself received NOTHING — because Phaser queues a freshly-created interactive Game Object in `InputPlugin._pendingInsertion` and only splices it into the hit-test list `_list` on the NEXT scene pre-update. The buttons here appear in a tween's `onComplete` and `buttonsShown` flips in that same frame, so polling for it alone races a ~16ms window. A human cannot press inside 16ms of a button fading in, but CDP can. Fixed in the HARNESS (`pollAndSettle`, which polls and then settles a few frames before pressing) with the whole explanation written at the helper — not by changing the scene.
- **Why:** all within HOW — no personal content invented or altered, and `NORTH_STAR.md` / `PROGRESS.md` / the plan files untouched. ZERO Matter bodies (the scene never touches `this.matter`), camera zoom 1 with no zoom compensation, every field reset in `create()` and torn down on SHUTDOWN (including deleting the DEV `__credits` snapshot), and every tunable in `constants.ts`'s new `CREDITS` block. The ambient rain reuses the `PARTY.confettiFall*` knobs that block already documents as shared by both finale scenes — no new confetti numbers were invented.
- **How verified:** `npm run build` clean; `npm run test` **723/723 green** (704 -> 723, +19 CreditsScene layout/pacing/legibility cases in `tests/finale.test.ts`), and those 19 were MUTATION-CHECKED — flipping the text colour to plum, collapsing two line Ys, shrinking the primary button and moving a confirm offset fails 5 of them. New committed harness `node scripts/playtest-credits.mjs` exits 0, gating: the three lines byte-exact and centred as three separate top-down Text objects (the last with THREE `!`); a genuinely PARTIAL reveal observed (1/3) then completion at 2834ms, and a tap anywhere skipping 1/3 straight to 3/3; the tally `🌷 × 13 collected` rendered with both glyphs proven real; a tiny heart; 60 confetti pieces still falling; 0 Matter bodies; "Play again?" routing to the Title by BOTH click and tap with all 5 `gabby22.*` keys byte-identical across the hop; "Fresh start" opening a confirm that swallows a press aimed at the button under it, cancel leaving every key untouched AND re-enabling both buttons, confirm leaving 0 keys behind; rapid double-presses starting the Title exactly once each; the full chain party -> "Credits ->" -> credits -> "Play again?" -> Title with no dead ends; and zero console/page errors — with screenshots (full scene, partial reveal, the confirmation, a tight crop of the tulip line) to the gitignored `playtest-out/`. Neighbours re-run and green: `node scripts/playtest-party.mjs` (which routes INTO this scene — avg 60.9 fps, no errors) and `node scripts/playtest-levelcomplete.mjs` (which drives a real level-22 finish into the party, 32.6s, no errors).
- **Screenshots eyeballed, honestly:** cream on deep indigo with pastel confetti drifting behind the words reads warm and calm rather than clinical; the three lines land one at a time with the punchline in its own bigger font and a small coral heart blooming under it; the divider is a soft dimmed rule rather than a box; and the confirmation is unmistakable without sounding like a system dialog. One thing worth naming for PLAN-10: the confirm's cream BUTTON faces sit on the cream PANEL face, so they read as buttons only by their chunky outline and drop shadow — true of any `ui.ts` button on a `ui.ts` panel, legible today, and worth revisiting when the real art lands.

## 2026-07-22 — PLAN-09 ST-4 spec-review round: a glyph argument that proved less than it claimed, a blanket colour rule that was false, and the one new string with no unit-level oracle

- **Question:** The spec review returned ✅ spec compliant with four minor findings, having re-verified everything with its own live probes rather than trusting the harness (code points dumped off the live Text objects; `resetAll()` attacked with 5 rapid clicks, a no-move double-press, then 11 mouse presses / 4 touch taps / 10 keystrokes with the confirm open, all with a falsifiability control). Which findings were real, and what did fixing them change?
- **The four, in order of how much they would have hurt:**
  - **THE ADVANCE-WIDTH ARGUMENT WAS NOT THE PROOF IT WAS WRITTEN AS, and it is the load-bearing one.** ST-4's entry above, `scripts/playtest-credits.mjs`'s header and its `glyphProbe` doc all offered "`×` advances exactly 24px, one font size, so it came from Press Start 2P" as evidence the glyph is real. It is not: that font is FIXED-WIDTH, so its own **.notdef box advances 24 too**. Width only ever ruled out a fallback font (Courier New would advance 14.4) — a tofu box would have sailed through it. What actually proves the glyph is the DRAWN INK, and it does: `×` lights 135 px in a bounding box of 17x15 against the missing-glyph control's 363 px at 20x21, measured against several unassigned/private-use controls, plus a tight crop read by eye. The harness already ran that ink comparison and it was always the real evidence — the writing just credited the wrong signal. Every statement of it is now corrected in place and the two claims are explicitly separated ("INK is the proof; ADVANCE is a weaker, different claim"), including the wrong sentence in the entry above, because leaving a false claim standing is the comment-drift habit this project named in the ST-3 round.
  - **"EVERY string on this screen sets `CREDITS.textColor`" was simply false.** Both `CreditsScene.ts`'s module doc and `CREDITS.textColor`'s own doc said it, and added that the `creditsText()` helper meant "no call site can forget". Live, SIX strings render in pixelText's default plum (the count in this bullet originally said four, and the code-quality review corrected it): the two bottom button labels AND the confirmation's two button labels, all four inside `ui.ts`'s cream faces, plus the confirmation's title and body on its cream panel — all of which is CORRECT, and a comment at the call site already said so. But a blanket rule that contradicts the code invites a future editor to "fix" the panel text into **cream on cream**, which would be the actual bug. Both docs now state the two-way rule: `dark field -> creditsText()`, `cream panel/button face -> createPixelText()` with its default.
  - **THE ONE NEW STRING HAD NO UNIT-LEVEL ORACLE — and it was the one carrying both risky code points.** `CREDITS_LINES`, `PARTY_BANNER_TEXT` and `bouquetToastText` are each pinned in `tests/finale.test.ts` against an independent `String.fromCodePoint` oracle, but the tulip tally was a private function inside `CreditsScene.ts`, guarded only by the browser harness: swapping `\u{00D7}` for an ASCII `x`, or changing the spacing, left `npm run test` green. It has moved to `src/data/finale.ts` as `tulipTallyText` — that module's stated purpose is being the one home for this plan's rendered copy — with five new oracle tests. Mutation-verified: the ASCII-`x` swap now fails 3 of them. Its doc records what it is NOT, too: unlike the names/banner/toast/credit lines it is plan-specified rendered copy rather than locked personal content, so a documented screenshot-driven ASCII fallback would have been legitimate had the glyph not rendered.
  - **The double-press check was narrower than its comment claimed.** `doublePressDesign` drove ONE mouse pointer twice, while the comment sold it as "the rapid double-click / two-finger-tap". Rather than just correcting the words, the coverage was widened to match: the mouse half is now `doubleClickDesign` (on "Play again?") and a new `twoFingerTapDesign` drives TWO simultaneous CDP touch points at the destructive "Erase it all" — the literal phone case the `leaving` latch was added for. Two supports make it real rather than theatre: `ensureMultiTouch` tops the game up to 3 pointers (Phaser defaults to ONE touch pointer; `systems/pedals.ts` does the same top-up the first time GameScene runs, so a real player genuinely arrives at the credits with the budget), and a falsifiability control asserts the tap arrived as exactly **2 pointer-downs**. Mutation-verified end to end: deleting the `leaving` guard in `freshStart()` makes the run fail with "TitleScene.create ran 2 times (want 1)".
- **Also fixed, and it was mine to flag:** `scripts/playtest-party.mjs`'s `checkCreditsRoute` polled `creditsShown` and pressed on that same frame — the identical `_pendingInsertion` hit-test race diagnosed while building the credits harness (a freshly created interactive object only enters Phaser's `_list` on the next scene pre-update, so a press inside that ~16ms window reaches the scene but never the button). It had never flaked in a run here, but it is PLAN-09's primary acceptance gate and a spurious failure there would be expensive to debug later. It now settles, with the whole explanation at the function. Scope was explicitly widened to that harness file only; `PartyScene.ts` is untouched.
- **And one habit finding, worth naming again:** `CreditsScene.ts`'s module doc and four `CREDITS` doc comments had hand-typed COPIES of the credit lines in prose. Nothing rendered from them, but they are exactly the kind of duplicate that drifts silently from `data/finale.ts` — and "never re-typed" was an explicit instruction for this particular copy, where drift is worst. All of them now REFER to `CREDITS_LINES` ("the LAST credit line", "the longest of them") instead of quoting it; `grep 'Happy 22nd\|Created by Caleb\|Created for Gabriella' src/systems/constants.ts src/scenes/CreditsScene.ts` is now empty.
- **How verified:** `npm run build` clean; `npm run test` **728/728 green** (723 -> 728, +5 `tulipTallyText` oracle cases), with both new mutation checks reproduced (the ASCII-`x` swap fails 3; removing the `leaving` latch fails the two-finger check). `node scripts/playtest-credits.mjs` exit 0, now reporting the ink figures as the proof and the advance as the secondary claim, plus "a rapid double-CLICK on 'Play again?' and a genuine TWO-FINGER TAP on 'Erase it all' (2 pointer-downs, 3 pointers available) each started the Title exactly 1/1 times". `node scripts/playtest-party.mjs` exit 0 after the settle (avg 61 fps, min 60.9, zero errors).

## 2026-07-22 — PLAN-09 ST-4 code-quality round: a release-only activation hole in the SHARED button kit, and three more unfalsifiable premises

- **Question:** The code-quality review returned CHANGES_REQUIRED for one issue plus eleven minors, having independently re-run the previous round's mutations (the `×`->`x` swap fails 3 tests; `twoFingerTapPointerDowns: 2`), proved teardown clean at 68 display objects across three entries including leaving with the confirm open, confirmed `__credits`/`__party` are absent from the prod bundle, and checked a synthetic 100-second frame degrades the reveal to the skip outcome rather than double-firing. What was the critical issue, and where did it actually live?
- **THE CRITICAL ONE — AND IT WAS NEVER CreditsScene's BUG.** `createPixelButton` fired `onClick` on `pointerup` **without requiring a prior `pointerdown` on that button**, so EVERY button in this game could be activated by pressing somewhere else and releasing over it. On the credits that is not theoretical: the two buttons materialise ~3.2s in, in the lower-centre of the screen — exactly where a phone thumb rests — during the window when the player is most likely to press and hold BECAUSE THEY WANT IT TO HURRY UP. Reproduced live at the threshold: a 300ms hold did nothing, a **420ms hold (past `CREDITS.tailFadeMs` = 400) both skipped the reveal and fired "Play again?"**, and a 700ms touch-hold opened the fresh-start confirmation. `CreditsScene` has ONE entry point (the party's "Credits ->", itself only reachable by finishing level 22), so being ejected to the Title means replaying level 22 to see the credits again — nothing is destroyed, but the recipient loses the screen the whole gift exists to deliver.
  - **Fixed at the root in `src/systems/ui.ts`**, so every button in the game is immune, not just these two. `createPixelButton` now keeps a `pressedPointers` set: `pointerdown` adds the pointer, `pointerout` drops it (dragging off then back on must not re-arm without a fresh press), and `pointerup` only calls `onClick` if that same pointer is in it.
  - **KEYED PER POINTER, not a single boolean, and that choice is load-bearing.** A boolean would ALSO collapse a genuine two-finger tap into one `onClick` — which sounds like a bonus until you notice it silently disarms the scenes' own double-press guards (`PartyScene`/`CreditsScene`'s `leaving` latch) and makes the harness checks that prove them unfalsifiable. Per-pointer keeps multi-touch semantics exactly as they were, so those guards stay real. `pointer.id` is the same idiom `partyBalloons.ts`'s press dedupe already uses.
  - **`CREDITS.tailFadeMs`'s doc was claiming this could not happen.** It said late button construction meant "nothing interactive exists until this fade completes". Late construction only ever BOUNDED the window at ~400ms; it never closed it. The doc now states what each mechanism actually buys: the ui.ts latch is the safety mechanism, and late construction is a composition choice (nothing competes with the three lines while they land) plus belt-and-braces against the alpha-0-container hazard.
  - **New harness coverage, mutation-verified:** `scripts/playtest-credits.mjs` now presses on empty space and HOLDS for 300 / 420 / 700ms before releasing over "Play again?", plus a 700ms CDP touch-hold over "Erase it all" — each must skip the reveal and activate NOTHING, and each also asserts the reveal DID skip so the check cannot pass by simply missing. Reverting the latch reproduces the original report exactly: 300ms clean, 420ms and 700ms navigate, touch-hold opens the confirmation.
- **THE MINOR THAT WAS THE SAME BUG AS LAST ROUND'S:** `tests/finale.test.ts`'s "fits the widest confirm body line inside the panel" asserted `40 * confirmBodyFontSizePx <= usable` — a bound on an imaginary 40-char line. `CONFIRM_BODY_LINES` was private to the scene and never entered the assertion, so rewording a body line to 60 chars failed nothing, and the block's whole `BUTTON MODEL` header ("every minWidth exceeds its label's natural width, so minWidth IS the face width") rested on the same unasserted prose. Structurally identical to the `tulipTallyText` finding fixed one round earlier — and the same fix: the five chrome strings moved to `src/data/finale.ts` (the only import-safe home; `CreditsScene.ts` has a runtime Phaser import, so nothing in it can be reached from a plain-Node Vitest suite), and the tests now measure the REAL strings. Mutation-verified: lengthening one body line and one button label fails 2 tests. **`CreditsScene.ts` now authors NO rendered copy at all** — every string on that screen is unit-testable, and `data/finale.ts` documents its three tiers explicitly (locked personal content / plan-quoted copy / UI chrome) so nobody treats a button label like a birthday message or vice versa.
- **The two composition calls (both screenshot-driven, both taken):**
  - **The confirm panel was slicing the "Play again?" label in half.** The panel spans y 180..540 and that button's face 492..580, so the bottom row of its glyphs poked out under the border as plum fragments — on the game's most consequential dialog that reads as clipped text, not a dimmed button. Both bottom buttons are now `setVisible(false)` alongside the existing `disableInteractive()`, restored together on cancel. It also makes the disable VISIBLE, which is the honest UX: nothing under a modal should look pressable.
  - **The two bottom buttons read as peers.** The old doc claimed "being far wider than the secondary IS the hierarchy"; with identical cream faces, outlines, plum labels and font size, 480 vs 340 simply did not carry it — both looked like *the* button, leaving the confirmation to do all the safety work. Fixed with the cheapest honest levers that do not touch the shared kit: `freshStartButtonAlpha` 0.8 (dims face, outline, shadow and label together; deliberately well above ui.ts's 0.55 `disabled` look, because this is a real option) and `freshStartButtonY` 648 -> 660, widening the gap between the faces from 24px to 36px. The hierarchy test now asserts all three axes — narrower, lower, dimmer.
- **Five documentation drifts, all real, several introduced by my own previous commits:** the colour rule enumerated FOUR cream-surface strings when there are SIX (the confirmation's own two button labels are the same case) — corrected in `constants.ts`, `CreditsScene.ts` and the DECISIONS bullet above; "matching LEVEL_COMPLETE's row gap" was false (CREDITS was 112, LEVEL_COMPLETE is 116 — and the LEVEL_COMPLETE block's own comment said "here 112", also wrong, now fixed too); "written as `\u{...}` escapes IN THE SCENE" was stale the moment the tally moved to `data/finale.ts` in the previous commit; the CREDITS block header still listed only `CREDITS_LINES` as living there; and `heartSizePx` claimed "smaller than the smallest text on the screen" when the confirmation's 16px body is smaller than 22 (it is the smallest on the credits FIELD). **This is the fourth review round running to find a comment quoting a number or a set that its own change had just moved.** The rule this project already wrote down applies again: a comment that quotes a derived number or enumerates a set is a CLAIM, and it must be re-derived whenever the change touches its inputs, or replaced by a pointer to the test that computes it.
- **Harness symmetry and nits (all taken):** `playtest-credits.mjs` now holds the credits to the same 55fps floor `playtest-party.mjs` holds the party to (they run the same 60-piece rain) — measured avg 60.8, min 60.7; the confirmation's own heading, three body lines and both button labels are now asserted to have RENDERED rather than inferred from `confirmShowing`, with a note that an "OK/Cancel" pair on the one button that deletes everything is exactly the dialog a tired thumb gets wrong (a unit test pins that too); the unused `letterX` glyph probe is gone; and the two-finger pointer-down count now settles before it is read, like every other press site in the file. Naming collapsed to ONE name for one string: `tulipTallyText` as the builder, the scene field and the DEV seam.
- **How verified:** `npm run build` clean; `npm run test` **731/731 green** (728 -> 731), with three independent mutations reproduced (the ui.ts latch removal fails the hold checks; a longer body line and a longer button label fail the geometry premises; the earlier `×`->`x` swap still fails the oracle). **FULL committed harness sweep, because `ui.ts` is shared by every menu scene** — `playtest-drive`, `-touch`, `-pause`, `-orientation`, `-character`, `-tulips`, `-fail`, `-levelcomplete`, `-notes-rotation`, `-level07`, `-level12`, `-level15`, `-level11`, `-level18`, `-party`, `-credits`, `playtest-tricks` in all three modes, and the 22-level `playtest-levels` sweep: ALL exit 0, and no harness needed relaxing. (`playtest-levels` prints its two long-standing ungated warnings about level 7 not finishing gas-only and crash-restarting mid-drive — expected and pre-existing: level 7 is the invisible-cars level, the one level the game deliberately does not clear on gas alone.) Live re-probe of the original report: a 700ms hold over "Play again?" now leaves the player exactly where they were, with the reveal skipped and nothing else changed.

## 2026-07-22 — PLAN-09 ST-5: the level-22 arrival — where the ride-in starts, how she is slowed, and the one staging the bike rig would not allow

- **Question:** PLAN-09 task 1 fixes WHAT the finale is ("bike auto-rides to the venue (short scripted ride-in), Gabby & Caleb hop off, doors open with light spilling out -> PartyScene") but leaves ST-5 every HOW: where the venue stands, when the cutscene takes control, how a bike that GameScene has already frozen ends up parked in front of the doors, how long the hand-off is held, and — the one that turned out to be load-bearing — how you show two people getting off a bike whose rider sprite you are not allowed to touch.
- **Choices:**
  - **EVERYTHING IS PLACED RELATIVE TO THE FINISH FLAG, because the beat straddles it.** `arrivalGeometry(finishX, …)` derives `rideInX` (900px before the flag, where the pedals are taken), `crawlX` (360px before, where she starts slowing) and `doorX` (300px past, where the venue stands). The event carries the two PLACEMENT leads as optional overrides that `level22.ts` authors explicitly; `crawlLeadPx` stays a system constant because it is a property of the bike's braking distance, not of a level. `tests/arrival.test.ts` pins the `rideInX < crawlX < finishX < doorX` ordering at the shipped constants on the shipped level, and pins the venue inside the `LEVEL.finishMarginPx` runway every level has past its flag.
  - **THE RIDE-IN IS THE PEDALS, NOT A FORCE** (`ctx.setInputOverride`, the level-12 pickup's mechanism). That is what makes the arrival unconditional: the override WINS over player input, so a player flat out and a player leaning on the brake get the identical ride in. The harness proves the second case rather than assuming it — it brakes to a genuine standstill 700px short of the takeover, rolls into it, then holds the BRAKE for the entire rest of the run and asserts the bike never comes to rest again (measured floor: 1.5 px/step) and still reaches the party.
  - **A BANG-BANG CRAWL, not a scripted stop — because GameScene freezes the pedals the instant she crosses the flag.** Past `finishX` the run has ended and both pedals are forced false, so she free-coasts and nothing can steer where she stops. The only lever left is the speed she crosses AT, so the last 360px are driven by a two-line controller: brake while faster than `crawlSpeedPxPerStep` (1.8), gas otherwise. It has two properties that matter more than its simplicity — it ALWAYS reaches the flag (a stopped or backwards-rolling bike gets gas), and it can never reverse-creep her back OFF it (brake is only ever held above a positive target, never near the bike's `reverseEngageThreshold`). Browser-measured: she arrives at ~9.6 px/step, crosses at ~1.5-2.4, and rolls to rest at x≈15550-15580 — about 230px short of the doors, which is exactly the composition wanted. It also takes SIGNED `velocityX`, not `BikeHandle.speed`: a magnitude would read a fast reverse as "too fast" and brake her further from the flag.
  - **THE DOORS OPEN BEFORE THE FLAG, ON PURPOSE.** They start swinging when the crawl does, so the venue opens up AHEAD of her while the run is still live and the slow-down reads as pulling up to a house with its doors thrown open, rather than as the game quietly taking the controls away. The harness gates that ordering (`doorsOpenBeforeFinish`), not merely that they opened.
  - **ONLY CALEB'S DISMOUNT IS SHOWN, AND THE LIGHT COVERS THE REST — because the alternative was two visible Gabbys.** `BikeHandle` deliberately does not expose its rider sprite (PLAN-04) and `bike.ts` is a byte-unchanged project invariant, so there is no sanctioned way to take Gabby OFF the bike; a standing Gabby beside a seated one is the single thing this screen must never ship. So the beat is staged the other way round: `PassengerHandle` gained a minimal additive `hide()` (the exact inverse of `activate()`, cosmetic only — "Caleb is picked up" is save-DERIVED, never this flag), pillion Caleb hides, a standing brown-haired Caleb hops down off the back and walks into the doorway, Gabby stays with the bike she is parking, and the doors' warm light wash covers the transition into PartyScene — where the two of them are standing together anyway. The harness asserts a recursive display-list census of EXACTLY one Gabby and one Caleb at every sample of the whole run.
  - **He starts at the pillion's exact position and depth, which is not cosmetic detail.** The first attempt spawned him level with the chassis at `DEPTHS.rider + 1` — screenshot-caught, he covered Gabby completely on the frame he appeared, so the "hop off" read as GABBY VANISHING. He now appears at the pillion's own chassis-local offset and `PASSENGER.depth` (behind her), making the swap invisible and the tween a real hop off the back. His walk-in fade got its own steeper ease for the same reason: a linear fade ghosted him out halfway down the road instead of at the door.
  - **The hand-off is a match on colour, not a flash.** The warm wash (`PALETTE.sunshine`) fades up, then a second wash in `PALETTE.duskIndigo` — PartyScene's own night sky and level 22's `finalDusk` upper band — fades over it, and the hold settles there for a breath before the cut. `finaleHoldMs` (2800) is asserted to exceed the last tween's end rather than having the arithmetic restated in a comment; the first value (2600) left an 80ms window that the harness raced.
  - **The light pool is THREE nested ellipses, sunk below the road line.** One flat ellipse at a single alpha read as a tan RUG lying on the road with a hard edge — the same thing PartyScene's floor pool records being caught on, twice — and centred on the surface its upper half floated above the road and read as a lit MOUND. Nested rings give the falloff; sinking the centre keeps the light on the ground. It also sits at `DEPTHS.terrain + 5`, BELOW every prop, so the finish flag, the venue and the bike all stand IN the light instead of under a shape painted over them.
  - **`playtest-levels.mjs`'s `EVENT_LEVEL_IDS` did NOT gain level 22, and that is deliberate.** That list is the warn-only bucket for levels whose real events could legitimately need more than gas; it is no longer a mirror of `REQUIRED_EVENTS`, and its doc now says so. The arrival drives the pedals itself and can never fail the player, so level 22 must stay under the STRICT gate — if it ever stops delivering a gas-only player to the party, that sweep has to fail, not warn.
- **Why:** all within HOW — no personal content invented or altered, `NORTH_STAR.md` / `PROGRESS.md` / the plan files untouched, `bike.ts` still byte-unchanged, and `PartyScene.ts` / `CreditsScene.ts` not touched at all. ZERO Matter bodies (the module never references `scene.matter`), which level 22 has no room for whatsoever: it is the longest level in the game and sits at the hard maximum of 99/100. Every finale beat is self-driving via tweens + `scene.time.delayedCall`, because GameScene stops calling `handle.update()` the instant the run ends.
- **How verified:** `npm run build` clean; `npm run test` **766/766 green** (731 -> 766: a new `tests/arrival.test.ts` for the geometry/crawl/phase-machine/pacing invariants, new `createPassenger` lifecycle cases in `tests/passenger.test.ts` covering `hide()`, plus the `validateLevels` and dispatch-seam additions). New committed harness `node scripts/playtest-arrival.mjs` exits 0, gating all of the above plus a mid-finale RESTART (the one lifecycle the two full drives never exercise — tweens in flight, timed events still queued) coming back up as a fresh `approaching` run with 0 errors. Neighbours re-run and green: the 22-level `playtest-levels.mjs` sweep (level 22 finishes gas-only in 36.8s with 0 restarts, max bodies 99, 0 errors; its two long-standing level-7 warnings are ungated and expected as always), `playtest-levelcomplete.mjs` (which drives a real level-22 finish into the party — 36.6s, `completed[21]` marked), `playtest-party.mjs` (avg 61 fps, 0 errors) and `playtest-level12.mjs` (the other `setInputOverride` consumer).
- **Screenshots eyeballed, honestly:** the frame that sells it is the crawl — the bike rolling the last few metres through a warm pool of light toward a house with its doors thrown open, warm windows, a strand of bulbs along the roof, and the finish flag standing lit in the middle of it. It genuinely reads as arriving somewhere rather than as a level ending. The dismount is legible at placeholder fidelity (Caleb behind Gabby, then beside the bike, then at the door), and the palette holds all the way through the wash into the party's own dusk sky. The honest weakness is the venue itself: it is a brown box with two square windows, and it will only really become a party venue when PLAN-10's art lands.

## 2026-07-22 — PLAN-09 ST-5 follow-up (coordinator call): Gabby gets off the bike too, and `bike.ts` stops being byte-unchanged

- **Question:** ST-5 shipped only CALEB's dismount on screen and covered Gabby's with the doors' light wash, because `BikeHandle` does not expose its rider sprite and `src/systems/bike.ts` had been byte-unchanged since PLAN-02. I flagged that as a partial match to PLAN-09 task 1's "Gabby & Caleb hop off". The coordinator overruled the staging: getting off the bike together, at her own party, is the emotional payload the whole 22-level ride builds toward (NORTH_STAR §5 "Gabby & Caleb arrive"), and the byte-unchanged lore was never meant to forbid a cosmetic seam. So: what is the smallest change to `bike.ts` that closes it, and what does it cost?
- **WHAT THE INVARIANT WAS ACTUALLY PROTECTING — and why this doesn't threaten it.** `bike.ts` is the most feel-critical module in the game, and everything in it was browser-MEASURED, not reasoned: the gas spin-up that had to outrun frictionAir, the trick-input/pitch-authority mechanic that makes flips deliberate but accidental flips impossible, the anti-endo braking (weak front brake + grounded stability damping) that stopped full-brake stops vaulting the bike onto its nose. "Byte-unchanged" was the cheap proxy for "nobody has quietly retuned the feel". The change made here is **36 inserted lines, 0 deleted, 0 modified**: a `setRiderVisible(visible: boolean)` method on `BikeHandle`, its four-line implementation, its entry in the returned object, and their doc comments. No control law, tuning constant, body, constraint, geometry, `update()` ordering or `destroy()` step was touched, and the only callers in the entire repo are level 22's arrival and its own teardown (`grep -rn setRiderVisible src` — two call sites, both in `arrival.ts`). The proxy is retired; the property it stood for is now evidenced directly, by re-running every feel harness (below).
- **Choices:**
  - **The seam is `setRiderVisible`, not "expose the sprite".** Handing out the `Phaser.Image` would let any caller move, retexture or destroy it; a boolean setter can only do the one cosmetic thing the cutscene needs. It is also a documented no-op after `destroy()`, so no teardown ORDER can make it unsafe.
  - **The alternative was worse, not merely uglier.** The only other way in was to find the rider on the scene's display list by texture key — which would have depended on the recolor cache key format, on nothing else in the scene sharing that texture (the standing Gabby does), and on the seated sprite still being a top-level display-list member. One additive setter is less code and far less to break.
  - **Standing Gabby comes from the ONE source of truth,** `buildCharacterTextures(scene, getSave().loadCharacter() ?? defaultCharacter())` — the same call GameScene builds the SEATED rider with and that `CharacterCreationScene`'s preview and `partyCast.ts` use, resolved in `create()` and never at module scope. So she is the character the player actually made; the harness asserts her texture is a real `tex-gabby|<hair>|<eyes>|<outfit>` variant rather than some second sprite.
  - **The swap is one frame.** The seated rider is hidden inside the same function call that creates the standing one, at the seated sprite's own chassis-local offset and depth. The harness's census checks 1 Gabby / 1 Caleb over the whole run AND specifically on the frame she steps off — and it was MUTATION-CHECKED: commenting out the single `setRiderVisible(false)` fails three assertions, including "2 Gabbys were visible at once".
  - **THEY WALK IN TOGETHER, WHICH TOOK A SECOND PASS.** The first attempt chained each walk onto its own landing; because Caleb dismounts first he then LED Gabby all the way across, and the screenshot showed the two of them straggling rather than arriving together. The walk is now ONE shared timed beat both figures start on, and they land the same distance from the door (34px apart, stopping 34px apart), so their walks are identical in length and they cross the forecourt side by side with Gabby half a step ahead — it is her party. A second screenshot pass moved Caleb's landing a full sprite-width clear of the seated rider's spot, because that is exactly where Gabby materialises a beat later and the two were overlapping on the frame she appeared.
  - **Nothing about the dismount is a second guess at where the bike stops.** Both landings are anchored to `bikeRestX()` — the bike's resting x MEASURED once at the first dismount and reused, so the ~0.4s stagger between the two cannot stretch the gap between them as the bike creeps to a halt.
  - **The restart path is proven, not assumed.** Every `create()` builds a fresh bike whose rider starts visible, so a fail-restart cannot inherit a hidden rider — but `destroy()` restores it anyway (hand the seam back the way you found it), and the harness now takes its mid-finale restart *while the seated rider is hidden* and asserts the fresh run shows exactly 1 Gabby / 1 Caleb. An invisible rider would be unplayable, so this is gated rather than reasoned about.
- **Pacing changed with the staging:** Caleb off at 300ms, Gabby off at 700 (after he has landed), both walk from 1120 for 950ms, the warm wash starts at 2150 once they are inside, and the hold is 3450ms. `tests/arrival.test.ts` pins the ordering rules (pillion first, nobody walks mid-hop, both inside before the wash, hold outlasts the last tween) rather than restating the arithmetic in comments.
- **PLAN-10 item, recorded rather than gold-plated:** the venue is a brown box with two square windows, a lit doorway, two swinging panels and a strand of roof bulbs. It reads correctly and matches PartyScene's palette, but it is the weakest thing on screen in the arrival and is squarely PLAN-10's to replace with real pixel art (as is the audio: the door/music hook is marked with a TODO at `openVenue`).
- **How verified:** `npm run build` clean; `npm run test` **768/768 green**. `git diff --stat src/systems/bike.ts` = **1 file changed, 36 insertions(+)** — insertions only. THE FEEL RE-PROOF, because `bike.ts` was touched: `playtest-tricks.mjs flip` (3/3 landed >=360 clean off level 2's kicker, each firing the byte-exact toast and persisting a tulip, 0 -> 3 across a reload), `gasonly` (finished 17.5s, 0 crashes, worst airborne angle 2 deg, cleared the kicker upright), `brake` (flat grounded pitch 3 deg, rolling descents 22 deg, no endo, no crash), `playtest-drive.mjs` (finished 17.3s, 57 bodies stable across 3 restarts and a real fail, button restart 62ms, avg 60.9 fps) and the full 22-level `playtest-levels.mjs` sweep (17/17 gated levels finish gas-only with 0 restarts, level 22 in 37.4s at 99 bodies, max bodies seen 99, 0 errors; the two long-standing level-7 warnings are ungated and expected) — all exit 0, none needed relaxing. Then `playtest-arrival.mjs` (Caleb off first and Gabby 389ms later as `tex-gabby|blonde|blue|classic`, both anchored to the measured rest x 15530 and walking 251/241px in together; 1 Gabby / 1 Caleb throughout INCLUDING the frame she steps off; the mid-dismount restart put the seated rider back), `playtest-levelcomplete.mjs` (real level-22 finish into the party, 37.5s, `completed[21]`), `playtest-party.mjs` (avg 60.9 fps, 0 errors) and `playtest-level12.mjs`.
- **Screenshots eyeballed, honestly:** the beat now lands. She rolls up through the warm pool of light, Caleb swings off the back and stands clear, Gabby steps off her own bike a beat later, and the two of them walk into the lit doorway side by side while the empty bike sits parked behind them in the light — then the wash. The parked, rider-less bike in the middle of the frame is what sells it: they really did get off and go inside.

## 2026-07-22 — PLAN-09 ST-5 spec-review round: no defects, three doc drifts — and naming the failure mode this plan kept hitting

- **Question:** The spec review returned NO functional defects, having verified all four of its questions independently and live rather than from the report: the `bike.ts` seam is additive-cosmetic-only (`--numstat` = `36 0`, and — a check I had not thought to make — `grep setVisible src/systems/bike.ts` returns exactly ONE hit, so `syncSprites()` cannot fight the hide, plus `setRiderVisible`'s `destroyed` early-return is genuinely safe because `destroyed = true` is the FIRST statement of `destroy()`); the never-fail guarantee holds under adversarial input (stop dead then hold brake all the way in; chaotic gas/brake; **crashing on the exact frame the override engages** -> friendly fail and a restart that comes up `approaching` / `override: null` / seated rider VISIBLE; **quitting to the Title mid-dismount** -> `__arrival` deleted, 0 children, 0 tweens, and PartyScene still not active 4s later, so the hold's `delayedCall` cannot resurrect a dead transition); the one-Gabby-one-Caleb property is invariant by construction (both swaps are synchronous single statements, zero-frame gap) and its census is not vacuous (the reviewer's own probe reads `{gabby:1, caleb:1, seated:true}` before the dismount). So what was left was documentation — and it was the same class of finding for the FIFTH round running.
- **THE FAILURE MODE, NAMED, so PLAN-10/11 inherit it instead of rediscovering it:** *a comment that RESTATES a derived number, or ENUMERATES a set, is a claim with no owner — it goes stale the moment the change that moves the value lands, and nothing fails.* Five rounds on this plan have each found at least one. The rule this project now applies: **derive it, point at the test that computes it, or state the RULE instead of its arithmetic result.** Concretely, this round:
  - `arrival.ts` claimed the equal landing/door gaps make both walks "exactly `doorX - restX - 56` px long". The shipped constants give 82, not 56 (live: both walks 192px, not the 218 implied) — stale from before the screenshot pass moved the landings. The EQUALITY, which is the load-bearing half, was correct and confirmed live. **Fixed by making it testable rather than by correcting the number:** the four offsets moved into an exported `ARRIVAL_DISMOUNT_OFFSETS`, the comment now states the RULE (equal sums == equal distances) with no arithmetic result in it, and four new cases in `tests/arrival.test.ts` pin the equality, the lead order and the signs. A future retune that breaks the pairing now fails a test instead of quietly desynchronising the walk.
  - **`src/levels/events.ts`'s dispatch-switch comment still described the pre-seam staging** ("onFinish() plays the hop-off (ctx.passenger.hide() + a standing Caleb walking inside)") — omitting Gabby's dismount entirely, which is the whole point of the follow-up commit AND the sole justification for retiring the `bike.ts` invariant. That switch case is the DOCUMENTED ENTRY POINT to the system, the first thing a reader hits; `constants.ts` and `GameScene.ts` were updated and this one was missed because the commit touched 7 files and `events.ts` was not among them. **Lesson within the lesson: when a change alters WHAT a system does, grep for every place that DESCRIBES it, not just the files the change happens to touch.**
  - **`src/systems/tricks.ts` still asserted "bike.ts stays byte-unchanged"** — contextually it meant "tricks.ts needed no bike.ts change" (still true), but it had become the only statement of that project-wide invariant in `src/` outside `bike.ts`'s own doc, and this work deliberately retired it. Reworded to say what it actually means and to point at `setRiderVisible`'s doc.
- **Two precision fixes, both mine:**
  - The dismount anchor was called "the bike's RESTING x" and named `bikeRestX`. It is not one: it is sampled at `ARRIVAL.hopOffDelayMs` while the bike is still creeping, browser-measured ~35px short of where it finally settles. Sampling ONCE is still the right trade (reading twice would stretch the gap between the two figures and desynchronise their walk), so the behaviour stands — but the name and doc now say what it is: `dismountAnchor()` / `ARRIVAL_DISMOUNT_OFFSETS`, with the early-sample and its ~35px consequence written down. The visible consequence is cosmetic and acceptable: at the swap frame Gabby materialises about one sprite width from standing Caleb — touching, not overlapping — which `CALEB_LANDING_X_OFFSET_PX`'s comment now states accurately instead of implying more clearance than there is.
  - `ARRIVAL.rideInLeadPx`'s "~2s at gas-only cruise" (repeated in `level22.ts`) was wrong: 900px at the measured 9.5 px/step x 60Hz is ~1.6s. Both corrected, and both now cite the measured cruise they are derived from.
- **HEADROOM, recorded because the next person to touch this needs it:** level 22 now finishes in **~37.2-37.5s**, inside NORTH_STAR §4's 20-45s window but with materially less room than the ~32.5s it ran before ST-5. The two costs are the 360px crawl at 1.8 px/step (~3.3s, since it replaces cruising that stretch at ~9.5) and the 3450ms finale hold. Anyone lengthening `crawlLeadPx`, lowering `crawlSpeedPxPerStep`, or growing the hold should budget against the ~7.5s that remains, not against the old figure.
- **PLAN-10 items, recorded rather than gold-plated:** the arrival venue is a brown box with two square windows, a lit doorway, two swinging panels and a roof strand of bulbs — it reads correctly and matches PartyScene's palette, but it is the weakest thing on screen in the whole beat and wants real pixel art. The door-creak / music-swell hook is marked with a TODO at `openVenue`.
- **How verified:** `npm run build` clean; `npm run test` **772/772 green** (768 -> 772, the four new `ARRIVAL_DISMOUNT_OFFSETS` cases). `node scripts/playtest-arrival.mjs` exit 0 on the final code. Changes were documentation plus one rename and one const-grouping — no behaviour touched — so the full sweep was not re-run (it had passed on the identical logic one commit earlier: 17/17 gated levels gas-only, level 22 37.4s at 99 bodies, 0 errors).

## 2026-07-22 — PLAN-09 ST-5 code-quality round: a never-fail premise that was false, a materialising Caleb, the third replica of one figure, and five staging fixes

- **Question:** The code-quality review returned APPROVED_WITH_MINORS, having verified live: the walks measured 178px each and exactly equal, 34px apart the whole way; it called the `arrivalCrawlPedals` signed-velocity signature "the single best decision in the change" and the `ARRIVAL_DISMOUNT_OFFSETS` fix "the model fix" for closing a claim by making it testable. But it found one important correctness item, one robustness item, a raft of doc minors, and five staging notes — none of it art. What was real, and what did hardening it turn up?
- **THE IMPORTANT ONE, AND IT GOT WORSE THE FARTHER I PULLED THE THREAD.** The module doc justified the never-fail guarantee on a FALSE premise: "the one input it forces is GAS on flat ground (level 22's finish flat zone)". But the ride-in takes the pedals at rideInX, which is 500px BEFORE the flat zone starts — on rolling terrain (hilliness 0.38), which the level config and the module's own test both state. The reviewer probed the airborne flag at 16ms across the ride-in and caught the bike LEAVING THE GROUND four times while the cutscene owned the pedals. Held pedals are never air-fresh so the measured worst case was a harmless 8°, BUT the cutscene's inputs are not always held: `setInputOverride({gas:true})` is a FRESH press on the takeover frame, and the crawl's bang-bang law toggles brake every frame — either landing on an airborne frame is a fresh mid-air press, which bike.ts's airPitchAuthority gives FULL flip torque. Fixed BOTH halves the review asked for: (1) the sentence now says the forced input is gas over rolling terrain then gas/brake in the flat zone, and the reason it is safe is that held pedals are never air-fresh; (2) HARDENED — the takeover and the per-frame crawl both gate on `!ctx.bike.airborne`, so neither forced pedal can ever become trick input. Structural, not measured.
- **WHICH THEN EXPOSED A SECOND, REALER HOLE — the one the harness actually caught.** Hardening (a) meant probing forced input against adversarial drives, and the stop-dead harness started failing for a NEW reason: forcing GAS on from a DEAD STOP part-way up one of level 22's climbs stalls the bike (measured: velocity decaying to ~2e-7 px/step, then a fail), and — the part that matters — the override then DENIES THE PLAYER THE ONE RECOVERY they'd use, rolling back for a run-up. This is a genuine never-fail violation, live, not theoretical. Fixed with a second gate: the takeover also requires the bike to be ALREADY MOVING at `ARRIVAL.takeoverMinSpeedPxPerStep` (= crawlSpeedPxPerStep). Below that the cutscene simply doesn't engage — the player keeps full control, gets themselves going as they would anywhere, and the ride-in fires the instant they are moving at least as fast as it intends to travel. And nothing is lost if it never fires: onFinish() opens the venue itself, so a player who crawls to the flag under their own power still gets the whole finale. The ride-in is a flourish, not a prerequisite. THE LESSON: a "never-fail" guarantee that forces input is only as good as the worst state you force it FROM — grounded was necessary but not sufficient; moving mattered too, and only an adversarial drive found it.
- **(e) A MATERIALISING CALEB.** `calebStepsOff()` called `ctx.passenger.hide()` (a no-op when inactive) then built the standing figure UNCONDITIONALLY — so on a save where deriveCalebPickedUp is false (level 12 never completed, reachable via the dev/harness direct-entry to level 22) Caleb would appear beside the bike and walk into the party having never been picked up. One line: `if (!ctx.passenger.active) return;`.
- **(f) THE THIRD VERBATIM REPLICA of the brown-haired Caleb figure** — identical band height, identical `-HEIGHT + BAND/2` arithmetic, identical `PALETTE.brown` rect over `TEXTURE_KEYS.caleb` — sat in pickup.ts, partyCast.ts and now arrival.ts. pixelText.ts's doc records this project's rule: it was extracted at the FOURTH replica, after the copies had diverged; don't wait for the fourth. Extracted `src/systems/calebFigure.ts` (`calebFigureParts` + `calebHairBand`, import-safe) and migrated all three. It also gives PLAN-10 one place to delete when tex-caleb gets real brown-haired art.
- **THE SIXTH INSTANCE of the failure mode I named last round, and the review named it back:** `arrival.ts`'s module doc said "(and UNLIKE decorations.ts) this module has NO runtime Phaser import" — decorations.ts has been import-safe since PLAN-07 task 3, and ui.ts is the only runtime-Phaser module left in src/systems. Copied verbatim from pickup.ts, which predates the fix — and the same stale sentence was propagating through police.ts / wheelieRider.ts / tricks.ts too. Corrected in ALL of them, because the point of naming a failure mode is to sweep the whole class, not the one instance a reviewer happened to land on.
- **The doc/precision minors, all real:** (b) the decorations.ts claim above; (c) DECISIONS.md recorded the fix using the very identifier the same bullet said was renamed away; (d) the harness said cruise "~8" where everything else measures ~9.5; (m) "exactly the discipline police.ts follows" — police.ts uses no delayedCall, so the DISCIPLINE is shared but not the mechanism (arrival.ts is the first src/systems module to schedule with delayedCall); (n) an over-long line after the ~2s->~1.6s edit; and (g) the dismount GEOMETRY sits locally as placeholder art while its TIMING sits in ARRIVAL — defensible (the offsets are tuned against placeholder sprite widths and move with the art), now stated in the ARRIVAL header.
- **The test minors, all closed by making the claim testable rather than by eyeballing:** (h) level22.ts restates the ARRIVAL defaults and claims to echo them — now pinned `expect(LEVEL22_ARRIVAL?.rideInLeadPx).toBe(ARRIVAL.rideInLeadPx)` and the same for the door; (i) `doorAheadOfFinishPx`'s doc says "plus half the facade" but the test checked only the doorway — `VENUE_WIDTH_PX` exported and the real bound asserted; (j) the crawl test checked `< topSpeed` but never "slower than it cruises" — both halves now, against a conservative measured-cruise floor; plus a NEW doorway-fit test (the offsets keep both figures inside the opening, so Caleb-on-the-jamb becomes a test failure next time).
- **The harness minors:** (k) `calebWalkedPx`/`gabbyWalkedPx` diffed each figure's spawn-on-the-bike against its last sample, folding in the hop-down from two different bike offsets, so they read 245/235 and the OK line looked like the equal-walk invariant had failed — now windowed from `walkInDelayMs` so it measures the WALK alone (live: 209-221 / identical), with a NEW live `|caleb - gabby| < 4` assertion so that equality is proven on screen and not only in a unit test; (l) `caughtInFlight` reported from `calebDismounted` while its gate used `gabbyDismounted` — aligned. The stop-dead SETUP also turned out genuinely flaky (braking to a dead stop lands on whatever slope it lands on, and some can't be re-climbed from rest under gas) — so it now retries across a LIST of brake points instead of hammering one, which is honest: the flakiness is the harness's hard braking, not the arrival, and the arrival is never retried once it has the pedals.
- **The five staging fixes (all screenshot-judged, none art):**
  1. **The pedals were taken 210px before the venue was on screen.** At rideInLeadPx 900 the camera's lead put the venue ~200px off the right edge at takeover, so phase 1's "the venue is already standing there ahead of them" was aspirational — the player lost control looking at empty road. Trimmed to 660 so the venue is in frame at the takeover.
  2. **The finish flag parked in the middle of the tableau** — at crawlSpeed 1.8 she coasted only ~70px past the flag, leaving a checkered race-finish marker beside the bike through every dismount frame. Raised crawlSpeed to 3 (for COMPOSITION, not pace): she now coasts ~110-150px past, leaving the flag trailing behind her at the edge of frame where it belongs.
  3. **The light pool read as bands.** Three graded rings showed three visible arcs across the road (and six still showed edges). Now GENERATED — 12 rings at a uniform low alpha from a count + a scale ramp — so raising the count smooths it further with no other edit, and the composed falloff reads as light. Same single Graphics, zero extra objects.
  4. **The archived walk-in screenshot wasn't the walk-in** — its predicate fired ~70px into a ~180px walk, catching the pair still beside the bike. Retargeted to Gabby within a body's width of the doorway centre; the shot now shows both of them in the lit doorway with the empty bike parked in the light behind them, which is the frame the whole beat is about.
  5. **Caleb stopped on the door jamb** — his outer edge was flush with the frame. Moved his stop inside the opening; the equal-walk coupling meant his LANDING had to move by the same amount, which is exactly what ARRIVAL_DISMOUNT_OFFSETS + its test now protect (and the new doorway-fit test guards).
- **How verified:** `npm run build` clean; `npm run test` **775/775 green** (772 -> 775: the level22-echoes-defaults pin, the venue-fit + crawl-vs-cruise strengthenings, the doorway-fit test, and the takeover-gate test). `node scripts/playtest-arrival.mjs` exit 0, THREE consecutive times (the setup-retry made it stable), reporting the equal walks live (209-221/identical), the new takeover gate, and the stop-dead carry-in still holding. Full re-run because the change touches forced input near the trick system AND extracts a figure used by pickup/party — all exit 0: `playtest-levels.mjs` 22-level sweep (17/17 gated levels finish gas-only with 0 restarts, level 22 in 36.9s at 99 bodies, max bodies 99, 0 errors; the two long-standing level-7 warnings are ungated and expected), `playtest-level12.mjs` and `playtest-party.mjs` (both consume the new calebFigure.ts — party avg 61 fps, 0 errors), `playtest-levelcomplete.mjs` (real level-22 finish into the party, 36.8s), `playtest-tricks.mjs gasonly` (finished 17.4s, 0 crashes, worst airborne 2 deg — the trick system is unaffected by the forced-input gating). ONE SELF-INFLICTED SNAG worth recording: the first sweep run failed with `__gabbyGame` undefined because I edited a comment WHILE it was running and Vite HMR reloaded the page out from under it — exactly the hazard the brief warns about. Re-run clean with no source edits in flight.
- **UPDATED HEADROOM:** raising crawlSpeedPxPerStep 1.8 -> 3 for composition also bought back time — level 22 now finishes in ~36.5-36.9s (was ~37.3s), so the crawl costs ~2s rather than ~3.3s. Still inside NORTH_STAR §4's 20-45s window with ~8s to spare; the previous round's headroom note (which quoted 1.8 / ~37.3s) is superseded by this one.
- **PLAN-10 items unchanged:** the venue is still a brown box wanting real art; the door/music hook is a TODO at openVenue. calebFigure.ts is the one place that collapses to a plain Image once tex-caleb gets brown-haired art.
